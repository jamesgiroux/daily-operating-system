# Daily Operating System - Setup Wizard Dependencies
#
# The setup wizard itself has minimal dependencies (Python 3.8+ standard library only).
# These are optional dependencies for enhanced functionality.

# Google API (required for /today, /week, /email-scan)
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.5.0

# YAML processing (for frontmatter parsing)
pyyaml>=6.0

# Date handling
python-dateutil>=2.8.0
