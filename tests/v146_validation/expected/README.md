# v1.4.5 Expected Evidence

This directory is reserved for synthetic expected-output snapshots for W5-B validation.

Expected files must use counts, booleans, reason codes, entity ordinals, trust-band distributions, and opaque handles. They must not include raw paths, filenames, raw source handles, raw file IDs, claim text, prompt text, output bodies, entity names, emails, domains, customer/company names, or provenance blobs.

