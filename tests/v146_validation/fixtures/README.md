# v1.4.5 Validation Fixtures

This directory is reserved for synthetic W5-B workspace fixtures. Fixtures must be generic, PII-free, and aligned with the extractor-supported source shapes named in the L0 packet.

Do not commit real workspace files, real filenames, raw paths, claim text from real data, raw source handles, raw content hashes, prompt bodies, output bodies, or provenance blobs.

