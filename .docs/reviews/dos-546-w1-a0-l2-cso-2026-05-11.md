# DOS-546 W1-A0 — L2 /cso Review

**Reviewer:** security-auditor sub-agent (per `/cso` discipline)
**Date:** 2026-05-11
**Scope:** Commit `fada3fd9` — audit-log schema + `emit_surface_audit` helper
**Verdict:** **APPROVE-WITH-REFINEMENTS**

The diff satisfies AC lines 290, 293-294 and ADR-0102 §7.6 / ADR-0111 §8 cleanly.

## STRIDE pass

1. **Repudiation (AC line 290):** Helper enforces the SurfaceClient invariant at runtime (`AuditError::SurfaceClientMissingWpUserId`, no record written on failure, dedicated test `emit_surface_audit_rejects_surface_client_without_wp_user_id`). AC permits compile-time *or* runtime — runtime gate is in-bounds.

2. **Information disclosure:** `actor_instance` / `actor_scopes` are derived from the `&Actor` variant inside the helper — the `AuditFields` builder cannot inject them (doc comment lines 124-126 explicit). `SurfaceClientId` is opaque non-PII per registry.rs §31-45. Scopes flow from `BTreeSet` via `ScopeSet::iter` (sorted-by-construction) → `serialize_scopes` produces deterministic `Vec<String>`. No caller-controlled payload reaches the actor_* columns. `wp_user_id` is typed `u64` — unforgeable to a payload-injection attack.

3. **Tampering:** `#[serde(default, skip_serializing_if = "Option::is_none")]` is correct for additive forward/back compat; legacy parse test covers it. New write path uses the same `OpenOptions::append(true)` and hash-chain advance — append-only preserved; `verify_audit_log` test passes with new fields.

4. **Drift:** Field names match AC line 372 exactly (`actor_kind`, `actor_instance`, `actor_scopes`); `actor_kind_tag` returns `"surface_client"`. Helper consumes existing `ScopeSet::iter` — no new format invented.

5. **Schema-as-JSONL gap (doc-only):** AC line 289 names a SQL-style "index on `(wp_user_id, created_at)`"; module doc lines 17-20 reframes it as JSONL grep/jq. Acceptable — W6-A owns the forensic exercise.

## Path-α refinements (file to maintenance project `b8e6aea4-d47e-4f3a-b03d-a05bec914aeb`, non-blocking)

1. **R1: Typestate / `NonZeroU64` wrapper for SurfaceClient wp_user_id.** Promote the runtime gate to compile-time once W2 endpoint extraction stabilises.
2. **R2: `rotate_audit_log` line 429 `unwrap_or_else(|_| line.clone())`** re-emits the raw legacy line if re-serialise fails — low-risk but tampering-adjacent; add a structured warn.
3. **R3: W6-A CI lint** to assert no production call site invokes `AuditLogger::append` with a category that overlaps known SurfaceClient event vocabulary.

## Verdict

**APPROVE-WITH-REFINEMENTS.** Unblock; R1-R3 as maintenance tickets.
