# L2 Codex Adversarial Review — DOS-546 W1-D

**Date:** 2026-05-11
**Commit:** `57a57e1f` (DOS-546 W1-D: ability-description CI gate + W1-D/W1-C/W1-E.1 workflow integration)
**Branch:** `dos-546-wp-studio-spike`
**Reviewer:** codex CLI (L2 adversarial, bounded by AC)
**Files:** 4 (+341 lines): `.github/workflows/test.yml`, `scripts/ability_description_vocab_blocklist.txt`, `scripts/check_ability_descriptions.sh`, `scripts/check_ability_descriptions_test.sh`

---

## Verdict: REVISE

---

## Findings

### P1 — JSON scanner reads the wrong root key (confidence 10/10)

`scripts/check_ability_descriptions.sh:189` only scans `payload["tools"]`, but `tools/dailyos-abilities.json:3` uses `abilities`. A generated description containing `intelligence pipeline` in the serialized artifact exits `0`. The MCP-facing artifact named in AC is therefore **not actually gated**.

### P1 — PII blocklist silently missing (confidence 10/10)

`scripts/check_ability_descriptions.sh:82` loads `.claude/pii-blocklist.txt` with `required=False`. This worktree has no `.claude/pii-blocklist.txt`. CI therefore silently runs **vocab-only**, not "PII + vocabulary." A marker-only Rust fixture exits `0` when the blocklist is absent. The advertised dual-blocklist contract is half-disabled in CI.

### P2 — AC drift: local pre-commit + inventory.toml not wired (confidence 9/10)

AC requires (a) local commit refusal and (b) `inventory.toml` description scanning. Commit `57a57e1f` does not wire the ability-description script into the local pre-commit gate (`.claude/hooks/pre-commit-gate.sh`), and the scanner only handles `.rs` and `.json` paths. An `inventory.toml` with `description = "uses intelligence pipeline"` exits `0`. Reproduced.

### P2 — Raw Rust string descriptions evade the scanner (confidence 8/10)

The `#[ability(...)]` macro accepts `LitStr`, so `description = r#"..."#` is valid Rust. `RUST_DESCRIPTION_RE` at `scripts/check_ability_descriptions.sh:133` only matches ordinary quoted strings. A raw-string fixture containing `intelligence pipeline` exits `0`. Adversarial bypass present today; would become a real evasion the moment any author uses a raw string literal.

---

## Other verification (passed)

- Workflow ordering correct: W1-D, W1-C inventory, W1-E.1 authorship steps all after "Enforce durable source comments" in `.github/workflows/test.yml:84`.
- Vocab blocklist contains the 7 required seed terms (enrichment, AI enrichment, intelligence pipeline, pipeline run, prompt fingerprint, claim writer, trust band scoring).
- No real customer names in fixtures — uses the fictional marker `pii-fixture-marker` per CLAUDE.md PII rule.
- `scripts/check_ability_descriptions_test.sh` happy-path passes (both bad-trips-1 and clean-exits-0 assertions hold).
- Scope discipline confirmed: commit touches only workflow + scripts + blocklist; no implementation churn.
- Companion gates `check_ability_inventory.sh` and `check_composition_authorship.sh` run clean against current tree.

---

## Recommended fixes before re-review

1. **P1 JSON root key:** scan both `payload["tools"]` and `payload["abilities"]` (or pick the canonical key after inspecting `tools/dailyos-abilities.json`).
2. **P1 PII blocklist absent:** treat a missing `.claude/pii-blocklist.txt` in CI as a configuration error (exit 1), or commit a CI-environment copy under `.github/`/repo-tracked path so CI is never vocab-only. Either way, the dual-blocklist contract must be enforced not silently downgraded.
3. **P2 inventory.toml + raw strings:** extend the scanner to handle `.toml` description fields and Rust raw string literals (`r#"..."#`, `r##"..."##`). Add adversarial fixtures to `check_ability_descriptions_test.sh` covering both cases.
4. **P2 local pre-commit:** wire `check_ability_descriptions.sh` into `.claude/hooks/pre-commit-gate.sh` (or document why the gate is CI-only and adjust AC).

After fixes, re-run `check_ability_descriptions_test.sh` with new adversarial fixtures and re-submit for L2.
