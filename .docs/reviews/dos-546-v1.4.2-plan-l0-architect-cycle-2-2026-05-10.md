# L0 Architecture Review — v1.4.2 (Personal Intelligence Engine: WordPress Foundation) — Cycle 2

**Reviewer:** architect-reviewer
**Date:** 2026-05-10
**Cycle:** 2 (post cycle-1 revisions)
**Cycle-1 verdict:** APPROVE with 6 blocking findings (A1, A2, B1, B2, D1, D2) + 7 non-blocking observations.
**Scope:** Verify cycle-1 blocker closure on the revised plan packet (25 issues; new W0-D, W1-A0, W4-A0). Spot-check for new contradictions or regressions introduced by the revision pass.

---

## Verdict

**APPROVE.** All six cycle-1 blockers (A1, A2, B1, B2, D1, D2) close cleanly. The substrate coherence is preserved through the ADR-0102 amendment chain (W0-D → W1-B → consumed by W4-A0 and W3-C). The three new issues (W0-D, W1-A0, W4-A0) are SPEC-TEMPLATE-conformant, dependency-clean, and load-bearing — they are NOT scope expansion, they are scope discovery (per `feedback_think_in_dod_and_outcomes_not_ticket_scope`). Wave ordering remains correct; wall-clock estimate unchanged at ~6 weeks because the new issues fan out parallel inside their host waves.

Three trivial doc-internal count contradictions surfaced (W0/W1/W4 prose lines still read "Three/Five issues/agents" while the summary table and §"Wave shape" correctly read 4/6/6). These are cosmetic and do not block kickoff; file as a single one-line maintenance ticket.

---

## Blocker closure verification

### A1 — `mcp_exposure` tri-state ADR amendment — CLOSED

- W0-D (02-issues.md:203-261) authors the ADR-0102 §7.1 amendment to tri-state `McpExposure { None | MetadataOnly | Invocable }`, retires `client_side_executable`, and pins the SurfaceClient compile-error gate. Acceptance is documentation-only with named consequences (§7.1, §7.6, default-policy bullet, artifact 05 status note, ADR-0111 §8 cross-ref).
- W1-B (02-issues.md:447) consumes the amended ADR text: `McpExposure` enum, no `client_side_executable`, default `None`.
- 03-wave-plan.md:106 lists W0-D in the W0 wave; line 153 of the CI invariants table grep-bans `client_side_executable` in source.
- L0 open question §2 (03-wave-plan.md:602) explicitly closed: "Per cycle-2 decision: `mcp_exposure: McpExposure` is the canonical tri-state."

Nothing in the revised plan still references `mcp_exposure: bool` or a separate `client_side_executable` field as a live shape. Closure is complete.

### A2 — `mcp_exposure` vs allowlist precedence — CLOSED (with one micro-tightening recommended, not blocking)

- W3-C (02-issues.md:1186) names "Read-mostly default: the allowlist defaults to ability `mcp_exposure: Invocable` ∩ ability `category: Read | Transform`. Publish/Maintenance abilities are not in the default allowlist; promotion to allowlist is per-ability with explicit ADR review."
- Line 1180 has the `mcp_exposure: None` negative fixture; line 1183 has the default-server-exposes-nothing negative fixture.

The intersection language makes `mcp_exposure` necessary-but-not-sufficient (matches the architect resolution); the negative fixture for `Invocable + category: Maintenance` is implicit in "Publish/Maintenance not in default allowlist." Closure is functional. Suggested micro-tightening (NOT blocking): add one explicit negative fixture line "Ability with `mcp_exposure: Invocable` AND `category: Maintenance` is NOT enumerated by the custom MCP server by default." This is a single acceptance bullet, can be filed at W3-C L0 plan, not at L0 cycle-2 gate.

### B1 — W3-C dependency under-statement — CLOSED

03-wave-plan.md:129 now reads verbatim: "**W3-A gates both W3-B and W3-C. W3-B additionally depends on W2 full merge. W3-C additionally depends on W2 full merge + W1-B + W1-C merges** (W1-B + W1-C precede W2 in the default merge-gate-on-prior-wave rule, but stated explicitly per architect cycle-1 finding B1 because the dependency is load-bearing)." This is the exact wording the finding asked for. A code agent reading the wave plan cannot misorder W3-C now.

### B2 — W2-D rate-limit calibration as path-α maintenance — CLOSED

W2-D (02-issues.md:980) carries the explicit bullet: "W4's L3 wave adversarial includes a calibration pass that MAY amend axis-4 and axis-5 thresholds. Any amendments land via Linear tickets against the Codebase Maintenance & Production Quality project (`b8e6aea4-d47e-4f3a-b03d-a05bec914aeb`), not via re-opening this issue." This is the path-α transfer rule from `feedback_l2_path_alpha_to_maintenance_project` applied correctly with the maintenance project ID inline.

### D1 — Markdown projection writer ownership — CLOSED via option (c)

- 01-project-description.md:80 rewritten: "Markdown filesystem as durable read-side archive — substrate is the authority and writes the projected markdown file on claim change ... markdown→substrate read-side detection is in scope (W4-C signature verification quarantines out-of-band markdown edits). Bidirectional markdown-as-input reconciliation ... is explicitly v1.4.6 scope."
- DoD §3 (line 140) matches: "Edit-in-WP and concurrent-tab scenarios ... pass; out-of-band edits in either WP DB or markdown ... detected, quarantined, and never silently promoted. Bidirectional markdown↔substrate edit propagation is named as v1.4.6 (Workspace Memory Refactor) scope and is NOT a v1.4.2 DoD criterion."
- Non-goals (line 108) carries the explicit deferral.
- W4-C (02-issues.md:1506, 1518) covers substrate→markdown signature emission + out-of-band-markdown-edit detection.

The DoD now matches the scope sections. The "wire existing substrate, don't ship empty-branch" trap is avoided because substrate→markdown emission IS in scope (W4-C); only bidirectional markdown-as-input is deferred, and that deferral is canonical to the v1.4.6 project, not a v1.4.2 dodge.

### D2 — `dailyos/account-overview` producer ownership — CLOSED

W4-A0 (02-issues.md:1231-1311) is a fully-formed new issue. Acceptance criteria are substantive: ability declaration with canonical macro attrs (`allowed_actors: [User, SurfaceClient]`, `required_scopes: [read.account_overview]`, `mcp_exposure: Invocable`), `AbilityOutput<Composition>` shape per artifact 14, claim_refs resolution, ProvenanceRef, trust band data populated, unit + eval tests, 7 enumerated edge cases.

Wave plan (03-wave-plan.md:130) places W4-A0 in W4 stage-2 (parallel with W4-C/D/E), and W4-A renderer in stage-3 depending on W4-A0. The producer/renderer split is now structurally real: W4-A0 produces, W4-A consumes. The load-bearing architectural claim is empirically verifiable at W6 close.

The L0 cycle-2 grep at 02-issues.md:1240 explicitly records: "A grep across `src-tauri/`, `src/`, and `abilities-runtime/` (run at L0 cycle-2) returns no matches for `account.overview`, `account_overview`, `AccountOverview`, `prepare_account_overview`, or `get_account_overview`." Producer non-existence is documented; the new issue is the answer.

---

## New issue quality (W0-D, W1-A0, W4-A0)

### W0-D — ADR-0102 §7.1 amendment

- SPEC-TEMPLATE conformant: Problem, Why now, Scope limits, Acceptance criteria, Intelligence Loop fit, Architectural surfaces, Edge cases, Dependencies, Shape.
- Intelligence Loop fit: all N-A — correctly honest (it's a documentation issue).
- Wave ordering: stage-1 W0 issue, independent of W0-A/B/C, must merge before W1-B starts. Plan correctly states this at 02-issues.md:253 and 03-wave-plan.md:203.
- No regression risk: acceptance is doc-only; W1-B is the code consumer.

### W1-A0 — Audit-log schema + emission contract

- SPEC-TEMPLATE conformant. Acceptance criteria are substantive (schema migration, helper signature, round-trip plumbing skeleton, binding contract for W2-W5 emission sites).
- Intelligence Loop fit: Feedback hook noted as "Touched indirectly" — honest; signals/health/intel-context/briefing correctly N-A or indirect.
- Architect cycle-1 spot-check on audit attribution (Category 1 §Pass bullet) noted this exact problem ("W6-A's CI lint ... unforgeable; combined with W1-A's mandatory audit emission ... and W2-C's pairing audit events, the forensic chain is complete"). Promoting the schema to W1 stage-1 makes the chain complete from inception rather than retrofitted in W6.
- Wave ordering: 03-wave-plan.md:127 places W1-A0 in W1 stage-1 alongside W1-A and W1-B (file-disjoint at the function level: `audit_log` migration + helper module vs `actor.rs` vs `policy.rs`).
- Cross-wave binding clean: W2-B/C/D, W3-C, W4-E, W5-A acceptance lines all bind to the `emit_surface_audit` helper rather than direct row writes. Spot-checked W2-D (line 977) — audit emission references the helper boundary; W4-C (line 1516) explicitly says "writes a `tamper_detected` audit event (via the W1-A0 `emit_surface_audit` helper)".
- W6-A scope reduction (02-issues.md:1940-2002) cleanly removes the schema migration and rewrites the issue around forensic-query exercise + CI lint + production hardening only. The dependency note at line 2000 explicitly states "Schema columns + helper are W1-A0's scope and are already merged before this issue starts."

### W4-A0 — `dailyos/account-overview` ability (producer)

- SPEC-TEMPLATE conformant. Acceptance criteria are substantive (10 bullets including ability declaration, input/output types, Composition shape per artifact 14, trust band data, ProvenanceRef, source attribution per ADR-0105, 4 unit-test cases, eval-harness fixture).
- Intelligence Loop fit: Signals (touched via claim-version invalidation), trust-scoring consumption, intel context usage, feedback hook (Composition's claim_refs are the feedback target) — all answered with substance, none perfunctory.
- Wave ordering: stage-2 of W4, depends on W1-A (actor), W1-B (policy), W1-E (Composition types), W4-B (composition_version). Stage-3 W4-A renderer depends on W4-A0 + W3-B merged.
- Architectural surfaces touched: 8 surfaces checked (services, abilities, provenance, claims, signals, eval, surface-parity, privacy-rendering) — substantive cross-ADR awareness consistent with the cycle-1 spot-check quality bar.

---

## New regressions spot-check

Spot-checked 3 issues whose acceptance criteria were modified in cycle 2: W1-B (mcp_exposure + required_scopes default semantics), W2-D (post-hoc calibration line), W4-C (Ed25519 rewrite).

### W1-B (02-issues.md:444-456) — clean

- Tri-state `McpExposure` consumed; `client_side_executable` retired.
- Macro compile-error invariant on `allowed_actors: [SurfaceClient]` + empty `required_scopes` + no `no_scope_required` is codified (line 449) with a named negative test (line 454).
- Backward compat preserved (line 456).
- No contradiction with W0-D ADR text; the issue references "as amended in W0-D" explicitly at line 446 + 494.

### W2-D (02-issues.md:947-1013) — clean

- The calibration acceptance bullet (line 980) names the maintenance project by ID and the W4 L3 calibration pass as the trigger.
- Five axes still enumerated; no contradictions with artifact 09.
- Audit emission (line 977) carries `surface_client_id` + `wp_user_id` — implicitly binds to W1-A0's helper; minor nit: the bullet could explicitly call out the helper, but the wave-plan §"CI-enforced invariants" table (03-wave-plan.md:152) makes this binding load-bearing already. Not a regression.

### W4-C (02-issues.md:1490-1569) — clean

- Ed25519 + RFC 8785 + domain separator + `key_id` + offline verification — all match artifact 03 verbatim.
- `projection_signatures` renamed `projection_ledger` per artifact 03 §"runtime ledger" — Shape B table at line 1565 carries the canonical column set.
- ADR-0108 64KB cap respected (line 1519): provenance envelope carries only references; canonical signed payload lives in `projection_ledger`.
- Tamper-event audit emission (line 1516) explicitly via the W1-A0 helper — correct binding.
- No HMAC references remain anywhere in W4-C; cross-check on the wave plan (03-wave-plan.md:391) reads "tamper detection — Ed25519 signatures per Phase 0 artifact 03."

---

## Substrate coherence — ADR amendment chain

The cycle-2 amendment chain is: **W0-D (ADR-0102 §7.1 + §7.6 amendment) → W1-B (consumes tri-state + codifies compile-error gate) → W3-C (consumes `mcp_exposure: Invocable | MetadataOnly`) + W4-A0 (declares `mcp_exposure: Invocable` per the amended ADR) + W4-C ProvenanceEnvelope refers to W1-A0 audit-emission helper bound to ADR-0111 §8 actor attribution**.

No other ADRs are touched in a way that would break:
- ADR-0111 §8 cross-reference is updated as part of W0-D acceptance (line 228) — no drift.
- ADR-0130 §1, §2, §5 (substrate-owned authorship + ProvenanceRef) — untouched; W1-E still load-bearing for "only ability code constructs Composition."
- ADR-0105 (provenance envelope) — untouched; W1-E ProvenanceRef + W4-C signature-ref-outside-envelope respect the 64KB cap.
- ADR-0108 (sensitivity rendering) — composability with `mcp_exposure` is honored (W1-B Privacy Rendering note); architect cycle-1 finding D3 remains non-blocking and rendered correctly via render-time gating per existing ADR text.
- ADR-0125 (sensitivity tiers) — untouched; cycle-1 D3 path-α'd correctly.
- ADR-0129 (WP Studio as primary surface) — unchanged; the producer/renderer split now structurally proves the §4 MCP allowlist policy.

The amendment chain is acyclic, ordered (W0 → W1 → W2/W3/W4), and CI-enforced at multiple points (`Composition` constructed-only-by-ability; `Block.provenance: ProvenanceRef`; `client_side_executable` grep-banned; `emit_surface_audit` lint on SurfaceClient sites; tri-state enum shape type test). No new ADR will be invalidated by the cycle-2 changes.

---

## Non-blocking observations (cycle-2 specific)

### N1 — Doc-internal count contradictions (cosmetic)

Three lines in 03-wave-plan.md still read pre-cycle-2 counts:

- Line 174: "Three issues. Two are documentation ... One is project hygiene." Should read "Four issues. Three are documentation (W0-A, W0-C, W0-D). One is project hygiene (W0-B)."
- Line 219: "Five agents in parallel." Should read "Six agents in parallel (two-stage internal ordering)."
- Line 381: "Five agents." Should read "Six agents."

Plus 02-issues.md:18 header reads "Wave 0 — Contract lock + supersession (3 issues)" while there are 4 issues in the wave.

The summary table (03-wave-plan.md:106-114) and the §"Wave shape" §-line content are correct. The contradiction is purely in prose lead-ins. Fix in one micro-PR or fold into W0-A's L0 hardening pass — not blocking kickoff.

### N2 — W3-C precedence sentence not literal

A2 close is functional but the architect's exact request was "State this precedence in the W3-C acceptance criteria as a single sentence and add a negative fixture that publishes `mcp_exposure: Invocable + category: Maintenance` and asserts the WP MCP server does NOT enumerate it." The intersection language at line 1186 is logically equivalent but does not literally read as a precedence sentence and does not literally name the `Invocable + Maintenance` negative fixture. Recommend adding both at W3-C L0 plan (one acceptance bullet + one fixture). Not blocking — the functional gate is in place via the intersection rule.

### N3 — W3-A WP 7.0 deferral removal

The revision summary says W0-D closes the WP 7.0 client-side `executeAbility()` question (architect C1). Confirmed at 03-wave-plan.md:602: open question §2 explicitly closed. No regression.

### N4 — Bundle 24 propagation

The "1-23 → 1-24" sweep is consistent across:
- 01-project-description.md:140 (DoD §3 mention)
- 03-wave-plan.md:87 ("Six new fixture bundles (19-24)"), :114 (W6 fan-out 2 agents), :154 (CI invariants "Bundles 1-24 mandatory"), :487 (W5 L3 wave adversarial), :521 + :647 (release gate).
- 02-issues.md:477 (W5-C scope) — already correct.

Sweep is clean.

---

## Final summary

| Cycle-1 blocker | Status | Mechanism |
|---|---|---|
| A1 mcp_exposure tri-state | **CLOSED** | W0-D ADR amendment merges before W1-B |
| A2 mcp_exposure ∩ allowlist precedence | **CLOSED** | W3-C intersection rule; suggested micro-tightening at W3-C L0 (non-blocking) |
| B1 W3-C dependency under-statement | **CLOSED** | Wave plan §"W3 internal staging" carries the load-bearing dependency verbatim |
| B2 W2-D post-hoc calibration | **CLOSED** | Path-α maintenance transfer with project ID inline |
| D1 Markdown projection writer | **CLOSED via option (c)** | DoD §3 + Scope §Three-view + Non-goals + W4-C all reconciled |
| D2 account-overview producer | **CLOSED** | W4-A0 new issue, grep-confirmed non-existence, full spec |

All six cycle-1 blockers close. Three new issues (W0-D, W1-A0, W4-A0) are SPEC-TEMPLATE conformant, load-bearing, and substrate-coherent. The amendment chain (ADR-0102 §7.1 → W1-B → W3-C + W4-A0) is acyclic and CI-enforced. Three trivial doc-internal count contradictions surfaced and are cosmetic only.

**Verdict: APPROVE.** The plan is ready for W0 kickoff. The N1 prose count contradictions are filed as a single one-line maintenance ticket (or folded into W0-A's L0 hardening pass); N2 is an L0-plan-time tightening for W3-C, not a cycle-2 gate.

---

**End of L0 architecture review — cycle 2.**
