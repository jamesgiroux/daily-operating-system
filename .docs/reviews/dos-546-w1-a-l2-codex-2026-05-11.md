# DOS-546 W1-A — L2 Codex Adversarial Review

- **Commit:** `7fba6a22` (DOS-546 W1-A: SurfaceClient as the fourth actor class)
- **Branch:** `dos-546-wp-studio-spike`
- **Reviewer:** codex (`codex-cli 0.128.0`, default model)
- **Date:** 2026-05-11
- **Bound:** W1-A acceptance criteria (`.docs/plans/dos-546/v1.4.2-project/02-issues.md`)
- **Substrate:** ADR-0102 §7.1/§7.6, ADR-0111 §8, Phase-0 artifact 05 lines 383-412

## Verdict: APPROVE

No REVISE-class divergences found against the bounded W1-A AC for commit `7fba6a22`.

## Evidence

- `Actor::SurfaceClient(SurfaceClientId)` is present and identity-only, with W1-B scope grants deferred: `src-tauri/abilities-runtime/src/abilities/registry.rs:100-121`.
- `SurfaceClientId` and `SurfaceScope` have the requested derives, `#[serde(transparent)]`, `Display`, and `as_str`: `registry.rs:43-63`, `registry.rs:78-98`.
- Match plumbing uses explicit clones or W1-B+ `todo!()` arms, not silent default arms:
  - `src-tauri/abilities-runtime/src/abilities/get_entity_context.rs:396`
  - `get_entity_context.rs:431-435`
  - `get_entity_context.rs:471-473`
  - `src-tauri/abilities-runtime/src/abilities/prepare_meeting/synthesis.rs:1552`
  - `prepare_meeting/synthesis.rs:1572-1575`
  - `src-tauri/src/bridges/mcp.rs:732-734`
  - `src-tauri/src/bridges/tauri.rs:632-634`
  - `src-tauri/src/bridges/types.rs:59-62`
  - `src-tauri/src/operations/mod.rs:251`
- The ten unit tests cover identity/Display, serde-transparent, Hash+Eq, Actor clone/serde round-trip, distinct-instance inequality, and negative `.contains`: `registry.rs:1794-1899`.
- No W1-B `AbilityPolicy` schema or W1-A0 audit schema expansion exists in the target commit; `AbilityPolicy` remains the pre-W1-B shape at `registry.rs:124-131`.

## Reviewer Note

Codex reviewed the `7fba6a22` commit blob, not the current worktree. The worktree has additional dirty changes — including `registry.rs` changes that appear to expand `AbilityPolicy` — which were ignored for this verdict. Tests were not rerun because the sandbox is read-only (L1 reported `cargo clippy -- -D warnings` and `cargo test --lib` both green, 255 tests pass).

## Findings

None.

## Path-α (maintenance, non-blocking)

None surfaced.
