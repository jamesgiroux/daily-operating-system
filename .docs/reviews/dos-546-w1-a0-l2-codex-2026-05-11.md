# DOS-546 W1-A0 — L2 Codex Adversarial Review

- **Commit:** `fada3fd9` — DOS-546 W1-A0: audit-log schema for SurfaceClient attribution + `emit_surface_audit` helper
- **Scope:** `src-tauri/src/audit_log.rs` only, +424/-4
- **Branch:** `dos-546-wp-studio-spike`
- **Reviewer:** codex (`codex-cli 0.128.0`, default model), `codex review --commit fada3fd9`
- **Date:** 2026-05-11
- **Bound:** W1-A0 acceptance criteria (`.docs/plans/dos-546/v1.4.2-project/02-issues.md` lines 283–294)
- **Substrate:** ADR-0102 §7.6 audit-attribution; ADR-0111 §8 audit-attribution requirement; W1-A.1 (`0e98377c` — `Actor::SurfaceClient { instance, scopes: ScopeSet }`); W1-B.1 (`a03a9e1f` — `ActorKind` discriminator, `AbilityPolicy` schema).

## Verdict: APPROVE

Codex final: *"No actionable defects were found in the commit. The new audit attribution fields are backward-compatible, and the helper enforces the SurfaceClient wp_user_id invariant without changing legacy append behavior."*

No REVISE-class divergences against bounded W1-A0 AC.

## AC-bound evidence

1. **Additive optional fields (AC line 287, 372).** `AuditRecord` gains `actor_kind: Option<String>`, `actor_instance: Option<SurfaceClientId>`, `wp_user_id: Option<u64>`, `actor_scopes: Option<Vec<String>>`, each `#[serde(default, skip_serializing_if = "Option::is_none")]`. Forward + backward compat for legacy JSONL preserved; non-attributed writes omit keys entirely. Test `legacy_records_without_actor_fields_still_parse` pins it. ✅
2. **Helper invariant (AC line 290).** `emit_surface_audit(logger, event_kind, actor, fields)` rejects `Actor::SurfaceClient { instance, scopes }` with `fields.wp_user_id = None`, returning `AuditError::SurfaceClientMissingWpUserId` and writing NO record. Test `emit_surface_audit_rejects_surface_client_without_wp_user_id` asserts both error AND empty record set. ✅
3. **Drift-proof derivation (AC line 292).** `actor_instance` and `actor_scopes` are derived from the `&Actor::SurfaceClient` variant inside `append_with_actor`; the `AuditFields` builder rejects them from callers by construction (only `event_kind` + `wp_user_id` are caller-supplied). ✅
4. **Stray-wp_user_id drop (AC line 293).** Non-SurfaceClient actors get `actor_kind` tagged; `wp_user_id` set on a non-SC call is silently dropped (matches AC's "drop, not promote"). Test `emit_surface_audit_drops_wp_user_id_for_non_surface_client` pins it. ✅
5. **Test count (AC line 294).** 6+ new unit tests cover OK path, reject + no-record, non-SC OK without wp_user_id, non-SC stray drop, JSONL round-trip with hash-chain re-verify, legacy parse via serde defaults. ✅
6. **Scope discipline.** Downstream call-sites at W2-C/W2-D/W3-C/W4-E/W5-A NOT wired here — they consume the helper later per the wave plan. Commit body explicit. ✅
7. **(wp_user_id, created_at) index (AC line 289).** Documented as JSONL-backed forensic grep/jq addressable; module doc lines 17–20 reframe the SQL-style "index" wording from AC as the right primitive for an append-only JSONL store. W6-A wires query templates. ✅

## Findings

None blocking.

## Path-α (file to maintenance project `b8e6aea4-d47e-4f3a-b03d-a05bec914aeb`, non-blocking)

None surfaced by codex. The `/cso` sibling review (`dos-546-w1-a0-l2-cso-2026-05-11.md`) filed three path-α refinements (typestate/`NonZeroU64` wrapper for SurfaceClient `wp_user_id`; structured warn on `rotate_audit_log` re-serialise failure; W6-A CI lint against `AuditLogger::append` overlap with SurfaceClient event vocab). Codex L2 surfaces no additional path-α items beyond those.

## Reviewer note

Codex reviewed the `fada3fd9` commit blob via `--commit fada3fd9`. The other two L2 reviewers — code-reviewer and `/cso` — agree on APPROVE (code-reviewer unconditionally, `/cso` APPROVE-WITH-REFINEMENTS path-α). Three-of-three unanimous APPROVE bounded by AC. Ready for PR.
