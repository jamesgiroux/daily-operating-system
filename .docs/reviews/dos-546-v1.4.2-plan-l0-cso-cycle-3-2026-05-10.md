# DOS-546 v1.4.2 Plan — Cycle-3 L0 /cso Review

**Reviewer:** security-auditor sub-agent (per `/cso` discipline)
**Date:** 2026-05-10
**Scope:** STRIDE pass on cycle-3 security-relevant deltas
**Verdict:** **APPROVE**

Cycle-3 deltas preserve the cycle-2 threat model and tighten two trust boundaries.

---

## Delta 1 — `client_side_executable` re-separation (W0-D + W1-B)

Confirmed at `02-issues.md:446-451`. Macro compile-error gate still fires on `allowed_actors: [SurfaceClient]` + empty `required_scopes` without `no_scope_required`. The second gate at SurfaceClientBridge (`PolicyError::ClientInvocationDisabled`) is additive, not bypass-introducing — it rejects when `client_side_executable: false` even if actor+scope check passes.

**Effective gate order (verified against W1-B + W2-A + W2-B + W1-A0):**

Host → Origin → HMAC verify (nonce + freshness) → rate limit → auth/actor resolve → registry lookup → `allowed_actors` ∩ caller → `required_scopes` ⊆ caller scopes → `client_side_executable` (SurfaceClient path) → `mcp_exposure` filter (MCP path) → dispatch → audit.

No bypass class introduced; the two-field separation closes the rigidity Phase 0 artifact 05 lines 389-412 named.

---

## Delta 2 — W4-C key lifecycle lift (`02-issues.md:1517-1524`)

All seven required surfaces covered:
- (a) Rotation cadence implicit via revocation+replacement provisioning
- (b) Revocation path explicit with `KeyRevoked` failure
- (c) Refresh on unknown `key_id` via keyring fetch
- (d) Re-signing walks `projection_ledger`
- (e) Keyring distribution endpoint `GET /v1/surface/keyring` paired + HMAC-gated
- (f) Retired-key public keys retained with `status: retired` for historical verification
- (g) Revoked-key tamper invariant preserved (`signature valid but key revoked → tamper banner`, artifact 03 line 386)

Synchronous-vs-queued re-sign deferred to maintenance project — acceptable path-α routing.

---

## Delta 3 — W2-A Origin guard PHP-curl-primary (`02-issues.md:744-745`)

Absent-Origin as primary does **not** relax the trust boundary because:
- (a) `Host` header guard still requires exact `127.0.0.1:<bound_port>` match
- (b) HMAC signing with per-session key + nonce + freshness window still mandatory
- (c) `site_url`-match retained as defense-in-depth backup
- (d) `Origin: null` still explicitly rejected

A browser cannot omit Origin on cross-origin POSTs to the loopback (browsers auto-attach Origin on POST); CORS-preflight rejection is structural. No new spoofing class — the change reconciles W2-A with the W4-E transport model (browser → WP REST → PHP cURL → loopback) which was already canonical.

---

## Verdict

**APPROVE.** No new gaps. Two non-blocking observations:

1. **Sync-vs-queued re-sign open question** — file the v1.4.3 maintenance ticket at W4-C kickoff, not after merge. Routes to project `b8e6aea4-d47e-4f3a-b03d-a05bec914aeb`.

2. **`GET /v1/surface/keyring` route surface** — W4-C adds a fifth route; W2-A originally enumerated four. **Fixed inline during cycle-3 reconciliation:** route added to W2-A's enumeration so Host/Origin/HMAC gates compose uniformly.

/cso continues as default L0 + L2 panelist for trust-boundary issues.
