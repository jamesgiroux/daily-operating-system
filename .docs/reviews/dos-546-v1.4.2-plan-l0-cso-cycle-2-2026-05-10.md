# DOS-546 v1.4.2 Plan — Cycle-2 L0 /cso Review

**Reviewer:** security-auditor sub-agent (per `/cso` discipline)
**Date:** 2026-05-10
**Scope:** Verify cycle-1 path-α observations closed + STRIDE pass on cycle-2 deltas
**Verdict:** **APPROVE**

---

## Cycle-1 path-α observations — all closed

1. **Bundle cardinality reconciled to 19-24 / 1-24.** Confirmed across `01-project-description.md:149`, `02-issues.md:1903`, `03-wave-plan.md:87,131,154,477,479,486,521,647,683`. Bundle 24 (audit attribution) promoted to mandatory — consistent with the audit schema being promoted to W1-A0.

2. **W2-A Origin guard rewritten as positive allowlist** at `02-issues.md:744` — accepts only empty/absent OR `site_url`-matching origin; rejects `Origin: null`; ordering "before auth, rate limits, ability lookup, feedback handling" preserved.

3. **W2-B HMAC freshness numerically bound** at `02-issues.md:825` — 30s stale / 5s future, typed errors `timestamp_stale` / `timestamp_future`, "config-driven, centrally configurable, not float-literal'd in handler code." Matches artifact 08 lines 208/251-252/278-279 verbatim.

---

## STRIDE pass on cycle-2 deltas

### W4-C Ed25519 algorithm change preserves threat-model defenses

- Private key in OS keychain (`02-issues.md:1513`); public key embedded in WP block attribute + markdown HTML comment for offline verify (no runtime round-trip)
- `key_id` is opaque-not-path; domain separator `dailyos.wp_studio.projection.v1`
- `runtime_anchor_id` binding survives reinstall/restore
- Retired-`key_id` public keys remain published for historical verification (`02-issues.md:1548`)
- **Net:** Ed25519 strictly improves the cycle-1 HMAC contradiction — verification no longer requires shared secret and public key custody is information-disclosure-safe (public keys leaking is the expected state)

### W1-A0 audit schema — both `actor_instance` AND `wp_user_id` from inception

- `02-issues.md:289`: schema columns committed
- `emit_surface_audit` helper as canonical writer (line 290)
- W2-C/W2-D/W3-C/W4-E/W5-A bound to the helper (line 292)
- Index on `(wp_user_id, created_at)` for forensic queries
- W6-A keeps the forensic exercise + CI lint + production hardening
- **Net:** CI lint at W6-A now has rails to enforce against from first W2 emission

### HMAC key custody escaped `wp_options`

- W3-B (`02-issues.md:1116`) requires WP-side retrieval ONLY via `dailyos_wp_bridge_session_key` filter at request time, gated by `manage_options` AND single-use runtime-issued handshake gate `dailyos_pairing_handshake_complete:<pairing_id>:<session_id>`
- Plugin MUST NOT persist key in `wp_options`/transients/postmeta/block attributes/browser storage
- Grep CI check asserts no persistence path in plugin source (line 1120)
- **Survives WP-admin compromise:** without the runtime-issued handshake gate (consumed single-use), an attacker with `manage_options` alone cannot retrieve the key — the second factor is the runtime side, which a WP-admin compromise does not own
- WP admin CSRF + nonces on pairing/revoke forms (line 1117) blocks the cross-site-forgery vector

### `required_scopes` default-empty semantics closed by compile-error gate

- `02-issues.md:449,454`: default `vec![]` valid only for abilities NOT exposed to `SurfaceClient`
- Negative fixture asserts compile failure when `allowed_actors: [User, SurfaceClient]` + empty `required_scopes` + no `no_scope_required` opt-out

### New issues (W0-D, W1-A0, W4-A0) — no new trust-boundary gaps

- W0-D amends ADR-0102 cleanly (tri-state `McpExposure`, retires `client_side_executable`)
- W1-A0 schema is comprehensive (columns + index + helper + round-trip + edge cases)
- W4-A0 is producer-side, no SurfaceClient surface, trust-boundary impact zero

---

## Verdict

**APPROVE.** All cycle-1 path-α items closed; all new cycle-2 surfaces (Ed25519 swap, audit-attribution promotion, key-custody escape from `wp_options`, scope compile-error gate) preserve or strengthen the threat model.

Two doc-only inconsistencies flagged inline and fixed during cycle-2 reconciliation (W2-B clock-skew line + W4-C `projection_signatures`→`projection_ledger`). No outstanding blockers.

/cso continues as default L0 panelist for trust-boundary issues AND default L2 panelist for trust-boundary diffs across W1-A/A0/B/E, W2-A/B/C/D, W3-B/C, W4-C/E, W5-A.
