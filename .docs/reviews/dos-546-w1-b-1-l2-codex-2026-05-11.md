# DOS-546 W1-B.1 — L2 Cycle-2 Codex Review

- **Commit:** `a03a9e1f` — close codex L2 findings (defaults + macro positive path via ActorKind)
- **Reviewer:** codex CLI (read-only sandbox)
- **Date:** 2026-05-11
- **Cycle-1 reference:** `.docs/reviews/dos-546-w1-b-l2-codex-2026-05-11.md` (REVISE — F1 + F2)

## Verdict: APPROVE

No blocking findings remain.

## Findings closed

### F1 — `AbilityPolicy::default()` defaults
**Closed.** `AbilityPolicy::default()` now ships `allowed_actors: &[ActorKind::User]`, not empty. The default test asserts that exact invariant.

- `src-tauri/abilities-runtime/src/abilities/registry.rs:433` — `Default` impl
- `src-tauri/abilities-runtime/src/abilities/registry.rs:2330` — `ability_policy_default_has_closed_w1b_defaults` test asserting `assert_eq!(policy.allowed_actors, &[ActorKind::User])`

Note: actual repo path is `src-tauri/abilities-runtime/...`, not `src-tauri/crates/abilities/...` as the dispatch brief stated.

### F2 — Macro positive path for `SurfaceClient`
**Closed by source inspection.**

- **Parse-time gate** (`src-tauri/abilities-macro/src/lib.rs:107`) fires only on the intended intersection: `SurfaceClient` in `allowed_actors` AND empty `required_scopes` AND no `no_scope_required` opt-out.
- **Codegen path** (`src-tauri/abilities-macro/src/lib.rs:915`) `ActorArg::SurfaceClient::registry_expr()` now emits `crate::abilities::registry::ActorKind::SurfaceClient` instead of `compile_error!`.
- **Positive fixture** exists at `src-tauri/abilities-macro/tests/trybuild/surface_client_with_scopes_passes.rs:214`, exercising the W1-D `dailyos/account-overview` AC-line-1254 shape (`allowed_actors = [User, SurfaceClient]`, `required_scopes = ["read.account_overview"]`).
- **Trybuild runner** (`src-tauri/abilities-macro/tests/trybuild_runner.rs:5`) globs `tests/trybuild/*_passes.rs` and `*_fails.rs`, so the new fixture is automatically picked up.

## Architectural assessment

The `ActorKind` discriminator split is clean:

- `AbilityPolicy.allowed_actors: &'static [ActorKind]` — `const`-friendly, satisfies `inventory::submit!`'s `static`-only descriptor invariant.
- `Actor::SurfaceClient { instance, scopes }` — runtime variant retains instance identity and per-invocation owned scope state.
- `Actor::kind()` projection bridges the two worlds for membership checks.

Migrated gates correctly use `actor.kind()` for policy comparison:

- `src-tauri/abilities-runtime/src/abilities/registry.rs:717` — registry enumeration
- `src-tauri/abilities-runtime/src/abilities/registry.rs:1252` — invocation validation
- `src-tauri/src/bridges/types.rs:430` — bridge pre-dispatch

## Regression sweep

No migration regressions found in the 16 touched consumer/runtime-export files. `rg` confirms no stale `allowed_actors: &[Actor::...]` policy slices remain in runtime/bridge/test paths. `git show --check a03a9e1f` passed (no whitespace/conflict markers).

## Caveats

Codex ran in read-only sandbox and did not execute `cargo test` / `cargo build` / trybuild (those write build artifacts). Verification is by source inspection. Implementing agent's L1 must confirm the trybuild positive fixture compiles and the negative fixtures still fail.
