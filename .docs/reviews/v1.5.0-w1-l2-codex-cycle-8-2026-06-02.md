# v1.5.0 W1 L2 Review - Codex Cycle 8

**Date:** 2026-06-02
**Branch:** `codex/v1.5.0-w1`
**Base:** `public/dev` after W0 merge
**Command:** `codex exec review --uncommitted --ignore-user-config --ignore-rules --json -o /tmp/dailyos-w1-l2-cycle8-codex-review.txt`
**Verdict:** Request changes; blocking finding remediated in working tree, cycle 9 pending.

## Scope

Reviewed the staged W1 diff after cycle 7 remediation.

The cycle explicitly checked:

- recommendation-backed Account work actions
- projection and renderer trust/freshness metadata paths
- surface projection authorization and cache/provenance behavior
- Tauri capability and correction-routing boundaries
- prior L2 remediation trail from cycles 1-7

## Finding

| Priority | Finding | Resolution |
|---|---|---|
| P2 | Recommendation and commitment `ActionList` blocks carried `trust_band` and `source_asof` only under `items[0]`. `ProjectedBlock.trust_band` and the renderer shell read block-level metadata, so work-action blocks could display default caution/freshness instead of the claim's actual trust and source as-of. | The Account producer now emits block-level `trust_band` and `source_asof` alongside item-level action metadata. The fallback projection policy admits those root ActionList fields, and the recommendation regression asserts both producer and projected work-action blocks preserve shell-visible trust/freshness metadata. |

## Remediation Verification

| Check | Result |
|---|---|
| `cargo test --manifest-path src-tauri/abilities-runtime/Cargo.toml recommendation_claims_render_as_work_actions -- --nocapture` | Pass: 1 focused regression |
| `cargo test --manifest-path src-tauri/abilities-runtime/Cargo.toml account_overview -- --nocapture` | Pass: 11 account overview tests plus 3 filtered fallback projection tests |
| `cargo clippy --manifest-path src-tauri/abilities-runtime/Cargo.toml --all-targets -- -D warnings` | Pass |

## Cycle 9 Preconditions

Cycle 9 should verify the cycle 8 ActionList metadata regression target, inspect the re-staged W1 diff, and continue to treat the native screenshot proof as an open tooling blocker rather than a passed visual gate.
