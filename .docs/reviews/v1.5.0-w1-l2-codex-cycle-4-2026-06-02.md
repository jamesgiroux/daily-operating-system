# v1.5.0 W1 L2 Review - Codex Cycle 4

**Date:** 2026-06-02
**Branch:** `codex/v1.5.0-w1`
**Base:** `public/dev` after W0 merge
**Command:** `codex exec review --uncommitted --ignore-user-config --ignore-rules --json -o /tmp/dailyos-w1-l2-cycle4-codex-review.txt`
**Verdict:** Request changes; blocking findings remediated in working tree, cycle 5 pending.

## Scope

Reviewed the complete W1 working-tree diff after cycle 3 remediation, including staged W1 work, unstaged remediation, and untracked W1 docs/frontend artifacts.

The cycle explicitly checked:

- `useProjectedComposition` first-load and navigation state behavior
- fallback projection field allowlists for account-overview payloads
- producer compatibility with the tightened projection policy
- W1 trust-boundary handling for display bindings and computed fields

## Findings

| Priority | Finding | Resolution |
|---|---|---|
| P2 | `useProjectedComposition` masked a first-load error as an infinite loading state because the failed account id was never attributed as the loaded account. | The hook now records the current account id when a load fails and only reports the loading state when no visible error exists. `surfaces first-load errors instead of keeping the account loading` covers the retryable-error path. |
| P1 | The account-overview fallback policy admitted the whole `/vitals` array, allowing unvetted fields under each vital object to cross the surface trust boundary. | This was the second same-shape composite-root finding after the cycle 3 `/account` issue, so remediation expanded to a class-wide account-overview sweep. The fallback policy no longer admits composite roots for `/account`, `/counts_by_trust_band`, `/context`, or `/vitals`; producer bindings now enumerate safe leaf paths for display fields, trust-band counts, context entries, and vitals. Tests cover sentinel fields under those composites and a source-level guard prevents reintroducing the composite root policies. |

## Remediation Verification

| Check | Result |
|---|---|
| `pnpm exec vitest run src/hooks/useProjectedComposition.test.tsx` | Pass: 4 hook tests |
| `cargo test --manifest-path src-tauri/abilities-runtime/Cargo.toml --test dos570_fallback_projection -- --test-threads=1` | Pass: 21 fallback projection tests |
| `cargo test --manifest-path src-tauri/abilities-runtime/Cargo.toml account_overview -- --nocapture` | Pass: 10 account-overview tests, including producer projection compatibility |
| `pnpm exec vitest run src/components/composition/ReactBlockRenderer.test.tsx src/hooks/useProjectedComposition.test.tsx` | Pass: 2 files, 12 tests |
| `pnpm exec tsc --noEmit --incremental false` | Pass |
| `cargo clippy --manifest-path src-tauri/abilities-runtime/Cargo.toml --all-targets -- -D warnings` | Pass |
| `git diff --check` | Pass |
| `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` | Pass |
| `cargo test --manifest-path src-tauri/Cargo.toml --lib commands::abilities::tests::projected_composition_metrics_only_count_producer_runs -- --exact` | Pass |

## Cycle 5 Preconditions

Cycle 5 should verify the cycle 4 findings as regression targets, inspect the normalized W1 staged diff, and continue to treat the native screenshot proof as an open tooling blocker rather than a passed visual gate.
