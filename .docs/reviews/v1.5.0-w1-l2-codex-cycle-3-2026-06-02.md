# v1.5.0 W1 L2 Review - Codex Cycle 3

**Date:** 2026-06-02
**Branch:** `codex/v1.5.0-w1`
**Base:** `public/dev` after W0 merge
**Command:** `codex exec review --uncommitted --ignore-user-config --ignore-rules --json -o /tmp/dailyos-w1-l2-cycle3-codex-review.txt`
**Verdict:** Request changes; blocking findings remediated in working tree, cycle 4 pending.

## Scope

Reviewed the complete W1 working-tree diff after cycle 2 remediation, including staged W1 work, unstaged remediation, and untracked W1 docs/frontend artifacts.

The cycle explicitly checked:

- fallback projection field allowlists
- account-overview producer compatibility with projection validation
- frontend account navigation behavior
- rendered provenance and trust-band serialization shape
- W1 ability-surface and service-context paths

## Findings

| Priority | Finding | Resolution |
|---|---|---|
| P2 | The `account_overview` fallback projection rule admitted the root `/account` object. Any extra key under `account` could bypass the closed leaf allowlist before the narrower `/account/id`, `/account/display_name`, and `/account/type` policies ran. | Removed the root `/account` object policy and added `account_overview_does_not_admit_unvetted_account_object_fields`, which proves vetted leaves still project while extra account object fields are dropped. Producer display bindings now target the vetted account leaves so the producer still passes the projection contract. |
| P2 | `useProjectedComposition` could return previous account composition data for the first render after account navigation, before its effect cleared state. | The hook now gates returned data, loading, error, and rendered provenance by the account id that produced the loaded data. The navigation regression now asserts stale data and provenance are hidden immediately after rerender. |

## Remediation Verification

| Check | Result |
|---|---|
| `cargo test --manifest-path src-tauri/abilities-runtime/Cargo.toml --test dos570_fallback_projection -- --test-threads=1` | Pass: 19 fallback projection tests |
| `cargo test --manifest-path src-tauri/abilities-runtime/Cargo.toml account_overview -- --nocapture` | Pass: 10 account-overview tests, including producer projection compatibility |
| `pnpm exec vitest run src/components/composition/ReactBlockRenderer.test.tsx src/hooks/useProjectedComposition.test.tsx` | Pass: 2 files, 11 tests |
| `pnpm exec tsc --noEmit --incremental false` | Pass |
| `cargo clippy --manifest-path src-tauri/abilities-runtime/Cargo.toml --all-targets -- -D warnings` | Pass |
| `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` | Pass |
| `cargo test --manifest-path src-tauri/Cargo.toml --lib commands::abilities::tests::projected_composition_metrics_only_count_producer_runs -- --exact` | Pass |
| `git diff --check` | Pass |

## Cycle 4 Preconditions

Cycle 4 should verify the two cycle-3 findings as regression targets and should continue to treat the native screenshot proof as an open tooling blocker rather than a passed visual gate.
