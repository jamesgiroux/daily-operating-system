# v1.5.0 W1 L2 Review - Codex Cycle 5

**Date:** 2026-06-02
**Branch:** `codex/v1.5.0-w1`
**Base:** `public/dev` after W0 merge
**Command:** `codex exec review --uncommitted --ignore-user-config --ignore-rules --json -o /tmp/dailyos-w1-l2-cycle5-codex-review.txt`
**Verdict:** Request changes; blocking findings remediated in working tree, cycle 6 pending.

## Scope

Reviewed the normalized staged W1 diff after cycle 4 remediation.

The cycle explicitly checked:

- account-overview degraded and empty states
- hook loading/error behavior across account changes
- Tauri command and bridge invocation wiring
- provenance/trust-band rendering and sensitivity handling
- W1 proof bundle open gates

## Findings

| Priority | Finding | Resolution |
|---|---|---|
| P2 | The account-overview renderer ignored `snapshot_degraded`, so claim-backed mastheads could look normal when sourced account identity/vitals failed to load. | `AccountOverviewBlock` now renders a visible degraded state when `snapshot_degraded` is present, with user-facing copy that does not expose the internal degradation reason. `ReactBlockRenderer.test.tsx` asserts the degraded state appears and `account_snapshot_unavailable` does not render. |
| P2 | A stale error from the previous account could suppress the loading state for the next account because `visibleLoading` checked raw `error` while `visibleError` was hidden. | `useProjectedComposition` now computes `visibleError` first and gates `visibleLoading` on the visible current-account error only. The hook test now covers navigation away from a failed account while the next account is loading. |

## Remediation Verification

| Check | Result |
|---|---|
| `pnpm exec vitest run src/components/composition/ReactBlockRenderer.test.tsx src/hooks/useProjectedComposition.test.tsx` | Pass: 2 files, 14 tests |
| `pnpm exec tsc --noEmit --incremental false` | Pass |
| `git diff --check` | Pass |

## Reviewer Replay Notes

The reviewer replayed `pnpm exec tsc --noEmit` successfully. Its `cargo check --manifest-path src-tauri/Cargo.toml` attempt failed on the local `.cargo-lock` with `Operation not permitted`; the implementer L1 cycle already has passing Rust clippy/tests recorded in the proof bundle, and cycle 5 remediation did not touch Rust.

## Cycle 6 Preconditions

Cycle 6 should verify the two cycle-5 findings as regression targets, inspect the re-staged W1 diff, and continue to treat the native screenshot proof as an open tooling blocker rather than a passed visual gate.
