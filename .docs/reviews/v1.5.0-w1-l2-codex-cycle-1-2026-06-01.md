# v1.5.0 W1 L2 Review - Codex Cycle 1

**Date:** 2026-06-01
**Branch:** `codex/v1.5.0-w1`
**Base:** `public/dev` after W0 merge (`f4caeb05`)
**Verdict:** Request changes; blocking findings remediated in working tree, cycle 2 pending.

## Scope

Reviewed the W1 Account composition diff against the W1 L0 packet:

- shared projection service and first-party Tauri command
- `/v1/local/project-composition` parity with the command path
- projection cache and miss coalescing
- React block rendering and provenance presentation
- renderer exhaustiveness against Rust `BlockType`
- W1-F live routed proof readiness

## Blocking Findings

| Priority | Finding | Resolution |
|---|---|---|
| P1 | Cache miss coalescing still keyed the in-flight guard by `composition_version`, so first-render concurrent misses for the same account/current DB version could race rather than singleflight. | `MissKey` now excludes `composition_version` and keys by actor scope identity, composition id, surface kind, and fallback-policy version. Focused and full Rust tests pass. |
| P1 | The Tauri command, local loopback route, and signed route duplicated render/cache/project/store logic instead of sharing one service-owned path. | Added `project_composition_for_surface(...)` in `composition_render_orchestrator`; Tauri command and both surface-runtime routes now delegate to it after their own authorization/boundary checks. |
| P1 | Block-level provenance refs were preserved in data but not resolved into visible renderer affordances. | `ReactBlockRenderer` now passes rendered provenance to block components; block components resolve field attribution refs and render safe provenance status without raw field paths or diagnostics. |
| P1 | Renderer coverage was a local TypeScript list, not anchored to the canonical Rust block-type registry. | `ReactBlockRenderer.test.tsx` parses `src-tauri/abilities-runtime/src/abilities/composition.rs` and asserts the Rust known block ids exactly match `BLOCK_RENDERERS`. |
| P1 | W1-F live routed visual/L4 proof was not complete. | Still pending. An attempted Tauri proof run exposed a separate replica/mock DB isolation + Keychain blocker; another session is investigating that substrate issue. |

## Post-Remediation Verification

| Check | Result |
|---|---|
| `cargo test --manifest-path src-tauri/Cargo.toml composition_render_orchestrator -- --test-threads=1` | Pass |
| `cargo test --manifest-path src-tauri/Cargo.toml surface_runtime::tests::dos671_project_composition_cache_lookup_current_db_version_grep_gate -- --test-threads=1` | Pass |
| `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` | Pass |
| `cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1` | Pass |
| `pnpm tsc --noEmit` | Pass |
| `pnpm test ReactBlockRenderer useProjectedComposition` | Pass |
| `pnpm test` | Pass |
| `git diff --check` | Pass |

## Cycle 2 Preconditions

Before rerunning L2, complete W1-F with generic fixture data through the live routed Account page:

- real Tauri `invoke("get_projected_composition")`
- producer invocation
- section-driven folio navigation
- trust/provenance/fallback rendering
- desktop and mobile screenshots
- dense, sparse, stale/trust-caution, parent, and unknown-fallback states

L2 cycle 2 should treat the five cycle-1 findings as regression targets and verify that the live proof no longer relies on non-fixture or replica data.
