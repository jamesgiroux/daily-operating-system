# L2 Codex Cycle-2 Review — DOS-546 W1-E.1

- **Commit under review:** `9d09c1c3` ("DOS-546 W1-E.1: widen Composition types per ADR-0130 §2 + substrate-owned authorship gate")
- **Branch:** `dos-546-wp-studio-spike`
- **Cycle-1 review:** `.docs/reviews/dos-546-w1-e-l2-codex-2026-05-11.md` (verdict REVISE, P1-1 + P1-2)
- **Date:** 2026-05-11
- **Reviewer:** codex (gpt-5.5, xhigh reasoning)

## Verdict: REVISE

## Rationale

Codex verified each P1 fix against the commit contents directly via `git show 9d09c1c3` and confirmed:

- **P1-2 widening — RESOLVED.** `Composition::new` is correctly demoted to `pub(crate) fn` at line 570 of `src-tauri/abilities-runtime/src/abilities/composition.rs`. The Composition struct (line 541+) carries `kind: CompositionKind`, `subject: Option<EntityRef>`, `salience: Salience`, `generated_at: DateTime<Utc>`, `generated_by: AbilityRef`. Section carries `label: Option<String>` and `salience: Salience`. Block carries `render_hints: RenderHints`. Fields match ADR-0130 §2 lines 50–75 verbatim.
- **P2 envelope-validation fix — RESOLVED.** `block_construction_validates_field_path_when_envelope_provided` constructs a real `Provenance` envelope with `field_attributions` populated and pins `invocation_id` to match `sample_provenance_ref`. Negative companion test `block_construction_rejects_field_path_outside_envelope` asserts `UnresolvedFieldPath` on a bad pointer. Both pass.
- **`scripts/check_composition_authorship.sh` — PRESENT IN COMMIT.** The script exists, scans for both `Composition::new(` and `Composition {` literal constructors with `--glob '!src-tauri/abilities-runtime/**'` exclusion, and exits 0 against the current tree.

**P1-1 is NOT fully closed by `9d09c1c3`.** `git show --name-status 9d09c1c3` lists exactly two paths:

1. `scripts/check_composition_authorship.sh` (new)
2. `src-tauri/abilities-runtime/src/abilities/composition.rs` (modified)

`git show 9d09c1c3:.github/workflows/test.yml` contains no ADR-0130 authorship-gate step. The workflow wiring exists only as an **uncommitted** worktree edit (`git status` shows `M .github/workflows/test.yml`, current lines 93–94 add the step). AC line 666 mandates a CI lint — a script that exists but isn't invoked by CI does not satisfy the acceptance criterion. The defense-in-depth layer is half-wired.

## Required revision

Single follow-up commit on this branch wiring `.github/workflows/test.yml` to invoke `./scripts/check_composition_authorship.sh` as a required CI step. Squash or stack on `9d09c1c3` — either is fine; the audit trail just needs the workflow change inside the W1-E.1 commit boundary before merge.

Note: BlockPayload-vs-attributes is explicitly documented as a typed-refinement deferral filed to the maintenance project (`b8e6aea4-d47e-4f3a-b03d-a05bec914aeb`), and `serde_json::Value` is a strict superset of AC line 660's `BTreeMap<String, Value>`. That deferral is acceptable per project policy on path-α findings and is **not** the blocker — the missing workflow commit is.

## Status

Once the workflow step is committed to this branch, re-run cycle-3 verification (single-pass — only need to confirm the `.github/workflows/test.yml` step lands in-tree).
