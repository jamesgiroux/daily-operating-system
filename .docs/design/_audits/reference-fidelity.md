# Reference fidelity audit
Generated by `.docs/design/_audits/audit-reference.py`. Each reference HTML is compared against its canonical TSX. See the script docstring for what each finding means.

## Summary
- **Critical**: 14 surfaces (missing imports or >5 inline-style violations)
- **Major**: 4 surfaces (invented/missing classes, or some inline styles)
- **Minor**: 1 surfaces (text deltas only)
- **Clean**: 9 surfaces

## Critical (14)

### `.docs/design/reference/surfaces/briefing.html`
vs `src/components/dashboard/DailyBriefing.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `HealthBadge` — expected `HealthBadge_*` classes

**Suspicious inline styles** (15, TSX uses none):
- `background:var(--color-garden-sage)`
- `max-height:2000px`
- `background:var(--color-garden-sage)`
- `max-height:0`
- `max-height:0`
- … +10 more

**Text deltas** (2 strings in TSX absent from HTML):
- “Fix lifecycle change”
- “No stage”

---

### `.docs/design/reference/surfaces/account.html`
vs `src/pages/AccountDetailEditorial.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `FolioReportsDropdown` — expected `FolioReportsDropdown_*` classes
- `FolioToolsDropdown` — expected `FolioToolsDropdown_*` classes
- `EditableVitalsStrip` — expected `EditableVitalsStrip_*` classes
- `StateOfPlay` — expected `StateOfPlay_*` classes
- `StakeholderGallery` — expected `StakeholderGallery_*` classes
- `WatchList` — expected `WatchList_*` classes
- `UnifiedTimeline` — expected `UnifiedTimeline_*` classes
- `TheWork` — expected `TheWork_*` classes
- `AccountOutlook` — expected `AccountOutlook_*` classes
- `FileListSection` — expected `FileListSection_*` classes
- `WatchListPrograms` — expected `WatchListPrograms_*` classes
- `RecommendedActions` — expected `RecommendedActions_*` classes

**Suspicious inline styles** (101, TSX uses none):
- `background:var(--color-garden-sage)`
- `color:var(--color-spice-turmeric)`
- `color:var(--color-spice-saffron)`
- `color:var(--color-text-secondary)`
- `color:var(--color-garden-olive)`
- … +96 more

**Text deltas** (3 strings in TSX absent from HTML):
- “Competitive &amp;”
- “State of”
- “Value &amp;”

---

### `.docs/design/reference/surfaces/accounts.html`
vs `src/pages/AccountsPage.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `HealthBadge` — expected `HealthBadge_*` classes

**Missing classes** (54): TSX `styles.X` not in HTML:
- `archivedList` → expected `AccountsPage_archivedList`
- `bulkButton` → expected `AccountsPage_bulkButton`
- `createForm` → expected `AccountsPage_createForm`
- `createFormInlineRow` → expected `AccountsPage_createFormInlineRow`
- `createFormRow` → expected `AccountsPage_createFormRow`
- `discoveryAccountName` → expected `AccountsPage_discoveryAccountName`
- `discoveryAddButton` → expected `AccountsPage_discoveryAddButton`
- `discoveryAddedLabel` → expected `AccountsPage_discoveryAddedLabel`
- `discoveryCloseButton` → expected `AccountsPage_discoveryCloseButton`
- `discoveryContextPreview` → expected `AccountsPage_discoveryContextPreview`
- … +44 more

---

### `.docs/design/reference/surfaces/actions.html`
vs `src/pages/ActionsPage.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `ChapterHeading` — expected `ChapterHeading_*` classes

**Suspicious inline styles** (35, TSX uses none):
- `display:flex; align-items:flex-start; gap:12px; padding:14px 0; border-bottom:1px solid var(--color-rule-light); border-…`
- `flex:1; min-width:0;`
- `display:flex; align-items:center; gap:8px; margin-bottom:2px;`
- `font-family:var(--font-mono); font-size:10px; font-weight:600; letter-spacing:0.06em; text-transform:uppercase; color:va…`
- `font-family:var(--font-mono); font-size:11px; font-weight:600; letter-spacing:0.04em; color:var(--color-spice-turmeric);`
- … +30 more

---

### `.docs/design/reference/surfaces/meeting.html`
vs `src/pages/MeetingDetailPage.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `IntelligenceQualityBadge` — expected `IntelligenceQualityBadge_*` classes
- `HealthBadge` — expected `HealthBadge_*` classes
- `PostMeetingIntelligence` — expected `PostMeetingIntelligence_*` classes
- `EditableText` — expected `EditableText_*` classes

**Suspicious inline styles** (9, TSX uses none):
- `display:flex; flex-direction:column; gap:8px;`
- `display:flex; flex-wrap:wrap; align-items:center; gap:8px;`
- `display:inline-flex; align-items:center; gap:4px; font-family:var(--font-sans); font-size:12px; font-weight:400; color:v…`
- `width:12px; height:12px; opacity:0.7; flex-shrink:0;`
- `color:inherit; text-decoration:none;`
- … +4 more

**Text deltas** (15 strings in TSX absent from HTML):
- “Building context”
- “Format”
- “Markdown”
- “Meeting Outcomes”
- “Meeting Record”
- … +10 more

---

### `.docs/design/reference/surfaces/person.html`
vs `src/pages/PersonDetailEditorial.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `EditableVitalsStrip` — expected `EditableVitalsStrip_*` classes
- `UnifiedTimeline` — expected `UnifiedTimeline_*` classes

**Suspicious inline styles** (68, TSX uses none):
- `background:var(--color-garden-sage)`
- `width:48px; height:48px; border-radius:50%; background:var(--color-paper-linen); display:flex; align-items:center; justi…`
- `font-family:var(--font-mono); font-size:11px; font-weight:600; letter-spacing:0.06em; text-transform:uppercase; color:va…`
- `color:var(--color-garden-larkspur)`
- `color:var(--color-text-secondary)`
- … +63 more

**Text deltas** (1 strings in TSX absent from HTML):
- “Cancel”

---

### `.docs/design/reference/surfaces/project.html`
vs `src/pages/ProjectDetailEditorial.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `EditableVitalsStrip` — expected `EditableVitalsStrip_*` classes
- `UnifiedTimeline` — expected `UnifiedTimeline_*` classes

**Missing classes** (8): TSX `styles.X` not in HTML:
- `addChildButton` → expected `ProjectDetailEditorial_addChildButton`
- `hotspotLinkOlive` → expected `ProjectDetailEditorial_hotspotLinkOlive`
- `statusActive` → expected `ProjectDetailEditorial_statusActive`
- `statusCompleted` → expected `ProjectDetailEditorial_statusCompleted`
- `statusDotActive` → expected `ProjectDetailEditorial_statusDotActive`
- `statusDotCompleted` → expected `ProjectDetailEditorial_statusDotCompleted`
- `statusDotOnHold` → expected `ProjectDetailEditorial_statusDotOnHold`
- `statusOnHold` → expected `ProjectDetailEditorial_statusOnHold`

**Suspicious inline styles** (101, TSX uses none):
- `background:var(--color-garden-sage)`
- `font-family:var(--font-mono); font-size:11px; font-weight:600; letter-spacing:0.06em; text-transform:uppercase; color:va…`
- `color:var(--color-garden-olive)`
- `color:var(--color-text-secondary)`
- `color:var(--color-text-secondary)`
- … +96 more

**Text deltas** (3 strings in TSX absent from HTML):
- “Archive Project”
- “Cancel”
- “Create Sub-Project”

---

### `.docs/design/reference/surfaces/settings.html`
vs `src/pages/SettingsPage.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `SurfaceMasthead` — expected `SurfaceMasthead_*` classes

**Suspicious inline styles** (248, TSX uses none):
- `margin-bottom:32px;`
- `font-family:var(--font-mono);font-size:11px;font-weight:600;letter-spacing:0.06em;text-transform:uppercase;color:var(--c…`
- `font-family:var(--font-sans);font-size:13px;color:var(--color-text-tertiary);line-height:1.5;margin:0;margin-bottom:16px…`
- `display:flex;gap:12px;margin-bottom:20px;`
- `font-family:var(--font-mono);font-size:11px;font-weight:600;letter-spacing:0.06em;text-transform:uppercase;background:no…`
- … +243 more

**Text deltas** (1 strings in TSX absent from HTML):
- “Checking...”

---

### `.docs/design/reference/surfaces/week.html`
vs `src/pages/WeekPage.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `HealthBadge` — expected `HealthBadge_*` classes

---

### `.docs/design/reference/surfaces/onboarding/glean.html`
vs `src/components/onboarding/chapters/GleanConnect.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `ChapterHeading` — expected `ChapterHeading_*` classes

**Missing classes** (6): TSX `styles.X` not in HTML:
- `connectedEmail` → expected `onboarding_connectedEmail`
- `connectedLabel` → expected `onboarding_connectedLabel`
- `dangerColor` → expected `onboarding_dangerColor`
- `flexRowMd` → expected `onboarding_flexRowMd`
- `pt8` → expected `onboarding_pt8`
- `statusDot` → expected `onboarding_statusDot`

**Suspicious inline styles** (7, TSX uses none):
- `margin-bottom: var(--space-xl);`
- `border: none; border-top: 1px solid var(--color-rule-heavy); margin: 0 0 var(--space-md);`
- `display: flex; align-items: center; gap: var(--space-sm);`
- `margin: 0; font-family: var(--font-serif); font-size: 28px; font-weight: 400; line-height: 1.2; letter-spacing: -0.01em;…`
- `width: 100%; min-width: 0; height: 36px; padding: 4px 12px; font-family: var(--font-sans); font-size: 14px; color: var(-…`
- … +2 more

**Text deltas** (1 strings in TSX absent from HTML):
- “Connected”

---

### `.docs/design/reference/surfaces/onboarding/youcard.html`
vs `src/components/onboarding/chapters/YouCardStep.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `ChapterHeading` — expected `ChapterHeading_*` classes

**Suspicious inline styles** (11, TSX uses none):
- `margin-bottom: var(--space-xl);`
- `border: none; border-top: 1px solid var(--color-rule-heavy); margin: 0 0 var(--space-md);`
- `display: flex; align-items: center; gap: var(--space-sm);`
- `margin: 0; font-family: var(--font-serif); font-size: 28px; font-weight: 400; line-height: 1.2; letter-spacing: -0.01em;…`
- `max-width: 540px; margin: var(--space-md) 0 0; font-family: var(--font-serif); font-size: 17px; font-style: italic; font…`
- … +6 more

---

### `.docs/design/reference/surfaces/onboarding/first-account.html`
vs `src/components/onboarding/chapters/FirstAccountStep.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `ChapterHeading` — expected `ChapterHeading_*` classes

**Missing classes** (7): TSX `styles.X` not in HTML:
- `accountChipSimple` → expected `onboarding_accountChipSimple`
- `chipRemoveButton` → expected `onboarding_chipRemoveButton`
- `flexRowMd` → expected `onboarding_flexRowMd`
- `flexWrap` → expected `onboarding_flexWrap`
- `mb16` → expected `onboarding_mb16`
- `pt8` → expected `onboarding_pt8`
- `sageColor` → expected `onboarding_sageColor`

**Suspicious inline styles** (11, TSX uses none):
- `margin-bottom: var(--space-xl);`
- `border: none; border-top: 1px solid var(--color-rule-heavy); margin: 0 0 var(--space-md);`
- `display: flex; align-items: center; gap: var(--space-sm);`
- `margin: 0; font-family: var(--font-serif); font-size: 28px; font-weight: 400; line-height: 1.2; letter-spacing: -0.01em;…`
- `max-width: 540px; margin: var(--space-md) 0 0; font-family: var(--font-serif); font-size: 17px; font-style: italic; font…`
- … +6 more

---

### `.docs/design/reference/surfaces/onboarding/prime.html`
vs `src/components/onboarding/chapters/PrimeBriefing.tsx`

**Missing imports** (TSX imports a component whose CSS prefix isn't in HTML):
- `ChapterHeading` — expected `ChapterHeading_*` classes

**Missing classes** (3): TSX `styles.X` not in HTML:
- `dropZoneActive` → expected `onboarding_dropZoneActive`
- `flexCenterGap8` → expected `onboarding_flexCenterGap8`
- `processingIndicator` → expected `onboarding_processingIndicator`

**Suspicious inline styles** (8, TSX uses none):
- `margin-bottom: var(--space-xl);`
- `border: none; border-top: 1px solid var(--color-rule-heavy); margin: 0 0 var(--space-md);`
- `display: flex; align-items: center; gap: var(--space-sm);`
- `margin: 0; font-family: var(--font-serif); font-size: 28px; font-weight: 400; line-height: 1.2; letter-spacing: -0.01em;…`
- `max-width: 540px; margin: var(--space-md) 0 0; font-family: var(--font-serif); font-size: 17px; font-style: italic; font…`
- … +3 more

---

### `.docs/design/reference/surfaces/reports/monthly-wrapped.html`
vs `src/pages/monthly-wrapped/MonthlyWrappedPage.tsx`

**Missing classes** (10): TSX `styles.X` not in HTML:
- `emptyDescription` → expected `monthly-wrapped_emptyDescription`
- `emptyError` → expected `monthly-wrapped_emptyError`
- `emptyOverline` → expected `monthly-wrapped_emptyOverline`
- `emptyState` → expected `monthly-wrapped_emptyState`
- `emptyTitle` → expected `monthly-wrapped_emptyTitle`
- `folioAction` → expected `monthly-wrapped_folioAction`
- `folioActionActive` → expected `monthly-wrapped_folioActionActive`
- `folioActionDisabled` → expected `monthly-wrapped_folioActionDisabled`
- `loadingSkeleton` → expected `monthly-wrapped_loadingSkeleton`
- `skeletonBar` → expected `monthly-wrapped_skeletonBar`

**Suspicious inline styles** (6, TSX uses none):
- `background:radial-gradient(ellipse at 20% 80%, color-mix(in srgb, var(--color-garden-eucalyptus) 8%, transparent) 0%, tr…`
- `top:40%; left:50%; transform:translate(-50%, -50%); width:600px; height:400px; inset:auto; background:radial-gradient(el…`
- `--anim-delay:400ms; color:var(--color-text-primary); border:1px solid var(--color-spice-turmeric);`
- `--anim-delay:520ms; color:var(--color-text-primary); border:1px solid var(--color-garden-larkspur);`
- `--anim-delay:640ms; color:var(--color-text-primary); border:1px solid var(--color-garden-eucalyptus);`
- … +1 more

---

## Major (4)

### `.docs/design/reference/surfaces/inbox.html`
vs `src/pages/InboxPage.tsx`

**Missing classes** (46): TSX `styles.X` not in HTML:
- `batchBanner` → expected `InboxPage_batchBanner`
- `batchProgressBar` → expected `InboxPage_batchProgressBar`
- `batchProgressTrack` → expected `InboxPage_batchProgressTrack`
- `batchQuote` → expected `InboxPage_batchQuote`
- `driveImportButton` → expected `InboxPage_driveImportButton`
- `dropResultToast` → expected `InboxPage_dropResultToast`
- `dropZoneCompactDragging` → expected `InboxPage_dropZoneCompactDragging`
- `dropZoneHint` → expected `InboxPage_dropZoneHint`
- `dropZoneLarge` → expected `InboxPage_dropZoneLarge`
- `dropZoneLargeDragging` → expected `InboxPage_dropZoneLargeDragging`
- … +36 more

**Text deltas** (7 strings in TSX absent from HTML):
- “AI identifies which account or project the file relates to.”
- “Classify”
- “Content is routed to the right place automatically.”
- “DailyOS detects the file type and content.”
- “Drag meeting notes, transcripts, or documents into the inbox.”
- … +2 more

---

### `.docs/design/reference/surfaces/onboarding/google.html`
vs `src/components/onboarding/chapters/GoogleConnect.tsx`

**Missing classes** (5): TSX `styles.X` not in HTML:
- `connectedEmail` → expected `onboarding_connectedEmail`
- `connectedLabel` → expected `onboarding_connectedLabel`
- `flexRowMd` → expected `onboarding_flexRowMd`
- `pt8` → expected `onboarding_pt8`
- `statusDot` → expected `onboarding_statusDot`

---

### `.docs/design/reference/surfaces/onboarding/claude-code.html`
vs `src/components/onboarding/chapters/ClaudeCode.tsx`

**Missing classes** (6): TSX `styles.X` not in HTML:
- `flexRowMd` → expected `onboarding_flexRowMd`
- `noMargin` → expected `onboarding_noMargin`
- `primaryText` → expected `onboarding_primaryText`
- `pt8` → expected `onboarding_pt8`
- `statusDot` → expected `onboarding_statusDot`
- `tertiaryText` → expected `onboarding_tertiaryText`

---

### `.docs/design/reference/surfaces/onboarding/role.html`
vs `src/components/onboarding/chapters/EntityMode.tsx`

**Missing classes** (5): TSX `styles.X` not in HTML:
- `accentColor` → expected `onboarding_accentColor`
- `flexShrink0` → expected `onboarding_flexShrink0`
- `loadingText` → expected `onboarding_loadingText`
- `presetCardDisabled` → expected `onboarding_presetCardDisabled`
- `presetCardSelected` → expected `onboarding_presetCardSelected`

**Suspicious inline styles** (5, TSX uses none):
- `margin-bottom: var(--space-xl);`
- `border: none; border-top: 1px solid var(--color-rule-heavy); margin: 0 0 var(--space-md);`
- `display: flex; align-items: center; gap: var(--space-sm);`
- `margin: 0; font-family: var(--font-serif); font-size: 28px; font-weight: 400; line-height: 1.2; letter-spacing: -0.01em;…`
- `max-width: 540px; margin: var(--space-md) 0 0; font-family: var(--font-serif); font-size: 17px; font-style: italic; font…`

---

## Minor (1)

### `.docs/design/reference/surfaces/people.html`
vs `src/pages/PeoplePage.tsx`

**Text deltas** (2 strings in TSX absent from HTML):
- “Cancel”
- “Merge people”

---

## Clean (9)

### `.docs/design/reference/surfaces/me.html`
vs `src/pages/MePage.tsx`

---

### `.docs/design/reference/surfaces/projects.html`
vs `src/pages/ProjectsPage.tsx`

---

### `.docs/design/reference/surfaces/onboarding/welcome.html`
vs `src/components/onboarding/chapters/Welcome.tsx`

---

### `.docs/design/reference/surfaces/reports/risk-briefing.html`
vs `src/pages/RiskBriefingPage.tsx`

---

### `.docs/design/reference/surfaces/reports/swot.html`
vs `src/pages/SwotPage.tsx`

---

### `.docs/design/reference/surfaces/reports/account-health.html`
vs `src/pages/AccountHealthPage.tsx`

---

### `.docs/design/reference/surfaces/reports/ebr-qbr.html`
vs `src/pages/EbrQbrPage.tsx`

---

### `.docs/design/reference/surfaces/reports/weekly-impact.html`
vs `src/pages/WeeklyImpactPage.tsx`

---

### `.docs/design/reference/surfaces/reports/book-of-business.html`
vs `src/pages/BookOfBusinessPage.tsx`

---

