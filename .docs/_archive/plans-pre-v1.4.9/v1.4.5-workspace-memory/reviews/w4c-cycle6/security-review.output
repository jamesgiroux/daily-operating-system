[BLOCKER] `.docs/plans/v1.4.5-waves.md` §Agent W4-C lines 966-1021 conflicts with the L0 packet §0/§4. The supersession note at line 962 is not enough while the next heading still says “Frozen ability contract” and advertises optional category, `client_source_asof`, raw `ingestion_run_id`/`file_id`, old idempotency, and broad transactional write wording. Action: replace the stale W4-C contract/tests/done-when with a pointer to the L0 packet, or mark the whole block `SUPERSEDED - DO NOT IMPLEMENT`; add `.docs/plans/v1.4.5-waves.md` to the packet’s L1 reconciliation blocker until no active old surface remains.

[BLOCKER] L0 packet §4 Request, §6 Service Flow, §7 Fence algorithm, §10 Test Plan: `content_b64` has only a decoded-size cap, while schema failures write no service/rate rows and the service ledger starts after closed-schema parse. A large malformed or missing-category payload can consume parser/allocation/base64-validation work outside the rate boundary. Action: add a pre-decode max encoded `content_b64` length and max MCP argument/body size, enforced by bounded/streaming parsing before expensive work; mirror the cap in DOS-480; add tests for oversized malformed and missing-category payloads producing no rows and bounded memory.

[BLOCKER] L0 packet §4 allows `application/json`, while §9 only says claims inherit source attribution through the pipeline. It does not explicitly forbid client-supplied JSON provenance/trust/claim metadata from being interpreted as trusted input. Action: add a rule that MCP placement content is untrusted document content only; all `source_asof`, provenance, trust band, actor, lifecycle, link attribution, and claim identity fields are server/pipeline-derived. Add fixtures where JSON attempts to forge those fields and prove committed output still uses `WorkspaceFileKind::McpPlacement`, `LinkAttributionSource::McpPlacement`, server-derived `source_asof`, and default/allowed trust behavior.

Final verdict: HOLD