**START** by reading `.docs/plans/wave-W6/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W6-D — DOS-320

- **Title:** Render surfaces filter by trust band
- **Linear:** https://linear.app/a8c/issue/DOS-320 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W6/DOS-320-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W6-D — DOS-320 render surfaces filter by trust band (deferred-but-eligible)"
- **Reviewers (3 stacked):** accessibility-tester + codex-challenge + codex-consult

## Things to be aware of

- DOS-320 is **not in the original 22-issue spine** — it's "deferred-but-eligible": land in W6 if time permits, otherwise defer to v1.4.1. This means the L0 plan must be scoped conservatively. Don't over-design.
- `TrustBand` is a W4-A (DOS-5) deliverable: `LikelyCurrent`, `UseWithCaution`, `NeedsVerification`, `Unscored`. The render layer reads this from `AbilityOutput<T>` provenance. `TrustBand` does **not exist yet** in the codebase — plan against the W4-A frozen contract at `.docs/plans/wave-W4/DOS-5-plan.md:63-75`.
- This is a **UI/render layer** change. W6-D owns the surface render for `get_entity_context` and `prepare_meeting` outputs that bias by trust band. It is NOT a backend filtering gate — the Trust Compiler already assigned bands; W6-D is about how the frontend renders them.
- Accessibility is the lead concern: trust band UI must not rely on color alone, must have ARIA labels, must be screen-reader friendly. The accessibility-tester reviewer fires here.
- "Filter" may be a misnomer — the spec says "bias by trust band". Likely this means: `LikelyCurrent` renders normally; `UseWithCaution` renders with a soft indicator; `NeedsVerification` renders with a stronger warning or is collapsed by default; `Unscored` renders with a neutral indicator.
- Frontend surfaces: look at where `get_entity_context` and `prepare_meeting` results are currently rendered in the React frontend. These are the surfaces W6-D modifies.

## Files owned

- React render layer for `get_entity_context` output (surface that shows entity context entries / intelligence)
- React render layer for `prepare_meeting` output (meeting brief display)
- Trust band indicator component (accessible, design-token-based)
- ARIA labels and screen reader text for trust band states

## Key code surfaces to grep

- `src/` (React frontend) — find where entity context and meeting brief are rendered:
  - `src/hooks/useEntityContextEntries.ts` (existing hook)
  - `src/components/` — entity context display, meeting prep display components
  - Any existing meeting brief / intelligence surface components
- `src/CLAUDE.md` — product vocabulary / ADR-0083 for user-facing strings
- `.docs/plans/wave-W4/DOS-5-plan.md:63-75` — `TrustBand` enum names and stable serde strings
- `.docs/plans/wave-W5/DOS-218-plan.md` — W5-A output shape `AbilityOutput<Vec<EntityContextEntry>>`
- `.docs/plans/wave-W5/DOS-219-plan.md` — W5-B output shape `AbilityOutput<MeetingBrief>` + field attribution
- `.docs/decisions/0108-provenance-rendering-and-privacy.md` — how provenance/trust is rendered at surfaces

## Specific design decisions to record in §3

- "Filter" vs "indicator" vs "collapse": for each trust band, what is the default render behavior? Document the UX policy.
- Accessibility: how does the trust band indicator work for screen readers? (ARIA role, live region, visible label vs icon-only)
- Design tokens: what tokens from the existing design system apply to trust indicators? Do not introduce new color values — use existing semantic tokens.
- TypeScript: does `AbilityOutput<T>` arrive at the frontend with `trust_band` as a typed field, or does the frontend parse it from provenance JSON? This depends on W4-C's bridge return type.
- Scope: does W6-D touch the raw `get_entity_context_entries` hook/command path (legacy) or only the new W5 ability output path? If legacy is still in use during W6, W6-D may need to handle both.

## Coordination notes for §7

- W5-A (DOS-218): W6-D renders W5-A's `AbilityOutput<Vec<EntityContextEntry>>` with trust bands. Must be compatible with W5-A's output schema.
- W5-B (DOS-219): W6-D renders W5-B's `AbilityOutput<MeetingBrief>` with field-level trust attribution ("About this" affordance).
- W4-A (DOS-5): `TrustBand` enum must be stable before W6-D can code. Coordinate on serde string names.
- W4-C (DOS-217): `AbilityResponseJson` is the bridge return type. Confirm `trust_band` is surfaced at the top level or parseable from `rendered_provenance`.
- Accessibility-tester: this reviewer fires at L0, L2, and L4 QA for W6-D. The plan must include specific ARIA attribute names and screen reader test scenarios.

Write the plan now.
