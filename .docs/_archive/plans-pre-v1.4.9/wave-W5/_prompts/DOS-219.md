**START** by reading `.docs/plans/wave-W5/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W5-B — DOS-219

- **Title:** Substrate: migrate `prepare_meeting` as first Transform-category ability pilot
- **Linear:** https://linear.app/a8c/issue/DOS-219 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W5/DOS-219-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W5-B — DOS-219 migrate `prepare_meeting` (Transform pilot)"
- **Reviewers (3 stacked):** accessibility-tester + architect-reviewer + codex-consult

## Things to be aware of

- This is the **Transform pilot** — the first LLM-calling ability on the new substrate. The split is load-bearing: pure `build_meeting_brief` ability (the Transform) vs publish/cache/maintenance behavior (side effects that stay outside the pure ability).
- The legacy surface is `refresh_meeting_briefing` at `src-tauri/src/commands/core.rs:199` and `refresh_meeting_briefing_full` at `src-tauri/src/services/meetings.rs:2974`. These currently bundle synthesis + publish + caching in one call. The W5-B split separates them.
- `gather_account_context` at `src-tauri/src/prepare/meeting_context.rs:277` is the context-building function feeding the current meeting brief prompt — understand its shape before designing the ability input.
- **Transform category** per ADR-0102: calls `IntelligenceProvider::complete()`, produces a transformed output, declares `source_asof` for each item. The pure ability returns `AbilityOutput<MeetingBrief>`; it does NOT write to the DB or emit signals itself.
- "About this" affordance: meeting brief outputs must expose field-level attribution — which source produced each bullet/section. This is the W3-B Provenance envelope in practice. Read `.docs/plans/wave-W3/DOS-211-plan.md` for how field attribution works in `AbilityOutput<T>`.
- W5-B does NOT depend on W5-A. Coordinate only on shared utility extraction: helpers shared between the two abilities (e.g. `SubjectRef` resolution, entity context loading) should live in `abilities/common/`, not be duplicated. If W5-A already defined them, import; if not, define here and W5-A adopts.
- Parity test on bundle-5 (correction resurrection): the brief output after a `WrongSubject` correction tombstone should not include the corrected-away claim. This is the DOS-7 + DOS-301 projection at work.

## Files owned

- `src-tauri/src/abilities/prepare_meeting/` (new directory — split into `build_brief.rs` + `publish.rs` + `maintenance.rs`)
- `src-tauri/src/abilities/common/` helpers if not already created by W5-A
- Parity integration test against legacy `refresh_meeting_briefing` command
- Bundle-5 fixture seed data (coordinate with W6-A; stub if W6-A hasn't landed)

## Key code surfaces to grep

- `src-tauri/src/commands/core.rs:199` — `refresh_meeting_briefing` (the command being replaced)
- `src-tauri/src/services/meetings.rs:2974` — `refresh_meeting_briefing_full` (the service implementation)
- `src-tauri/src/prepare/meeting_context.rs:277` — `gather_account_context` (context builder)
- `src-tauri/src/intelligence/lifecycle.rs:49` — `refresh_meeting_briefing_from_state` (existing lifecycle hook)
- `src-tauri/src/intelligence/dimension_prompts.rs` — meeting brief prompt shape (sourcedAt, items per dimension)
- `src-tauri/src/intelligence/io.rs:29-41` — `ItemSource` (sourced_at on each item — feeds source_asof)
- `.docs/plans/wave-W3/DOS-210-plan.md` — W3-A: Transform category contract, `AbilityContext`, `IntelligenceProvider` access
- `.docs/plans/wave-W3/DOS-211-plan.md` — W3-B: `AbilityOutput<T>`, field-level `SourceAttribution`, "About this" affordance
- `.docs/plans/wave-W3/DOS-7-plan.md` — W3-C: how `commit_claim` and tombstones affect meeting brief content
- `.docs/plans/wave-W3/DOS-301-plan.md` — W3-D: derived-state projection (legacy `success_plans`/`accounts_columns` updated from claims)
- `.docs/plans/wave-W3/DOS-299-plan.md` — W3-G: `source_asof` per item, freshness fallback chain
- `.docs/plans/wave-W3/DOS-300-plan.md` — W3-H: claim_type registry must include meeting brief claim types
- `.docs/plans/wave-W4/DOS-5-plan.md` — W4-A: Trust band on brief items
- `.docs/plans/wave-W4/DOS-216-plan.md` — W4-B: fixture format (Transform uses continuous scoring + replayed judge)
- `.docs/plans/wave-W4/DOS-217-plan.md` — W4-C: bridge layer, `WorkerAbilityBridge` for scheduled maintenance
- `.docs/decisions/0102-abilities-as-runtime-contract.md` — Transform category definition
- `.docs/decisions/0110-evaluation-harness-for-abilities.md` — Transform fixture scoring with judge rubric

## Specific design decisions to record in §3

- The publish/cache/maintenance split: what exactly stays in `build_meeting_brief.rs` (pure Transform) vs `publish.rs` (writes brief to DB/file) vs `maintenance.rs` (scheduled re-generation, cache invalidation)? Confirm the boundary — the pure ability must not write state.
- "About this" affordance implementation: does each section/bullet in `MeetingBrief` carry an inline `SourceAttribution`, or does the ability return a top-level provenance map keyed by field path?
- Context input shape: does `build_meeting_brief` take a `MeetingId` + derive context inside, or does the caller pass a pre-assembled `MeetingContext` struct? Understand tradeoffs for testability.
- Snapshot/cache strategy for scheduled maintenance: when the worker re-runs the brief, does it publish to `intelligence.json` (legacy) via DOS-301 derived-state, or to the new claims substrate, or both during the transition period?
- `source_asof` on LLM-synthesized items: each item in the meeting brief comes from a source document. The `ItemSource.sourced_at` from `io.rs:29-37` feeds `source_asof` on the claim. Confirm this chain works end-to-end for the Transform category.

## Coordination notes for §7

- W5-A (DOS-218) coordinates on `abilities/common/` — share entity resolution, context loading, SubjectRef helpers. Do not build duplicate utility code in both pilots.
- W3-H (DOS-300): `CLAIM_TYPE_REGISTRY` must include all claim types produced by `build_meeting_brief`. Meeting brief items are among the 10-15 initial registry entries per the DOS-300 ticket — confirm they're covered.
- W3-D (DOS-301): the `publish.rs` side of W5-B writes to the derived-state projection, not direct legacy columns. Coordinate with DOS-301 author on which projection target covers meeting brief output.
- W4-B (DOS-216): Transform fixtures use replayed judge completions. The fixture for `prepare_meeting` must include both `provider_replay.json` (LLM reply) and `expected_provenance.json` (field attribution). Stub if needed.
- W4-C (DOS-217): `WorkerAbilityBridge` invokes the maintenance category; `TauriAbilityBridge` invokes the Transform. Confirm actor policy: does `build_meeting_brief` allow `Actor::System` for worker-scheduled re-generation, or is it `Actor::User`-only and maintenance is a separate ability?

Write the plan now.
