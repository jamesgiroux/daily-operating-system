# v1.5.0 WR — Chapter ⇄ Production Parity Map (DOS-852)

**Source of truth:** `public/main:src/pages/AccountDetailPage.tsx` (3-view page).
The deprecated `AccountDetailEditorial` components (StateOfPlay, WatchList,
AccountOutlook, StakeholderGallery, AccountHealthSection) are NOT production —
do not mirror them. Hero is APPROVED — do not touch.

Per chapter: the production rendering, its data needs, what the producer
ships today, and the gap. Producer subsets live in
`account_overview.rs::section_intelligence_subset`; snapshot reader at
`src/services/context.rs` (`get_entity_intelligence` already wired).

| VARIANT_D chapter | Production source (main) | Components | Data | Status |
|---|---|---|---|---|
| headline | — | composition hero | snapshot | ✅ APPROVED |
| outlook | `#outlook` "Outlook" | `ChapterHeading` title=\`The Call: ${renewalCallVerdict(agreementOutlook)}\` + `OutlookPanel` + `IntelligenceCorrection` (field agreementOutlook.renewalNarrative) | intelligence.agreementOutlook + contractContext | 🔶 OutlookPanel swapped in; needs The Call heading + correction prompt |
| state-of-play | `#on-track` "On Track" + `#needs-attention` "Needs attention" | `OnTrackChapter` (+IntelligenceCorrection on executiveAssessment) then `TriageSection` + `DivergenceSection` | intelligence (executiveAssessment, risks, currentState…) + **glean signals** + sentiment + consistency findings | ❌ renders deprecated StateOfPlay; OnTrackChapter is intelligence-fed (props: intelligence, accountSizeLabel); Triage/Divergence need glean+findings — check `hasTriageContent`/`hasDivergenceContent` inputs |
| the-room | `#the-room` "The Room" | `StakeholderGrid` (+`PendingStakeholderQueue`) | stakeholdersFull/linkedPeople records — NOT in intelligence subset | ❌ renders avatar rows; needs producer to ship stakeholder records (snapshot or claims) |
| whats-next | Work view: commitments cards + `#suggestions` | `CommitmentCard`, `SuggestionCard` (WorkSurface) | work data (actions/commitments/suggestions) — not intelligence | ❌ |
| watch-list | no direct production analog; `#needs-attention` covers risk surfacing | TriageSection family | risks + glean | ❌ duplicates state-of-play needs-attention; consider folding into state-of-play and dropping chapter (L6/James call) |
| value-commitments | `#value-commitments` "What we've built" | `ValueCommitments` + ChapterFreshness | intelligence.valueDelivered/successMetrics/openCommitments | ✅ component live; verify freshness header + heading copy "What we've built" |
| strategic-landscape | `#what-matters` "What matters" | `StrategicLandscape` + ChapterFreshness | intelligence.strategicPriorities/competitiveContext/marketContext | ✅ component live; heading copy "What matters" |
| the-record | `#the-record` (Work view) | `UnifiedTimeline` + `AddToRecord` | detail events/lifecycle/context entries — not intelligence | ❌ renders evidence quotes list |
| the-work | Work view `#programs`/`#recently-landed`/`#outputs` | ProgramPillRow, RecentlyLandedList, ReportGrid | programs + work data + report config | ❌ |
| reports | `#outputs` ReportGrid | `ReportCard`/`ReportGrid` via `getAccountReports` | report config + availability | ❌ |

Also in production, not yet mapped: `#your-assessment` SentimentHero,
`#relationship-health` SupportingTension, `#their-voice` QuoteWall (glean),
`#commercial-shape` CommercialShape, `#technical-shape`
AccountTechnicalFootprint (snapshot has /technical fields),
`#relationship-fabric`, `#about-dossier`/`#about-intelligence`.

**Sequencing:** (1) intelligence-fed chapters first (outlook heading/correction,
state-of-play → OnTrackChapter, headings for value/strategy), (2) producer
data-wiring for stakeholders/work/timeline/glean domains (same pattern as the
intelligence wiring: snapshot reader + per-chapter payload + projection policy),
(3) chapters whose production analog is ambiguous (watch-list) → James.

**Done so far on DOS-852 R3-chapters:** producer ships per-chapter intelligence
subsets; OutlookPanel/ValueCommitments/StrategicLandscape live; thumbs feedback
removed; salience.reason leak removed; empty chapters hidden; chrome-free quiet
shell. Feedback substrate reconciliation = DOS-854/ADR-0138.
