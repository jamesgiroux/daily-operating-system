- BLOCK — §2/§4 conflicts with ADR-0108 privacy: `source_handle` is defined as the ingestion run UUID when a run exists, while §2 says no raw internal IDs cross surface/MCP boundaries. An ingestion run UUID is a DB-backed internal run ID. Action: make `source_handle` an opaque public handle with server-side mapping/HMAC, or remove it from the public receipt; update §4/§10 to explicitly ban raw `run_id`/`ingestion_run_id` like `file_id`.

- BLOCK — §2/§4/§7 are not reconciled with stale source docs for W4-C/DOS-480. `.docs/plans/v1.4.5-waves.md` still describes the old W4-C input/receipt/idempotency contract, and `.docs/plans/v1.4.7-waves.md` still says DOS-480 exposes `dailyos.write.place_document(entity_id, filename_hint, content_b64, category?, client_dedup_key?)` with the old receipt/idempotency. The packet cites v1.4.7 as downstream owner while freezing a different contract. Action: amend both wave docs or add explicit supersession notes before L1; DOS-480 must be updated to consume the new required `entity`, required `category`, `content_type`, new receipt/error/dry-run semantics, and no old `filename_hint`/optional-category surface.

- BLOCK — §8 gated write set is incomplete against open PR lane #366. #366 touches `src-tauri/src/services/mod.rs` and `src-tauri/src/lib.rs`; §8 names context/migration/MCP coordination files but omits those shared wiring files. If W4-C needs service or ability wiring there, it can collide with account/Glean/commit_claim work. Action: either add those files to the gated/coordinated write set, or explicitly state W4-C will not touch them and identify the existing wiring path it relies on.

Final verdict: HOLD