.docs/plans/v1.4.5-workspace-memory/L0-packet-W1-A-DOS-463.md:362:- `Extractor::extract` takes `source_type: WorkspaceFileKind` (canonical), NOT a `SourceType` mirror.
.docs/plans/v1.4.5-workspace-memory/L0-packet-W1-A-DOS-463.md:382:No lane touches `mod.rs` after W1-A creates it. No lane creates a new file in `services/workspace_ingestion/` beyond placeholders. **No lane reinvents `SourceAttribution`, `SourceType`, `TrustFactorInput`, `SourceIdentifier`, `ClaimProposal`, or `Provenance` — the CI grep gate at §8 enforces this structurally.**
.docs/plans/v1.4.5-waves.md:102:1. **Shared types promoted to W1-A `contracts.rs`** (Eng cycle 5 #1). The `Extractor` and `SignalEmitter` trait signatures reference `WorkspaceClaimProposal`, `FileIdentity`, `RejectionReason`, and `SourceType`. If those types live in later-wave lanes (W3-A, W1-B respectively), the traits don't compile in W1-A. Cycle 6 moves the type definitions into `contracts.rs` (W1-A-owned). W1-B and W3-A then USE the types from `contracts` rather than defining them locally.
.docs/plans/v1.4.5-waves.md:152:W1-A L0 cycles 1–4 ran a class-pattern sweep that surfaced substrate reinventions the original cycle-6 contract introduced. Two W1-A `contracts.rs` types — `SourceType` and `SourceAttribution` — duplicate canonical substrate (`WorkspaceFileKind` at `abilities-runtime/src/abilities/provenance/source.rs:201` and the 7-field `SourceAttribution` at `:303`). The duplicates were rejected at L0 (architect cycle 3 + codex challenge cycle 3 + codex consult cycle 3).
.docs/plans/v1.4.5-waves.md:156:- `SourceType` enum dropped from `contracts.rs`. `Extractor::extract` and `IngestRequest` take `source_type: WorkspaceFileKind` instead. Migration column `source_type TEXT` stores serde-tag of canonical `WorkspaceFileKind` (storage shape unchanged).
.docs/plans/v1.4.5-waves.md:160:- W1-A §Done-when type-promotion list (line 431) drops `SourceType` from the promoted set; `WorkspaceClaimProposal`, `FileIdentity`, `RejectionReason` remain.
.docs/plans/v1.4.5-waves.md:161:- W1-A code snippet (lines 433–478) drops the `SourceType` enum line; `Extractor::extract` signature uses `WorkspaceFileKind`; `WorkspaceClaimProposal` carries canonical `SourceAttribution` + separate `ingestion_run_id`.
.docs/plans/v1.4.5-waves.md:166:**Class-pattern note for K-out:** the original cycle 6 amendment promoted `WorkspaceClaimProposal`, `FileIdentity`, `RejectionReason`, **`SourceType`** without grepping the canonical `abilities-runtime` substrate first. The substrate sweep at W1-A L0 (cycles 1–4) caught the `SourceType` and `SourceAttribution` reinventions and also the V1.1 `TrustFactorInput` reinvention. **Structural prevention going forward:** every L0 packet ships a CI grep gate (`tests/no_substrate_reinvention.rs`) asserting zero `pub struct/enum/trait` matches of the known reinvention list under the lane's owned directory. Wave-plan amendments that introduce a new W1-X / W2-X type MUST be K-in-greped first.
.docs/plans/v1.4.5-waves.md:451:- **Done when:** `workspace_file_lifecycle` table exists with the seven-state lifecycle enum (including `pending_entity_assignment`), all required columns per the project DoD plus `category: Option<TEXT>` (cycle 8 Option B-prime — stores the auto-detected or caller-provided sub-category for AI navigation); Rust types compile inside `services::workspace_ingestion::lifecycle`; `services::workspace_ingestion::contracts` defines the **shared type surface** that every subsequent lane consumes (cycle 6 amendment promotes `WorkspaceClaimProposal`, `FileIdentity`, `RejectionReason` into `contracts.rs` because the `Extractor` trait signature would otherwise reference types owned by W1-B and W3-A; cycle 8 amendment adds `WorkspaceCategory`; **cycle 12 amendment** drops the `SourceType` mirror and consumes canonical `WorkspaceFileKind` directly + drops local `SourceAttribution` reinvention and consumes canonical 7-field `SourceAttribution` from `provenance/source.rs:303`):
.docs/plans/v1.4.5-waves.md:455:    // Cycle 12 substrate-consumption: SourceType + local SourceAttribution dropped;
.docs/plans/v1.4.5-waves.md:495:        // Cycle 12: takes canonical WorkspaceFileKind (not the dropped SourceType mirror).
.docs/plans/v1.4.5-waves.md:641:- **Done when:** `IngestPipeline::run(IngestRequest)` records an ingestion run, transitions lifecycle, and produces zero claim proposals (W3-A's job to land them) where `IngestRequest { file: File, identity: FileIdentity, source_type: WorkspaceFileKind, entity_id: Option<EntityId>, mode: IngestMode, category_hint: Option<WorkspaceCategory> }` (cycle 12: `source_type` is canonical `WorkspaceFileKind` not dropped `SourceType` mirror) (cycle 9 amendment: pipeline accepts a request struct so caller-provided category from DOS-474, W2-C, W2-D, W5-A all flow through). **Cycle 10 contract reconciliation:** `category_hint` is contracted to be a registry-valid `WorkspaceCategory` value when `Some`. Caller-side validation (DOS-474, W2-C, W2-D, W5-A) is responsible for rejecting malformed/disallowed caller-provided slugs BEFORE constructing `IngestRequest` — the pipeline trusts the type. When `category_hint` is `None`, `auto_detect_category()` runs as fallback. The pipeline never sees an "invalid" hint at the type level. `pipeline::quarantine_source` API present and tested; all rejection paths return typed errors; CI lint script `check_workspace_mutation_allowlist.sh` (modeled on `check_claim_writer_allowlist.sh`) green; all tests green; `/cso` L0 plan AND L2 diff approval recorded.

codex
VERDICT: APPROVE.

Line 488 fix is good: wave plan now uses canonical `SourceAttribution` plus separate `ingestion_run_id`, and the snippet has all 10 fields.

Packet §4/§8 match the same shape; `ClaimType` / `ClaimSensitivity` are correct.

No active downstream stale-text reintroduction found. Remaining `SourceType`/old-shape hits are historical amendment context or explicit “not SourceType” guard text.
tokens used
40,808
VERDICT: APPROVE.

Line 488 fix is good: wave plan now uses canonical `SourceAttribution` plus separate `ingestion_run_id`, and the snippet has all 10 fields.

Packet §4/§8 match the same shape; `ClaimType` / `ClaimSensitivity` are correct.

No active downstream stale-text reintroduction found. Remaining `SourceType`/old-shape hits are historical amendment context or explicit “not SourceType” guard text.
