[BLOCKER] Supersession is incomplete. L0 packet §0/§4 says old v1.4.5/v1.4.7 placement surfaces are superseded and L1 is blocked until reconciliation, but `.docs/plans/v1.4.5-waves.md` §Agent W4-C still contains an active “Frozen ability contract” with optional `category`, `client_source_asof`, raw `ingestion_run_id`/`file_id`, old idempotency, and broad transaction wording. The live MCP taxonomy still advertises `topic`/`content`/`document_id`. Concrete action: replace the stale v1.4.5 W4-C block with a packet pointer or “SUPERSEDED - DO NOT IMPLEMENT”, carve DOS-474 migration ownership to TBD, and have DOS-480 or a coordinated amendment update/remove the live MCP placeholder before L1.

[BLOCKER] `content_b64` has no pre-decode/transport cap. L0 packet §4/§6/§7 rejects closed-schema failures before service/rate rows, so a huge malformed or missing-category payload can force JSON/base64 allocation outside the service ledger. Concrete action: add max encoded `content_b64` length and max MCP argument/body size, enforced before expensive parsing/decoding; mirror in DOS-480; test oversized malformed inputs produce no rows and bounded memory.

[BLOCKER] `application/json` trust boundary is underspecified. L0 packet §4 allows JSON content, while §9 says provenance/trust flow through the pipeline but does not explicitly forbid caller-supplied JSON provenance, trust, actor, source time, or claim metadata from being interpreted as trusted substrate state. Concrete action: state MCP placement content is untrusted document content only; all `source_asof`, provenance, trust, actor, lifecycle, link attribution, and claim identity are server/pipeline-derived; add forged-JSON fixtures.

[BLOCKER] `source_handle` replay/storage contract is inconsistent. L0 packet §4 requires live non-null opaque `source_handle`; §7 says the success update stores `source_handle`; the SQL table has `run_id` but no `source_handle` or mapping table. Concrete action: either freeze deterministic server-secret HMAC derivation from `run_id`, or add `source_handle` storage/mapping; update the fence algorithm and replay tests accordingly.

Final verdict: HOLD.