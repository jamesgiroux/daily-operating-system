# v2.0 Linear sweep — proposal (James gate before ANY execution)

**Date:** 2026-06-11 · **Status:** proposal; nothing below has been executed in Linear.
**Basis:** ADR-0139 (Proposed), `.docs/plans/v2.0-case.html`, full DailyOS-team project list (27 projects) and the complete v1.5.0 — Surface Designs issue list (32 issues) pulled 2026-06-11.

**Rule applied:** substrate work survives and re-homes; surface work is superseded by v2.0 with its *requirements* carried into v2.0 design (superseding a ticket never discards what it taught); maintenance pools and proposals stay.

## 1. New project
- **Create `v2.0 — Rebuild (Shell + Read Path)`** — lead James, High. Description links ADR-0139 + case doc + red-team synthesis. Track 1 (substrate): checkpoint prerequisite fixes (DOS-866 + recompute bounding + producer-read limit + commit-off-read-path) → **moat instrumentation** (2 weeks, shipped app — new ticket, the 2026-05-29 product-lens experiment) → latency re-measurement. Track 2 (parallel): James+Claude design phase (page inventory; list, triage/action-dispatch, report block families; D-spine briefing; chat escape hatch minimum shape). **Wave execution starts only after the instrumentation readout**; the read-model L0 (owned Rust projections default) opens only if the re-measurement says so. Conversion waves are route-by-route with deletion as AC.

## 2. Projects
| Project | Disposition |
|---|---|
| v1.5.0 — Surface Designs | **Supersede** (cancel with note → ADR-0139). Issues dispositioned individually in §3 — most re-home, none silently dropped. |
| v1.4.15 — Reports as Shareable Intelligence | **Keep, re-sequence.** Report-as-composition becomes the v2.0 report block family; project waits behind v2.0 design milestone. |
| v1.4.16 — Self-Healing v2 | **Keep (Planning).** Substrate-side; unaffected by shell. |
| v1.4.1 Path-α — Substrate Hardening | **Keep (maintenance pool).** One audit task added: tag tickets coupled to dying shell code as obsolete-on-conversion rather than fixing them twice. |
| v1.4.9 — Substrate Reset | **Close as complete** if no open issues remain (status never flipped; verify at execution). |
| Design System | **Keep (Maintenance).** Crosses the boundary; gains the three new block-family pattern work from v2.0 design. |
| Codebase Maintenance & Production Quality | **Keep (Maintenance).** Path-α routing target unchanged. |
| [PROPOSED] Proactive Surfacing & Interruption Policy | **Keep (Proposal).** Note added: v2.0's push-first taxonomy is its prerequisite landing; revisit immediately after v2.0. |
| [PROPOSED] Production Hardening / [PROPOSED] Accessibility & Role-Based Presets | **Keep (Proposal).** A11y's own thesis (wait for stable surfaces) now has a date: post-v2.0 conversion. |
| Already canceled/dissolved/completed projects | No action. |

## 3. v1.5.0 — Surface Designs issues (all 32; Done ones excluded)
**Re-home to v2.0 project — substrate that v2.0 commitments depend on:**
- **DOS-856** (RC-2a feedback unification, ADR-0138) — §5B "one feedback path" depends on it. High.
- **DOS-860** (enrichment split: extraction vs synthesis; retire snapshots) — read-path prerequisite; pairs with §5C.
- **DOS-861** (context assembly consolidation) — producers consume one builder; prerequisite for new producers.
- **DOS-862** (signal-bus hygiene) — write-side health under §5C.
- **DOS-855** (trigger-side cutover, legacy PTY deleted) — scheduled-producer path v2.0's briefing front door needs.
- **DOS-857 / DOS-858 / DOS-859** (post-WC reliability/dismissal consolidations) — keep, sequenced after DOS-856.
- **DOS-837** (Accept ADR-0130/0122) — still required; ADR-0130 must be Accepted before v2.0 waves. Quick.
- **DOS-835** (bank WP carve-out) — housekeeping, still valid.
- **DOS-828** (archive leaves orphaned workspace folders) — substrate archive semantics bug, shell-independent.
- **DOS-811 / DOS-812** (ADR-0125 expected_presence; ADR-0126 inv-5 enforcement) — substrate registry work; backlog in v2.0 project (parent DOS-316 see below).
- **DOS-777 / DOS-479 / DOS-173** (MCP read handlers/tools) — keep; v2.0's one-producer-contract makes these cheaper; re-home, sequence after first v2.0 producers.

**Supersede into v2.0 design as requirements (close with pointer; the need survives, the ticket's surface framing doesn't):**
- **DOS-852** (WR detail-surface reconciliation, In Progress, this branch) — substrate outputs (producers, trust-as-opacity, overlay) cross; the variant-D surface work is superseded by v2.0 design. **Close only after the branch's crossing substrate is merged to dev** (red-team amendment — trust-as-opacity's authority lives on this ticket); then close with an L4-learnings comment.
- **DOS-316 / DOS-317 / DOS-318 / DOS-443 / DOS-446 / DOS-447** (deviation section, engagement telemetry, contradiction UX, review queue, adversarial bundle, render rules) — briefing/review experience requirements → v2.0 briefing + review-queue block design inputs. The adversarial hundred-claims bundle (DOS-446) returns as a v2.0 acceptance fixture.
- **DOS-278** (briefing count divergence) — **STAYS OPEN** (red-team amendment): it is a live trust bug on the front-door surface; closing it against a months-out architecture promise trades a trust defect today for a promise. Re-home to the v2.0 project as an interim-fix-or-explicit-L6 decision; "one canonical candidate set" also becomes a v2.0 briefing AC.
- **DOS-826 / DOS-827 / DOS-829 / DOS-830** (list multi-select, editable lifecycle stages, drag-reparent, bulk archive) — close as superseded, but their needs roll into **one live umbrella ticket** in the v2.0 project ("list/triage block family requirements"), blocked on the design milestone (red-team amendment: a live tracking surface, not pointers in closed tickets).
- **DOS-836** (genericity audit) — folds into the v2.0 data-layer/producer L0s.
- **DOS-767 / DOS-768 / DOS-770** (ingestion staging follow-ups) — substrate; re-home to Path-α maintenance pool (not v2.0-blocking).

## 4. In-flight work (no Linear change; named for completeness)
- **DOS-866** (starvation fix, codex worktree) — ships regardless; its write shape is a §5C dependency.
- **DOS-851** (sensitivity dedup worktree) — substrate; ships.
- **DOS-867** (SRP sweep: extract invoke() from pages, worktree) — let the in-flight branch land if it passes L2 (cheap, improves the v1.x interim), but **no further shell SRP sweeps** — the pages it cleans are scheduled to die.

## 5. Execution order (after gate)
1. Create v2.0 project; 2. accept ADR-0139 + post it as comment on affected tickets; 3. re-homes (§3 group 1); 4. supersede-closes with pointer comments (§3 group 2); 5. project dispositions (§2); 6. K-out capture of the sweep itself.
