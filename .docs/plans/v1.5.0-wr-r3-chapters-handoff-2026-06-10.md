# Handoff — v1.5.0 WR · R3 chapter spine-D restyle — 2026-06-10

**For the fresh session.** WR R0–R5 is built, gated, and committed; James has L4-validated the trust model. The one piece left is the **chapter block spine-D restyle** — the chapter bodies still render through generic `composition*` CSS, not the bespoke spine-D chapter designs. This handoff has the grounded plan to finish it.

Branch: **`dos-852-v150-wr-detail-surface-reconciliation`** (HEAD `fd277507`). Read the WR L0 packet (`.docs/plans/v1.5.0-wr-l0-packet.md`) and proof bundle (`.docs/proofs/v1.5.0/WR-proof-bundle.md`) first.

---

## What's DONE (committed `a27fff60` → `fd277507`)

- **R0** producer↔projection parity gate (+ hardened to exercise vitals & all claim-block placements).
- **R1/R2** provenance + value formatting (prior session).
- **R3 so far:** `producer_unavailable` regression fixed; chrome-free variant-D **hero** (dot-strip vitals, ambient `IntelligenceQualityBadge`, no per-line provenance, identity-only); per-claim **`provenance_kind`** signal (`sourced`/`inferred`, keyed off `source_ref`); **trust-as-opacity** in `BlockShell` (inferred → faded ~62% + tooltip + confirm/contest; routine trust chrome pulled back; masked/unavailable safety kept); §3.1 fallback-privacy-in-edit-mode renderer test.
- **R4** type chip + vitals editable via `update_account_field` (service-layer correction + `composition.refetch`).
- **R5** proof bundle written.
- **Gates green:** `pnpm tsc`, `cargo clippy --workspace -D warnings`, `abilities-runtime` 16 tests incl. R0 gate, changed-area frontend tests.

**L4 validation (James, 2026-06-10):** "opacity level is great, the mix is right." The trust-as-opacity model is APPROVED. Do not re-litigate it.

---

## The task: chapter block spine-D restyle

The chapter **bodies** (everything below the hero) still render via generic `pageStyles.composition*` classes. They must render at **variant-D / spine-D quality**, matching the bespoke account chapter designs.

**Decision (James, 2026-06-10):** **Reuse the actual `@/components/editorial/*` primitives** so the composition chapters match spine-D as closely as the claim payload allows. **Build all chapters, then one review of the full scroll** (do NOT gate per chapter). This is design-collaborative — expect James to refine after the full-scroll review (the hero took several rounds).

### The spine-D design oracle

- Editorial primitive library: **`src/components/editorial/`** — `ChapterHeading`, `StateBlock`, `PullQuote`, `QuoteWall`, `TimelineEntry`, `MarginSection`, `FinisMarker`, `EditorialPageHeader`.
- Bespoke chapter components (what `src/pages/AccountDetailEditorial.tsx` renders per section): `AccountOutlook` (`src/components/entity/`), `StateOfPlay` (entity/ — uses `ChapterHeading` + `StateBlock`), `AccountHealthSection` + `AccountPullQuote` + `AccountTechnicalFootprint` (account/), `WatchList` (entity/) + `WatchListPrograms` (account/), `TheWork` (`src/components/weekly-impact/TheWorkSlide.tsx`), `AccountReportsSection` (account/).
- Reference CSS (canonical copy-paste): `.docs/design/reference/_shared/styles/` — `ClaimRow.module.css`, `ChapterHeading.module.css`, `StateOfPlay.module.css`, `AccountOutlook.module.css`, `AccountHealthSection`/`health.module.css`, `WatchList*.module.css`, `TheWork.module.css`, `AccountExecutiveSummary.module.css`, `AccountTechnicalFootprint.module.css`. Read `.docs/design/readme.md` first.

### Chapter order + block→primitive mapping

`VARIANT_D_SECTIONS` (`account_overview.rs:43`): `headline`(done) → `outlook` → `state-of-play` → `the-room` → `whats-next` → `watch-list` → `value-commitments` → `strategic-landscape` → `the-record` → `the-work` → `reports`.

The producer emits **one block per claim** by placement, so each composition block is a single claim — the `ClaimRow` treatment (single editorial claim row) is the natural fit for most narrative blocks; aggregate-style primitives (StateBlock) are a translation, not 1:1.

| Composition block (renderer fn) | Placement / chapter | Spine-D treatment |
|---|---|---|
| `ClaimSummaryBlock` (421) | win/value/overview/state-of-play | `ClaimRow` editorial (serif value line, mono field label) / `PullQuote` for emphasis |
| `RiskCalloutBlock` (460) | risk → watch-list/outlook | `ClaimRow` flagged treatment |
| `HealthSnapshotBlock` (435) | health → outlook | `AccountHealthSection` / `HealthBadge` look |
| `ActionListBlock` (490) | commitment/work → whats-next/the-work | editorial action rows (`TimelineEntry`-style) |
| `RelationshipMapBlock` (474) | relationship → the-room | stakeholder rows |
| `EvidenceListBlock` (509) | the-record / sources | `QuoteWall` / `TimelineEntry` evidence rows |
| `MarkdownDocumentBlock` (544) | markdown chapters | editorial body |
| `PrimitiveBlock` | pills/badges | keep as-is |

### The pattern (CRITICAL — preserve what's already built)

Each block body renders inside **`BlockShell`** (`BlockComponents.tsx:254`), which already provides: the **opacity model** (faded when `provenanceKind(payload) === "inferred"` + `INFERRED_TOOLTIP`), the **safety** provenance states (masked/unavailable/pending), and **confirm/contest** (`BlockFeedback` → `IntelligenceCorrection`). **Keep the BlockShell wrapper.** Restyle the *children* (the body) to the spine-D treatment. Keep `EditableBlockText` for inline claim edits (routes via `CompositionInlineEdit` → `edit_routes`, NOT `saveAccountField`).

- Reuse the editorial primitives directly where they fit; otherwise translate `ClaimRow.module.css` into a new `src/components/composition/blocks/CompositionChapters.module.css` (like the hero's `AccountHeroBlock.module.css`). **No inline CSS.**
- Do NOT touch `useChapterLayout`/dnd (ADR-0136). Do NOT reuse the bespoke `AccountDetailEditorial`/`saveAccountField`/`useAccountDetail` *data* wiring — only the visual CSS/primitives.
- Section headers are rendered by `AccountDetailPage.tsx` (`compositionSectionLabel`/`compositionSectionTitle`/`compositionSectionDeck`) — optionally align with `ChapterHeading`, but the chapter BODIES are the flagged gap.

### Key files
- `src/components/composition/blocks/BlockComponents.tsx` — block bodies (line refs above) + `BlockShell` (254) + `provenanceKind` helper + `BlockFeedback`.
- `src/components/composition/blocks/AccountHeroBlock.module.css` — the hero's pattern to mirror for chapter CSS.
- `src/pages/AccountDetailPage.module.css` — `compositionInferred` opacity + section/body classes.
- `src/components/editorial/*` — the primitives to reuse.
- Renderer test: `src/components/composition/ReactBlockRenderer.test.tsx` — keep green; it encodes the chrome-free hero contract + §3.1 fallback-in-edit-mode + the quiet-provenance contract. Update if a body change alters rendered text.

### Definition of done (chapter restyle)
All Account chapters render at the spine-D floor reusing editorial primitives; opacity/trust model intact; confirm/contest + inline edit intact; no raw generic placeholder bodies; no inline CSS; `pnpm tsc` + `cargo clippy -D warnings` + composition tests green; full-scroll L4 review with James recorded; then wave **L2** on the integrated diff → **PR to `dev`**.

---

## Operational state (READ)

- **Dev runs on the REPLICA. Launch: `DAILYOS_DB_MODE=replica pnpm tauri dev`** (never `live` → prod). Single instance only (`pgrep -f target/debug/dailyos` == 1; two writers → `disk I/O error (10)`). DB `~/.dailyos/dailyos-replica.db` (`PRAGMA quick_check` → ok). App log `/tmp/dailyos-dev.log`. Memory: `feedback_dev_uses_replica_never_live_db_mode`.
- **Tauri backend does NOT hot-reload** — a `.rs` change needs a rebuild + app restart (the watcher rebuilds the binary but often does NOT relaunch onto it; verify process-start vs binary mtime, restart if stale). Frontend (Vite) hot-reloads.
- Enriched test accounts: Digital-Marketing-Technology / Heroku / Credit-Karma (full arr/contract-end/nps vitals + enrichment claims to see the fade).
- **Branch hygiene:** the tree was found switched to `dev` mid-session (a different session's checkout); WR work was safe on the branch. If it happens again, `git checkout dos-852-...` — nothing is lost (commits are on the branch).
- **`pnpm test` gotcha:** the full run is polluted by a stale sibling worktree `.worktrees/codex/v1.5.0-list-surfaces-pr-b/` (vitest globs `.worktrees/`); main-repo suites pass. Run exact main-repo file paths, or scope. Path-α: vitest should exclude `.worktrees/`.
- **Commits:** WIP-checkpointed (`WIP=1 git commit` skips the heavy clippy/test/tsc tier; pre-push re-runs it). Code commits need `L2-status:` (use `not-run-acknowledged` mid-wave). Co-author: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## Decisions / parked
- **Trust = opacity, keyed off `provenance_kind` (not the score)** — APPROVED. Memory: `project_v150_trust_render_off_provenance_not_score`.
- **DOS-853** — trust-score cold-start recalibration (separate substrate track; ~90% of claims compress at 0.70–0.76 under the 0.75 threshold).
- **R4 routing** uses `update_account_field` (propagates) over the pure claim-feedback path (which wouldn't propagate the snapshot column within WR) — documented in the proof bundle for L2.
- **Lifecycle vital** — ideally source-populated (glean/salesforce); local fallback could be typeahead-from-history; plain input accepted for now.
- After chapters: **wave L2** on integrated diff → **PR to `dev`** (no auto-PR/tag before James's full-scroll L4 validation).
