# Daily Briefing Daily Briefing redesign — Ticket List

**Date:** 2026-05-06
**Status:** Draft — for review before Linear creation
**Decisions doc:** `.docs/plans/v1.4.0-daily-briefing-redesign-decisions.md`

26 tickets across 5 tracks. Each row: ticket subject (short), description summary, file allowlist (where applicable), dependencies. Once approved, I'll expand each into a full Linear ticket body and create them.

Naming convention for the wave map: `D-NN`. These will get DOS-XXX numbers from Linear on creation.

---

## Track A — Service contracts and data shape (display-layer-free)

These are the "no logic in the display" foundation tickets. Each section the briefing renders has a service that returns a shape; the view consumes the shape and renders it without conditionals or data-shaping.

| ID | Subject | What it covers | Dep |
|---|---|---|---|
| **D-01** | Define `BriefingViewModel` contract | TypeScript shape with one variant per section: `Lead`, `Schedule`, `Predictions`, `Moving`, `Watch`. Includes all states (empty/loading/error/cached/stale). Single canonical type, exported from `src/types/briefing.ts`. Blocks every Track B+C+D ticket. | — |
| **D-02** | Build `MovingService` (signal aggregation) | New service in `src-tauri/src/services/moving.rs`. Aggregates signals from email, Gong, Zendesk, Slack, Linear (via Glean), meetings, lifecycle changes. Returns top 2-3 entities by 24h-change-magnitude with their signal feed (3-5 most relevant signals each). Includes the entity provenance stats (Health/Stage/Confidence/Owner — varies by entity kind). | D-01 |
| **D-03** | Build `WatchService` (action triage) | New service in `src-tauri/src/services/watch.rs`. Returns: suggested actions with InferredActionSelector options, open actions needing attention today, parked items. Owns the rule "what triggers a Watch row" (TODAY / aging / suggested by ability runtime). | D-01 |
| **D-04** | Move email ranking + selection out of view | Lift `compareEmailRank`, today-filter, and the inline `useState` filtering currently in `DailyBriefing.tsx` into a `briefing` view-model service. View becomes pass-through. | D-01 |
| **D-05** | Move calendar selection + temporal grouping out of view | Lift the today/past/future grouping logic out of `DailyBriefing.tsx` and the equivalent ±7 day shape out of `WeekPage.tsx` into a single `BriefingScheduleService`. DayStrip neighbor-day previews come from this service. | D-01 |
| **D-06** | Build `PredictionsService` adapter | Wraps DOS-218/219 ability output into a `Predictions` slice that fits the `BriefingViewModel`. Default render is the count + "expand" hint; expanded render is the full prediction list. | D-01 |
| **D-07** | Lifecycle-change → MovingSignal adapter | Whatever currently renders the `lifecycleGroup` (account state transitions) converts into a `lifecycle` typedot SignalFeedItem on the relevant entity's Moving row. Owned by `MovingService`. | D-01, D-02 |

## Track B — Component extraction (parallel agents in W1)

| ID | Subject | What it covers | Dep |
|---|---|---|---|
| **D-08** | Build `DayStrip` primitive | TSX + module CSS for the yesterday/today/tomorrow nav. Spec already exists at `patterns/DayStrip.md`. Includes preview text from neighbor days (consumes D-05 output). | D-05 |
| **D-09** | Build `InferredActionSelector` pattern | TSX + module CSS. Trigger button + dropdown menu with selected/alternate options, divider, dismiss. Used by `WatchRow`. | — |
| **D-10** | Build `SignalDot` primitive | Tiny tinted dot indicating signal kind: `meeting`, `action`, `email`, `lifecycle`, `gong-call`, `zendesk-ticket`, `slack-thread`, `linear-issue`. Each kind gets a token-driven color. Spec + tsx + module CSS. | — |
| **D-11** | Build `MovingRow` pattern (Shape A — locked) | 3-column grid: entity column (name + state pill), main column (lede + signal feed), provenance column (stacked stats). Spec + tsx + module CSS. Composes `SignalDot`, `Pill`, `EntityChip`. | D-10 |
| **D-12** | Build `WatchRow` pattern | Adaptive row supporting suggested-action, open-action, parked-item, aging variants. Composes `InferredActionSelector` for the action variants. Spec + tsx + module CSS. | D-09 |
| **D-13** | Build `PredictionsSection` pattern | Default minimal state (count + expand hint), expanded list state. Composes ability-output cards. Spec + tsx + module CSS. | D-06 |
| **D-14** | Build `Lead` pattern | Hero section with sharp punch line emphasis. Spec exists; verify and ship tsx + module CSS if missing. | — |
| **D-15** | Wire trust-band visual into MeetingSpineItem | Replace existing fresh/developing IntelligenceQualityBadge with the W6 trust-band shape (DOS-320 output). Update spec. | DOS-320 (W6) |
| **D-16** | Wire claim-lifecycle "corrected" visual into SignalDot | When a signal's source claim has been corrected (DOS-411), the SignalDot picks up a corrected indicator. | DOS-411 (W6) |

## Track C — Surface integration

| ID | Subject | What it covers | Dep |
|---|---|---|---|
| **D-17** | Build `DailyBriefingRedesign.tsx` source component | Composes Lead + DayStrip + Schedule (MeetingSpineItem) + Predictions + Moving + Watch. Pure pass-through to `BriefingViewModel`. Zero conditional logic, zero data shaping. | D-01..D-14 |
| **D-18** | Feature flag + dev scenario for Daily Briefing redesign | Toggle in dev mode or via flag to switch the routed `/` between current DailyBriefing and DailyBriefingRedesign. Side-by-side validation. | D-17 |
| **D-19** | Promote `briefing-redesign.html` to `briefing.html` (canonical) | After QA on the routed page, the redesign reference becomes THE briefing reference. Old `briefing.html` archives. Update `surface-manifest.json`. | D-17, D-18 |

## Track D — Adjacent surface uplift

| ID | Subject | What it covers | Dep |
|---|---|---|---|
| **D-20** | Update `/emails` as raw inbox surface | Reframe /emails as the unfiltered email view (since Moving curates 2-3). Reference HTML + spec update. | — |
| **D-21** | Update `/actions` as canonical action backlog | Reframe /actions as the full action backlog (since Watch surfaces only TODAY's). Reference HTML + spec update. | — |
| **D-22** | Move expansion-panel content (`theRoom`, `prepGrid`) into MeetingDetailPage | The current briefing's BriefingMeetingCard expansion panel content (room + prep deck + before-meeting actions) lives on /meeting/$id going forward. Audit MeetingDetailPage for completeness; close any gap. | — |

## Track E — Cleanup

| ID | Subject | What it covers | Dep |
|---|---|---|---|
| **D-23** | Deprecate `/week` route | Remove WeekPage.tsx + module CSS. Remove route. Archive `reference/surfaces/week.html`. Audit any cross-page link to /week and redirect. | D-08 (DayStrip live), D-19 |
| **D-24** | Archive `BriefingMeetingCard` + `MeetingCard` (briefing usage) | Once D-19 lands, briefing surface no longer imports either. Audit all consumers; archive when zero remain. | D-19 |
| **D-25** | Trim `editorial-briefing.module.css` (1490 → ~400 lines) | Strip every rule whose consumers have been culled (`lifecycleGroup`, `priorityItem` family, `replyMeta`, `agingNotice`, `prepFlag`, `priorityCheck`, `capturedSummary`, expansion panel rules, etc.). Remaining rules stay only if multiple Daily Briefing redesign patterns share them. | D-19 |
| **D-26** | DailyBriefing.tsx audit — confirm zero data-shaping logic | Final pass: confirm `DailyBriefingRedesign.tsx` has zero conditionals beyond render-state checks, no data filtering, no ranking, no useState beyond UI-only state (expand/collapse). Capture exception list if any. | D-17 |

---

# Wave shape

| Wave | Tickets | Parallel | Notes |
|---|---|---|---|
| **W0** | D-01 | 1 | Contract is the unblock for everything. |
| **W1** | D-08, D-09, D-10, D-14 | 4 | Component extractions with no service deps. |
| **W2** | D-02, D-03, D-04, D-05, D-06, D-07 | 6 | Services in parallel — non-overlapping rust files. |
| **W3** | D-11, D-12, D-13 | 3 | Composed patterns; depend on W1 primitives + W2 services. |
| **W4** | D-15, D-16, D-22 | 3 | Trust-band + lifecycle wire-in (gated on W6 W6 outputs). |
| **W5** | D-17, D-18, D-20, D-21 | 4 | Surface integration + adjacent surfaces. |
| **W6** | D-19, D-23, D-24, D-25, D-26 | 5 | Cutover + cull. Sequential within: D-19 first, then the cleanup tickets. |

Wall-clock target: **2-3 weeks** assuming W6 v1.4.0 outputs land on schedule for the trust-band/lifecycle dependencies (D-15, D-16).

---

# Open items before Linear creation

1. **Linear project:** create under existing **v1.4.0 — Abilities Runtime Spine**, or a sibling project ("Daily Briefing Daily Briefing redesign")? My lean: sibling project, since this is a parallel design track that depends on but doesn't gate v1.4.0 release.
2. **Cycle:** target which cycle? Coordinate with W6 timing.
3. **Owners:** is this a "James + agents" track, or assign others?
4. **D-15/D-16 timing:** these depend on W6 (DOS-320, DOS-411). Are those green when Daily Briefing redesign wave hits W4? If not, sequence shift required.

Once we resolve those four, I create tickets and write `.docs/plans/v1.4.0-daily-briefing-redesign-waves.md`.
