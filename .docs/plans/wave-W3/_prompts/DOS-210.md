**START** by reading `.docs/plans/wave-W3/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W3-A — DOS-210

- **Title:** Substrate: Ability Registry and #[ability] Macro Infrastructure
- **Linear:** https://linear.app/a8c/issue/DOS-210 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W3/DOS-210-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W3-A — DOS-210"
- **Reviewer:** architect-reviewer

## Things to be aware of

- 2026-04-20 plan-eng-review locked **compile-time AST inspection** as the category enforcement mechanism. The macro analyzes the ability's function body AST at compile time, detecting transitive calls into `services::*` mutation functions. Read and Transform abilities with any detected mutation-path call fail to compile. Publish/Maintenance allowed; descriptors record inferred mutation set.
- ADR-0102 Amendment A (error-handling contract, `AbilityResult<T>`) and Amendment B (experimental flag) apply.
- ADR-0120 Observability Contract — macro emits instrumentation span at function entry.
- Depends on DOS-209 (W2-A ServiceContext) — AbilityContext wraps it. W2-B already merged.
- Companion: DOS-211 (Provenance) — abilities produce `AbilityOutput<T>` which requires Provenance.
- Files owned per wave plan: `src-tauri/abilities-macro/` proc-macro crate (new), `src-tauri/src/abilities/registry.rs` (new), trybuild fixtures.

## Key code surfaces to grep

- `src-tauri/abilities-macro/` — does not exist yet
- `src-tauri/src/abilities/` — does not exist yet (this PR creates it)
- `src-tauri/src/services/` — for the mutation allowlist seed
- `Cargo.toml` workspace members — proc-macro crate registration
- `.docs/decisions/0102-abilities-as-runtime-contract.md` — the spec + Amendments A, B
- `.docs/decisions/0120-observability-contract.md`

## Specific design decisions to record in §3

- Mutation allowlist: where is it defined? (`scoring::mutation_allowlist` per ticket — confirm)
- Cycle detection algorithm for `composes` DAG (property test with 100 random DAGs + 100 cyclic per ticket)
- Inventory uniqueness check at startup vs build time
- Documentation generator output format

Write the plan now.
