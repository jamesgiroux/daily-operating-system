**START** by reading `.docs/plans/wave-W3/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W3-D — DOS-301

- **Title:** Substrate: derived-state writers — keep legacy AI surfaces updated from intelligence_claims
- **Linear:** https://linear.app/a8c/issue/DOS-301 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W3/DOS-301-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W3-D — DOS-301"
- **Reviewers (3 stacked):** architect-reviewer + security-auditor + performance-engineer

## Things to be aware of

- Final design after Codex rounds 1 + 2: **sync DB projection inside transaction + sync-best-effort file projection POST-DB-commit**.
- Round 1 was sync-in-transaction (rollback fallacy). Round 2 was async-via-job-queue (stale-window problem). The right answer is sync-best-effort post-commit. Bundle 5 (correction resurrection) test reads from claims (source of truth), not files.
- Per-rule SAVEPOINT failure isolation inside the DB transaction: one rule fails → others succeed → status row records failure.
- Shared input validators (Codex round 2 finding 16): `db/accounts.rs:1205-1212` JSON validation logic moves to `src-tauri/src/validators/json_columns.rs`. `propose_claim` and `db/accounts.rs` both call into it. CI lint blocks duplicate inline validation.
- `claim_projection_status` table tracks per-target state (`entity_intelligence` | `success_plans` | `accounts_columns` | `intelligence_json`).
- Repair Tauri command: `cargo run --bin repair_claim_projections [--target=...] [--entity=...]`.
- Mode-aware: `Simulate` and `Evaluate` modes write nothing to legacy.
- Hard depends on DOS-7 (`commit_claim` write path), DOS-308 (is_suppressed correctness), DOS-310 (per-entity claim invalidation), DOS-311 (universal write fence — file projections respect schema_epoch via wrapped `write_intelligence_json`).
- Companion (parallel inside W3): DOS-7 — coordinate so W3-C ships schema + write path first, you layer projection on top in the same wave PR train.

## Files owned

- `src-tauri/src/services/derived_state.rs` (new — sync DB + sync-best-effort file)
- `src-tauri/src/validators/json_columns.rs` (new — shared validators extracted from `db/accounts.rs`)
- `claim_projection_status` table schema + migration
- Repair Tauri command
- Lint test: no direct legacy writes outside `services/derived_state.rs`
- Lint test: no inline JSON validation outside `validators/json_columns.rs`

## Key code surfaces to grep

- `src-tauri/src/db/accounts.rs:1205-1212` — JSON validators that move to shared
- `src-tauri/src/intel_queue.rs` — AI-output paths to refactor through `commit_claim`
- `src-tauri/src/state.rs:983-1009` — SQLite writer constraints
- `src-tauri/src/db/core.rs:117-140,217-223` — SQLite writer-lane behavior
- `src-tauri/src/intelligence/write_fence.rs` (W1-B, already merged) — wrapped `write_intelligence_json`
- `.docs/plans/storage-shape-review-2026-04-24.md` Knock-on A/D/E

## Specific design decisions to record in §3

- SAVEPOINT-per-rule semantics — exact API used (`tx.savepoint("rule_name")?`)
- Failure isolation test: simulated DB I/O error in one DB projection target → other projections committed
- Failure isolation test: simulated file write failure → DB committed, status='failed', success returned to caller
- Idempotency property test
- Reader smoke test on dev DB: ≥20 entities show identical content from `intelligence_claims` queries vs projected legacy
- DOS-287 cross-account bleed regression test green

## Coordination notes for §7

- DOS-7 (W3-C) is your closest collaborator. Migration numbering coordination is critical: W3-C ships schema + `services/claims.rs::commit_claim` first; you layer projection on top.
- W3-C calls into your `validators/json_columns.rs` from `propose_claim`. The validator API must exist before W3-C lands its `commit_claim` body.
- W3-E (DOS-294) `claim_feedback` writes are observed by your projection layer (test stub at minimum).

Write the plan now.
