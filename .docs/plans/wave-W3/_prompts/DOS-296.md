**START** by reading `.docs/plans/wave-W3/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W3-F — DOS-296

- **Title:** Substrate allowance: thread_id on intelligence_claims + Provenance for longitudinal topic threading
- **Linear:** https://linear.app/a8c/issue/DOS-296 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W3/DOS-296-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W3-F — DOS-296"
- **Reviewer:** architect-reviewer

## Things to be aware of

- This is a **substrate allowance** — minimal scope. Nullable column, additive Provenance field, zero behavioral impact. v1.4.2 will own retrieval + UI + thread creation.
- `Provenance.provenance_schema_version` STAYS at `1` — additive, optional, default-empty field per ADR-0105 §1 forward-compatibility.
- `dedup_key` formula does NOT include `thread_id` — same-content claims in different threads remain the same claim.
- Hard depends on DOS-7 schema landing first; coordinate migration numbering.
- Companion: DOS-211 — `thread_ids: Vec<ThreadId>` field lands in the Provenance envelope created there.

## Files owned

- `thread_id TEXT NULL` column on `intelligence_claims`
- `ThreadId(Uuid)` newtype with serde + JsonSchema impls
- `Provenance.thread_ids: Vec<ThreadId>` field (additive)
- Migration

## Key code surfaces to grep

- `src-tauri/src/abilities/provenance.rs` (or equivalent under DOS-211's `abilities/provenance/`)
- `src-tauri/src/migrations/` — current head migration number
- `.docs/decisions/0124-longitudinal-topic-threading.md` — ADR-0124, the substrate allowance + v1.4.2 decision deferral
- `.docs/decisions/0105-provenance-as-first-class-output.md` §1 forward-compat rules
- `.docs/decisions/0113-human-and-agent-analysis-as-first-class-claim-sources.md` §8 (`intelligence_claims` row schema)

## Coordination notes for §7

- DOS-7 (W3-C) writes the `intelligence_claims` migration first. Your migration adds the column on top. Coordinate file numbering.
- DOS-211 (W3-B) creates the Provenance envelope. Your `thread_ids` field lands there. Coordinate with W3-B so the field is included in their initial schema rather than added separately.

Write the plan now.
