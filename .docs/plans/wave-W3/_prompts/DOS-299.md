**START** by reading `.docs/plans/wave-W3/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W3-G — DOS-299

- **Title:** Substrate: source_asof population semantics + freshness fallback chain
- **Linear:** https://linear.app/a8c/issue/DOS-299 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W3/DOS-299-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W3-G — DOS-299"
- **Reviewers:** architect-reviewer + performance-engineer

## Things to be aware of

- Reframing: this is a **promote existing** `ItemSource.sourced_at` into a first-class column, NOT a greenfield design. The Rust type `ItemSource` already exists at `intelligence/io.rs:29-41`; the LLM-prompt contract already asks for `sourcedAt` per item at `dimension_prompts.rs:608`.
- ~10 write paths stamp `Utc::now()` instead of consulting upstream timestamps — audit and normalize each.
- Codex round 2 corrected scope: backfill IS required (round 1 said it wasn't — wrong). Backfill chain documented in ticket §"Backfill chain":
  1. parseable + within bounds → lift
  2. parseable but implausible (ADR-0105 §F) → lift + warn
  3. malformed → quarantine, migration halts
  4. no itemSource → `LegacyUnattributed` at confidence 0.5
- Trust factor count: **5 canonical factors per ADR-0114 R1.4**. `subject_fit_confidence` is a composer-local helper. `unknown_timestamp_penalty` is NOT a separate factor — folded as input modifier on `freshness_weight` via `FreshnessContext { timestamp_known: bool }`.
- Backfill bar: ≥95% coverage on legacy claims with `ItemSource.sourced_at` populated.
- Hard depends on DOS-211 (Provenance envelope shape — `SourceAttribution`).
- Companion: DOS-7 (lands together; backfill is part of consolidation migration).

## Files owned

- `source_asof` population code on `SourceAttribution`
- Freshness fallback chain in trust input prep
- Semantic-bound parser per ADR-0105 §F (rejects parseable-but-implausible timestamps)
- Backfill script
- Factor-count consistency assertion (ensures Trust Compiler scores using 5 canonical inputs)
- ADR-0114 R1.3 small extension for `timestamp_known: bool`

## Key code surfaces to grep

- `src-tauri/src/intelligence/io.rs:29-41` — `ItemSource` type
- `src-tauri/src/intelligence/io.rs:1185-1189` — `effective_confidence` default
- `src-tauri/src/intelligence/io.rs:1655,1719` — synthesized-item `Utc::now()` stamps
- `src-tauri/src/intel_queue.rs:2014` — `is_suppressed` semantics
- `src-tauri/src/intelligence/dimension_prompts.rs:608` — `sourcedAt` prompt contract
- `src-tauri/src/intelligence/glean_provider.rs:1182` — write path needing normalization
- `.docs/decisions/0105-provenance-as-first-class-output.md` §E backfill + §F bounds (2026-04-24 amendments)
- `.docs/decisions/0107-source-taxonomy-alignment.md` — LegacyUnattributed amendment
- `.docs/decisions/0114-scoring-unification.md` R1.3 (FreshnessContext) + R1.4 (5 canonical factors)
- `.docs/plans/storage-shape-review-2026-04-24.md` §Finding 3, §Finding 4

## Specific design decisions to record in §3

- Provenance builder fallback chain: `itemSource.sourcedAt` → Glean document `lastModified` / `createdAt` → emit `SourceTimestampUnknown { source_index, fallback }` warning.
- Quarantine table shape (per ADR-0105 §F bounds)
- `STALENESS_PADDING` default (30 days) and `unknown_timestamp_penalty` default (0.6) — config location

Write the plan now.
