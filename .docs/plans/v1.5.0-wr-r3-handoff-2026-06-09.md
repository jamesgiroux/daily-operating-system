# Handoff — v1.5.0 WR · R3 (Account Detail variant-D rendering) — 2026-06-09

**For the fresh session.** Read this, then the WR L0 packet (`.docs/plans/v1.5.0-wr-l0-packet.md`, §3 Decision D + §4 WR-R3) and the wave plan (`.docs/plans/v1.5.0-waves.html`, WR section). Branch: **`dos-852-v150-wr-detail-surface-reconciliation`** (6 commits, all gated). Dev runs on a **plain replica** — see Operational state. R0/R1/R2 are done; **R3 is the visible payoff**.

---

## The task

Render **Account Detail** at **variant-D quality** — chapter by chapter, **hero first** — consuming the claim-backed composition the producer already emits. This is the "new design rendered" part of W1's proof that was never delivered: the blocks currently render through a raw/generic component library (`BlockShell` cards + `pageStyles.composition*`), not the editorial design system. **The data is right; the visual is what's missing.**

R3 is **Account-only** (forward surfaces are a follow wave). The **design is James-collaborative**: translate the existing design system as the *floor*, make new design decisions with James per chapter — don't big-bang it, and don't just lift the meridian mockups.

## What R0/R1/R2 already unlocked (so R3 dogfoods correctly)

- **R1 (committed `695968f4`):** account vitals now carry provenance, so the producer no longer drops them. On the replica, 27+ accounts have `arr_source` etc.
- **R2 (committed `02667aac`):** each vital emits `value` (typed) + `display_value` (`$185,400`, `Nov 24, 2026`) + `kind` (currency/date/number/text) + a clean `source_label` ("Workspace file (entity document)"). **Render `display_value`** (fallback `value`); never re-format raw values in React.
- **R0 (committed `aee5c79d`):** a producer↔projection parity gate (`account_overview` test `dos670_producer_output_passes_w4d_projection`) fails CI if a producer payload and its bindings desync. If R3 needs the producer to emit a new field, add the payload field **and** its `display_only` binding together, or this gate fails loud (that's the point — it's what caught the earlier `producer_unavailable` blank-page).

## The R3 pattern

- Render the `account_overview` (hero) + the variant-D chapters reusing the **curated design-system CSS** — `EntityHeroBase.module.css` (76px serif title, 21px italic serif lede, mono uppercase meta row), `VitalsStrip.module.css`, `AccountHero.module.css`. **No inline CSS** — compose the modules.
- **Chrome-free masthead:** the hero must NOT use `BlockShell` (the white card + redundant trust chips James flagged). The headline chapter is full-bleed; the page already gives it special treatment (`compositionMasthead`).
- **Inline edits → claim feedback:** route through `CompositionInlineEdit` → the block's `edit_routes` (ADR-0130), **never** `saveAccountField` / DB field writes.
- **Inside the W2 overlay:** R3 renders inside the existing `useChapterLayout` edit/customize machinery (toggle/reorder/variant/dnd) — **do not modify** `useChapterLayout` or the dnd code (ADR-0136). Add a renderer test that the §3.1 unknown-block fallback privacy boundary still holds in edit mode.
- **HERO IS IDENTITY-ONLY — NO LEDE.** James decided this (2026-06-08): meta row (trust band · freshness · account-type chip) → 76px serif name → vitals strip. `EntityHeroBase`'s own comment also says the redesigned heroes are identity-only. Do **not** render a claim lede in the hero.
- Walk chapters in `VARIANT_D_SECTIONS` order (`account_overview.rs:43`): `headline` → `outlook` → `state-of-play` → `the-room` → `whats-next` → `watch-list` → `value-commitments` → `strategic-landscape` → `the-record` → `the-work` → `reports`. Each: claim payload in → variant-D component out (reuse curated CSS) → edits to feedback. Review with James per chapter.

### Starting reference (the stash)

`stash@{0}` ("WR ad-hoc hero/provenance/formatting draft") holds my **first-pass chrome-free hero** that James reviewed: `AccountHeroBlock.module.css` (composes `EntityHeroBase`), the `AccountOverviewBlock` rewrite, and the full-width masthead change in `AccountDetailPage.tsx`. **Use it as design reference, but DO NOT `git stash pop` it wholesale** — it also contains the *old* R1/R2 producer changes, now **superseded** by the committed R1/R2 (which are better/safer). Cherry-pick only the **frontend** intent (hero structure + CSS + masthead), re-derived on top of current `HEAD`. (`stash@{1}` is unrelated — an old w1 base.)

### Key files

- `src/components/composition/blocks/BlockComponents.tsx` — `BLOCK_RENDERERS` map + the (currently raw) block bodies; `AccountOverviewBlock` is the hero (still `BlockShell`-wrapped on `HEAD` — R3 makes it chrome-free). `BlockShell` is the card to drop for the hero. The R2 `display_value` render is already wired in the baseline vital row.
- `src/components/composition/ReactBlockRenderer.tsx` — block-type → component dispatch.
- `src/pages/AccountDetailPage.tsx` — page; headline section gets `compositionMasthead`; the rest render via `SortableBlockFrame`. (R2 left this baseline; the stash has the full-width masthead variant.)
- `src/hooks/useChapterLayout.ts` — the W2 layout overlay / customize. **Don't touch.**
- `src/components/composition/CompositionInlineEdit.tsx` — the edit→claim-feedback path.
- `src/services/composition/contracts.ts` — `ProjectedBlock`/payload TS types.
- **Visual source (reuse CSS, not data/edit wiring):** `src/components/account/AccountHero.module.css`, `src/components/entity/EntityHeroBase.module.css`, `src/components/entity/VitalsStrip.module.css`; `.docs/design/reference/` is the canonical copy-paste design system — read `.docs/design/readme.md` first.
- Producer (if a chapter needs a field the producer doesn't emit): `src-tauri/abilities-runtime/src/abilities/account_overview.rs` — extend it (emit field + `display_only` binding; R0 gate enforces parity). The vital formatting helpers (`vital_display`, `vital_source_label`, `format_thousands`) live here from R2.

## Operational state (READ — this bit the last session)

- **Dev runs on the REPLICA. NEVER `DAILYOS_DB_MODE=live`** — that points dev at the **production** DB. Debug builds default to Replica. **Launch: `DAILYOS_DB_MODE=replica pnpm tauri dev`** (or bare). Memory: `feedback_dev_uses_replica_never_live_db_mode`.
- **Replica (plain SQLite, unencrypted — v1.5.0 has no cipher):** DB `~/.dailyos/dailyos-replica.db`, workspace `~/.dailyos/replica-workspace`, state `~/.dailyos/replica`. Isolated from prod. As of this handoff: **48 active accounts, ~235 claims and climbing** (James re-synced Google + Glean — enrichment actively populating; vitals/claims will keep growing). Verify: `sqlite3 ~/.dailyos/dailyos-replica.db "PRAGMA quick_check;"` → `ok`.
- **Prod `~/.dailyos/dailyos.db` is ENCRYPTED** (James restored a backup) and **unopenable by the cipher-less v1.5.0 build** — leave it alone; recovering prod (decrypt with key, or rebuild) is a separate James decision, not needed for dev. This is also why MCP-on-prod is down.
- **Single instance only** (`pgrep -f target/debug/dailyos` == 1). Two writers on one SQLite file → `disk I/O error (10)`. Memory: `feedback_duplicate_dailyos_instance_disk_io_error`.
- Stale encrypted replica backup `~/.dailyos/dailyos-replica.db.encrypted-bak-stale` can be deleted (not in use).
- App log: `/tmp/dailyos-dev.log`.
- **First R3 move:** relaunch (replica mode), open an enriched account (e.g. Blackstone), confirm the hero now renders vitals (`$185,400` / `Nov 24, 2026` with "Workspace file" provenance) — then build the chrome-free editorial hero for James's review.

## WR wave status (DOS-852, L0 PASSED)

| Sub-stream | Status |
|---|---|
| R0 — producer↔projection parity gate | ✅ committed (`aee5c79d`; also fixed a pre-existing W3 scope-allowlist test-isolation bug) |
| R1 — field provenance at the file-sync | ✅ committed (`695968f4`; accounts; **people verified moot** — `push_person_field` has no provenance gate, so person vitals already render) |
| R2 — producer-side value formatting | ✅ committed (`02667aac`) |
| **R3 — Account variant-D rendering** | **← this handoff** |
| R4 — editable type/lifecycle chip | pending — via typed `AccountFieldCorrection` (ADR-0123), not a `FeedbackTarget` (field is snapshot-derived, not claim-backed); verify the correction propagates to the rendered value or descope to a follow ticket |

Commits on branch: `a1242018` L0 docs → `ae1ea93b` R0 tests (WIP) → `aee5c79d` R0 fix → `695968f4` R1 → `02667aac` R2 → `22abb126` handoff-landmine doc fix.

## Process notes / gotchas

- **L2-status** mandatory in code commits (`not-run-acknowledged` for WIP mid-wave; L2 review runs on the integrated wave diff before the PR to `dev`).
- **Pre-commit gate:** `WIP=1 git commit …` skips the heavy clippy/test/tsc tier for checkpoints (pre-push re-runs them). The schema/mock-sync check false-positives on any `db/*.rs` edit even when no table/column is added — if that fires on a non-schema change, `--no-verify` after verifying gates manually, and surface it.
- **Don't repeat the last session's two wrong turns:** (1) don't reuse the curated `src/components/account/*` components' *data/edit* wiring (`useAccountDetail`/`saveAccountField` — wrong runtime); reuse only their CSS. (2) Keep producer payload↔binding in parity (R0 gate).
- **Definition of done (WR-R3):** Account Detail matches the variant-D floor per chapter (L4 visual evidence vs `.docs/design/reference/`), renders provenance + trust + safe fallback, one-composed-scroll reads well, no inline CSS, no raw `pageStyles.composition*` placeholder bodies remain on Account chapters, fallback-privacy-in-edit-mode test passes, per-chapter review with James recorded.
- After R3: **R4** (editable chip), then the WR wave's L2 (on the integrated diff) → PR to `dev`.

## Parked / separate from R3

- **Prod DB recovery** (encrypted; decrypt vs rebuild vs leave) — James's call; fixes MCP-on-prod too.
- **MCP** down on dev + prod (same root cause: encrypted/mode DB state) — revisit when convenient.
- **`set_person_field_source` / `enrichment_sources` clobber-safety** (L0 data-integrity reviewer note) — enrichment-side, not composition, not WR-blocking → Codebase Maintenance.
- The enrichment/composition fixes on `codex/enrichment-pty-150s-hardening` (`4e9424e9`, `0a5d8641`, PTY timeout commits) still need their own PR to `dev`.
