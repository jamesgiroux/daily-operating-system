# v1.0.0 — GA: Rearchitecture + Intelligence + Launch Quality

**Thesis:** v1.0.0 is the GA release. It combines structural cleanup of the local architecture, a complete intelligence foundation, the CS report suite, and everything needed for a trustworthy, polished launch: search, offline mode, data export, privacy controls, and editorial consistency across every surface. v0.16.1 (beta hardening) and v0.16.2 (UI finesse) were dissolved and absorbed — one version, one launch, no half-measures. DailyOS stays local-first and personal. Governance and team views are solved in v1.1.0 via output-layer publication + Glean Agents, not server sync. ADR-0099 (remote-first) was proposed and withdrawn after first-principles review.

**Status:** Planning. Phase 1 specs written (I511, I512, I539). I509 reframed for Glean boundary. Open questions resolved. Requires v0.16.0 (onboarding) to ship first — only remaining pre-1.0 dependency.

**Architecture audit:** `.docs/architecture/` (14 documents)
**First-principles review:** `.docs/research/2026-03-03-architecture-first-principles-review.md`
**ADR-0099 (withdrawn):** `.docs/decisions/0099-remote-first-server-canonical-architecture.md`

---

## Principles

1. **DailyOS stays local-first.** SQLite is the canonical data store. No server dependency for core operation.
2. **Intelligence is personal.** Each user's intelligence is the synthesis of Glean's org knowledge + their personal context. Not shared.
3. **Workspace files are eliminated.** DB as sole local data layer. No `_today/data/`, no `intelligence.json` on disk.
4. **ServiceLayer is mandatory.** Every mutation goes through ServiceLayer. No direct DB writes from command handlers.
5. **Sharing is at the output layer.** Reports and curated summaries are publishable. Raw signals and intelligence are private.
6. **Intelligence computation is pluggable.** DailyOS owns the personal state machine, consumption surface, and orchestration. Whether intelligence is computed locally or provided by Glean Agents, the data model, rendering, and orchestration remain the same. See `.docs/research/2026-03-04-dual-mode-intelligence-architecture.md`.

---

## Phase 1: Schema + ServiceLayer + Workspace File Elimination

**Goal:** Clean data model, mandatory service boundary, workspace files gone. The app works exactly as before but on a cleaner architecture.

| ID | Title | Notes |
|----|-------|-------|
| **I511** | Local schema decomposition | Backend-only schema/migration safety hardening: fail-hard migration runner, guaranteed pre-migration backups, atomic decomposition migration, integrity checks, and decomposition schema correctness (`account_stakeholders` role/data_source path). Spec: `.docs/issues/i511.md`. Absorbs I381. |
| **I539** | Database recovery UX for migration/DB failure | Production recovery path in app UI: startup blocker + Settings/Data restore controls, with backup list/restore commands. Spec: `.docs/issues/i539.md`. Explicitly out-of-scope from I511. |
| **I512** | ServiceLayer: mandatory mutation path | Eliminate 16 remaining direct DB calls from commands.rs, signal emission audit on all 16 service modules, CI enforcement. Services already ~70% built — this completes and enforces. Spec: `.docs/issues/i512.md`. Absorbs I380, I402. |
| **I513** | DB as sole source of truth for app-generated state | App stops reading `intelligence.json`, `dashboard.json`, `_today/data/*.json` as data sources. DB is the sole read path. Workspace dirs + user files stay — filesystem remains the knowledge management surface for user-contributed documents. Spec: `.docs/issues/i513.md`. Absorbs I436. |

**Acceptance criteria:**

1. `meetings`, `meeting_prep`, `meeting_transcripts` tables exist and populated correctly (3-table decomposition — outcomes already stored via captures system)
2. `entity_assessment` replaces `entity_intelligence` (assessment data). `entity_quality` (existing) extended with health_score, health_trend, coherence columns migrated from entity_intelligence.
3. `account_stakeholders` replaces `entity_people` (account rows) + `account_team` — no data loss, roles and relationship_types preserved, data_source tracked per ADR-0098
4. Every user-facing mutation goes through `ServiceLayer`
5. App reads zero app-generated state from filesystem. `intelligence.json`, `dashboard.json`, `person.json` deletable without breaking app. `_today/data/` is write-only pipeline scratch — app reads daily data from DB. User-contributed files in entity dirs still read/processed normally.
6. App works end-to-end: briefing renders, meeting detail renders, reports generate
7. All tests pass. Clippy clean. TSC clean.
8. Production recovery flow exists in UI for database migration/integrity failure (startup blocker + Settings recovery controls).

---

## Phase 2: Intelligence Foundation

**Goal:** Every account has a health score and relationship data. Intelligence schema redesigned with 6 research-grounded dimensions. Glean provides org-level analysis that DailyOS synthesizes with personal context.

**Design principle:** Intelligence interfaces (traits, input types) should accept structured input from either local computation OR Glean Agent responses. Same types, different fill path. This ensures v1.0.0 local intelligence and v1.1.0 Glean Agent integration share the same data model without refactoring. See dual-mode architecture research doc.

| ID | Title | Notes |
|----|-------|-------|
| **I508** | Intelligence schema redesign | 6 dimensions on new schema. `entity_assessment` + `entity_quality` tables. |
| **I499** | Health scoring engine | 6 algorithmic dimensions, lifecycle weighting, sparse data handling. |
| **I503** | Health schema evolution | AccountHealth struct, RelationshipDimensions, migration. |
| **I500** | Glean org-score parsing | Extract structured health data from Glean as baseline. |
| **I504** | AI-inferred relationship extraction | Fix prompt schema, persist to `person_relationships`. |
| **I505** | Glean stakeholder intelligence | Contact discovery, profile enrichment, entity linkage, team sync. |
| **I506** | Co-attendance relationship inference | Algorithmic collaborator/peer edges from meeting frequency. |
| **I487** | Glean signal emission | New-only document signals, ADR-0098 purge compliance. |
| **I509** | Transcript personal interpretation + sentiment | Personal priority impact, relationship trajectory, sentiment as local signal. Org-level dynamics (talk ratios, question density) deferred to Glean. Spec: `.docs/issues/i509.md`. |
| **I507** | Source-attributed correction feedback | Person profile corrections + email disposition feedback only. LLM source attribution deferred post-v1.0.0. |
| **I528** | ADR-0098 data lifecycle infrastructure | `DataSource` enum, `purge_source()`, `data_lifecycle.rs`. Prerequisite for I487 + I505 purge ACs. Spec: `.docs/issues/i528.md`. |

**Acceptance criteria:**

1. 95%+ of enriched accounts have non-null health_score in `entity_quality`
2. All 6 intelligence dimensions populated for enriched accounts
3. Person relationships visible from AI inference (I504), Glean (I505), and co-attendance (I506)
4. Glean signals in `signal_events WHERE source = 'glean'` after enrichment
5. Sparse account (1 meeting, no email) has health score with confidence qualifier
6. A user correction on Glean-sourced data adjusts source reliability

---

## Phase 2a: Dev Tools Mock Data Migration

**Goal:** Mock data seeds conform to the v1.0.0 schema so frontend developers can test against realistic data. Scenarios simplified. Workspace file writes eliminated.

| ID | Title | Notes |
|----|-------|-------|
| **I536** | Dev tools mock data migration | Rewrite `seed_database()` for v1.0.0 schema tables. Rich 6-dimension intelligence per I508. Health scores per I499. Signal/feedback variety per I529/I530. Scenarios consolidated (6→4: reset, full, no_connectors, pipeline). Workspace file writes removed (except pipeline scenario). `mock-` prefix IDs for clean purge. Spec: `.docs/issues/i536.md`. Depends on I511, I508, I499, I503, I512. |

**Acceptance criteria:**

1. `full` scenario seeds data into v1.0.0 schema tables: `meetings` + `meeting_prep` + `meeting_transcripts`, `entity_assessment` + `entity_quality`, `account_stakeholders`
2. All 3 accounts have full 6-dimension intelligence with health scores: Acme 78/stable/GrowthFocus, Globex 42/declining/AtRiskSaveable, Initech 55/stable/Autopilot
3. `signal_events` has 20+ rows spanning 8+ signal types. `intelligence_feedback` has 6 rows. `signal_weights` has 4 source rows.
4. `full` scenario does NOT write workspace files. `pipeline` is the only scenario that writes to `_today/data/`.
5. DevToolsPanel shows 4 scenario buttons (not 6). No "Weekly Prep" section.
6. All mock entity IDs use `mock-` prefix. `purge_mock_data()` cleans with `WHERE id LIKE 'mock-%'`.
7. After applying `full` scenario and navigating every major page, zero console errors and zero blank sections.

---

## Phase 3: Structural Cleanup + Surfaces + GA Readiness

**Goal:** God modules broken up, pipelines reliable, frontend cleaned up. Health and intelligence render across all surfaces. Search, offline mode, data export, privacy, and editorial polish bring the app to GA quality.

### 3a: Backend structural cleanup

| ID | Title | Notes |
|----|-------|-------|
| **I514** | Module decomposition | `commands.rs` → domain command files. `db.rs` → domain query modules. |
| **I515** | Pipeline reliability | Retry with backoff, PTY circuit breaker, partial result preservation, `pipeline_failures` table. Spec: `.docs/issues/i515.md`. |
| **I538** | Meeting briefing refresh rollback | Snapshot existing prep before clearing; restore if enrichment fails. Prevents blank "Building context" page on failed refresh. Spec: `.docs/issues/i538.md`. |

### 3b: GA features (search, offline, export, privacy)

| ID | Title | Notes |
|----|-------|-------|
| **I427** | Full-text search | Cmd+K via SQLite FTS5. Build on clean schema post-I511. < 300ms. Spec: `.docs/plans/v0.16.1-dissolved.md`. |
| **I428** | Offline/degraded mode | Cached intelligence when APIs unavailable. System status indicator. No blank screens. |
| **I429** | Data export | JSON ZIP of all entities, signals, intelligence. Build on clean schema post-I511. |
| **I430** | Privacy clarity | Settings section: what's stored, how long, clear intelligence + delete all data. |
| **I431** | Cost visibility | Claude call tracking, estimated weekly cost in Settings. Depends on I435 (token optimization). P2 — cut if needed. |
| **I438** | Prime DailyOS | First content ingestion step in wizard; manual (drop file) or connector. |

### 3c: Frontend surfaces + intelligence rendering

| ID | Title | Notes |
|----|-------|-------|
| **I521** | Frontend structural cleanup | Remove ghost components, fix duplicate patterns, type alignment with Phase 2 schema changes. Do BEFORE editorial polish. **No spec yet** — scope after Phase 2 types land, since I503/I508 change `EntityIntelligence` and frontend types must align to the new schema. |
| **I502** | Health surfaces | Render health band, dimensions, divergence across all pages. |
| **I493** | Account detail enriched intelligence | Glean-sourced titles, coverage gaps, reports chapter. |

### 3d: Editorial polish + vocabulary (absorbed from v0.16.2)

| ID | Title | Notes |
|----|-------|-------|
| **I447** | Design token audit | Opacity tokens, rgba violations, max-width unification. Foundation for all editorial work. |
| **I454** | Vocabulary pass | Replace all remaining user-visible system terms per ADR-0083. Independent, low risk. |
| **I448** | ActionsPage editorial rebuild | CSS module, margin grid, ChapterHeadings, correct max-width. Spec: `.docs/plans/v0.16.2-dissolved.md`. |
| **I449** | WeekPage + EmailsPage polish | CSS module, stat line tokens, EditorialLoading/EditorialError, FinisMarker. |
| **I450** | Portfolio chapter extraction | Shared CSS module for Account + Project detail. Conclusion-before-evidence editorial order. |
| **I451** | MeetingDetailPage polish | Recent Correspondence editorial treatment, avatar tint tokens, FinisMarker. P2. |
| **I452** | Settings page editorial audit | Inline styles, vocabulary compliance, section rules, FinisMarker. P2. |
| **I453** | Onboarding pages editorial standards | v0.16.0 wizard/demo/tour built to editorial spec. No inline styles. |

### 3e: Intelligence feedback + signal taxonomy

| ID | Title | Notes |
|----|-------|-------|
| **I529** | Intelligence quality feedback UI | Hover-triggered thumbs up/down on any intelligence item (reports, briefings, entity detail). Feeds Bayesian source weights. `intelligence_feedback` table. Coarse source attribution via `enrichment_sources`. Spec: `.docs/issues/i529.md`. |
| **I530** | Signal taxonomy: curation vs correction | Delete = no source penalty (`intelligence_curated`). Edit = correction (source penalized). Thumbs down = correction (via I529). Fixes `record_enrichment_correction()` to only fire on edits, not deletes. Spec: `.docs/issues/i530.md`. |
| **I537** | Gate role presets behind feature flag | Hide preset selection from onboarding (skip EntityMode chapter) and Settings (hide RoleSection). Hard-default to CS. All preset infrastructure stays — vocabulary, vitals, prompts, reports unaffected. Spec: `.docs/issues/i537.md`. |

**Acceptance criteria:**

1. `commands.rs` < 500 lines. Each domain has its own command file.
2. `db.rs` is a re-export hub. Domain queries in own modules.
3. Intelligence pipeline: if enrichment fails mid-way, partial results preserved and resumable
4. Every async pipeline stage has error boundary (no silent failures)
5. Zero ghost components. `IntelligenceQuality` defined once. Phantom fields removed.
6. Health renders as confidence band with trend arrow on relevant pages
7. Account detail shows Glean-sourced titles, coverage gaps, reports chapter
8. Cmd+K search: type an account name, result in < 300ms, click navigates to detail
9. Disconnect from internet — briefing loads with cached data, "last updated" indicator, no blank screens
10. Data export: ZIP produced with all entity types, human-readable field names, < 30s for typical workspace
11. Settings → Data explains what's stored. "Clear intelligence" and "Delete all data" work.
12. `grep -rn "rgba(" src/ --include="*.tsx" --include="*.module.css" | grep -v "var(--"` — returns 0 new violations
13. Every page ends with FinisMarker. ActionsPage uses ChapterHeadings for group headers.
14. No user-visible strings contain "entity", "intelligence", "enrichment", "signal", or "prep" (ADR-0083)
15. Hover over any intelligence item (report section, briefing topic, entity insight) — thumbs up/down buttons appear. Clicking records feedback. Button shows selected state.
16. `intelligence_feedback` table has rows after feedback. One vote per field per entity (UNIQUE constraint). Changing vote replaces previous.
17. Thumbs up on Glean-sourced item → `signal_weights` alpha += 1 for `"glean"` source. Thumbs down → beta += 1.
18. Delete an intelligence item — `signal_events` has `intelligence_curated` row. `signal_weights` unchanged (no source penalized).
19. Edit an intelligence item (change the text) — `signal_events` has `user_correction` row. `signal_weights` updated (source penalized, beta += 1).
20. Delete 10 intelligence items across 5 accounts. `signal_weights` table unchanged — system does not learn to produce less intelligence.
21. All editable fields on entity detail pages save correctly and persist on reload (no split-brain between JSON and DB).
22. Feedback buttons do not disrupt reading flow — minimal visual footprint, hover-only.

---

## CS Report Suite (after Phase 2)

| ID | Title | Blocked by |
|----|-------|------------|
| **I489** | VP Account Review report | I499, I508 |
| **I490** | Renewal Readiness report | I499, I508 |
| **I491** | Portfolio Health Summary report | I499, I489 |
| **I496** | Stakeholder Map report | I504, I505 |
| **I497** | Success Plan report | I499, I508 |
| **I498** | Coaching Patterns report | I499, I508 |

**Acceptance criteria:**

1. VP Account Review references actual stakeholders, real risks. Leadership actions are VP-level.
2. Renewal Readiness only for accounts with renewal date. Rating aligns with health.
3. Portfolio Health Summary: exceptions list only surfaces declining/at-risk accounts.
4. Stakeholder Map shows actual stakeholders with preset roles. Coverage gaps visible.
5. Success Plan objectives from real intelligence — not hallucinated.
6. Coaching Patterns reflect actual calendar data. Recommendations are specific.
7. All reports export as PDF.

---

## Issues Absorbed

| Original | Absorbed Into | Rationale |
|----------|--------------|-----------|
| I380 | I512 (ServiceLayer) | ServiceLayer is more ambitious than service extraction |
| I381 | I511 (Schema decomposition) | Schema decomposition includes query migration |
| I402 | I512 (ServiceLayer) | All services through ServiceLayer |
| I436 | I513 (Workspace file elimination) | Complete elimination |
| v0.16.1 | v1.0.0 Phase 2-3 | Dissolved 2026-03-03. I427, I428, I429, I430, I431, I435, I438 absorbed. |
| v0.16.2 | v1.0.0 Phase 3d | Dissolved 2026-03-03. I447-I454 absorbed. I483 (theming) deferred post-1.0. |

---

## Explicitly Out of Scope

- **Server-canonical architecture** — ADR-0099 withdrawn. DailyOS stays local-first.
- **Sync engine / full data sync** — not needed. Publication of curated outputs in v1.1.0.
- **Auth / org model / RLS** — governance via Google Workspace + Glean, not in-app auth.
- **Portfolio Health page** (I492) — needs published intelligence from multiple ICs. v1.1.0.
- **Glean account discovery** (I494) — v1.1.0.
- **Local AI providers** (I432-I434) — v2.1.0.
- **Theming** (I483) — token layering, Dark/Cool themes, custom theme loader. Post-1.0. Warm theme is the GA aesthetic.

---

## Pre-1.0.0 Versions

| Version | Status | Notes |
|---------|--------|-------|
| v0.15.0 | In progress | Reports infrastructure + personal impact reports |
| v0.15.1 | Planned | Security hardening |
| v0.15.2 | Shipped | Audit log + Glean context |
| v0.16.0 | Mostly done | Onboarding + first-run. Only pre-1.0 version remaining. |
| ~~v0.16.1~~ | Dissolved | GA features (I427-I431, I435, I438) absorbed into v1.0.0 Phase 3. I527 (consistency guardrails) remains in v0.16.1 as pre-GA hardening. |
| ~~v0.16.2~~ | Dissolved | All issues absorbed into v1.0.0 (except I483 → post-1.0) |

---

## Dependency Graph

```
Phase 1 (sequential):
  I511 (schema + migration safety) → I539 (DB recovery UX) → I512 (ServiceLayer) → I513 (workspace elimination)

Phase 2 (after Phase 1, 5 waves):
  Wave 1 (parallel, no inter-dependencies):
    I503 (health schema — types) ← I511
    I504 (AI relationships) — parallel
    I506 (co-attendance) — parallel
    I528 (ADR-0098 data lifecycle infra — prerequisite for I487, I505)
  Wave 2 (after Wave 1):
    I508a (intelligence schema — sub-struct types) ← I503
  Wave 3 (after Wave 2, parallel):
    I508b (enrichment prompt update) ← I508a
    I499 (health scoring engine) ← I503
    I500 (Glean org-score parsing) ← I503
    I487 (Glean signals) ← I528
    I509 (transcript interpretation) ← I508a, I503
  Wave 4 (after I528, gated on Glean validation):
    I505 (Glean stakeholders) ← I528
  Wave 5 (after Wave 3):
    I507 (feedback — scoped to person + email only) ← I487, I504, I505
  I435 (token optimization) — parallel with any wave

Phase 2a (after Phase 2):
  I536 (mock data migration) ← I511, I508, I499, I503, I512

Phase 3a (after Phase 1):
  I515 (pipeline reliability) ← I512 (ServiceLayer)
  I514 (module decomp) ← I512 (spec written AFTER I512 ships, scoped against actual post-I512 state)

Phase 3b (after I511, independent of Phase 2):
  I427 (search) ← I511 (needs clean schema for FTS5)
  I428 (offline) — independent
  I429 (data export) ← I511 (needs clean schema for export queries)
  I430 (privacy) — independent
  I431 (cost visibility) ← I435 (token optimization)
  I438 (prime DailyOS) — independent

Phase 3c (after Phase 2):
  I521 (frontend cleanup) — do first, before editorial polish
  I502 (health surfaces) ← I499, I503
  I493 (account detail) ← I505, I502

Phase 3d (after I521):
  I447 (tokens) — foundation, do first
  I454 (vocabulary) — independent, parallel with I447
  I448-I453 (editorial polish) ← I447

Phase 3e (after Phase 2 + I513):
  I529 (feedback UI) ← I507, I513
  I530 (signal taxonomy) ← I529

Reports (after Phase 2):
  I489, I490, I497, I498 ← I499, I508
  I496 ← I504, I505
  I491 ← I499, I489
```

---

## End-to-End Verification (before tagging)

1. **Intelligence enrichment.** Enrich an account. `entity_assessment` has 6 dimensions. `entity_quality` has health_score. Delete `intelligence.json` from entity dir — app still shows all intelligence data.
2. **Health rendering.** Account detail shows health band + trend arrow + confidence. Person detail shows inferred relationships.
3. **Reports.** Generate VP Account Review. References real stakeholders, real risks. Export as PDF.
4. **Module size.** `wc -l src-tauri/src/commands.rs` < 500. `wc -l src-tauri/src/db.rs` < 200.
5. **Pipeline resilience.** Kill enrichment mid-way. Restart. Enrichment resumes (saga pattern).
6. **DB as sole read path.** Delete `_today/data/schedule.json` — briefing still renders. Delete `intelligence.json` from entity dir — account detail still renders. User-dropped `.md` file in entity dir — still processed and indexed. Workspace dirs intact, user files untouched.
7. **Offline.** Disconnect. Briefing loads from cached data. No blank screens. System status shows connectivity.
8. **Search.** Cmd+K. Type account name. Result in < 300ms. Click navigates to detail.
9. **Data export.** Settings → Data → Export. ZIP produced, all entity types, human-readable fields.
10. **Privacy.** Settings → Data → "Clear intelligence." `entity_assessment` rows cleared. Entity names preserved.
11. **Editorial.** Every page ends with FinisMarker. ActionsPage uses ChapterHeadings. No rgba() violations. No ADR-0083 vocabulary violations.
12. **Feedback loop.** Hover over a risk item on a report — thumbs up/down appear. Thumbs-down it. `signal_weights` penalizes the source. Delete a different risk item. `signal_weights` unchanged. Edit a third risk item's text. `signal_weights` penalizes the source. Three actions, two penalties, one curation — system learns from corrections without punishing editorial decisions.
13. **Inline editing.** Edit a field on entity detail page. Navigate away. Return. Edit persisted. No split-brain.
14. `cargo test` — all pass.
15. `cargo clippy -- -D warnings` — clean.
16. `pnpm tsc --noEmit` — clean.

---

## What Ships After v1.0.0

| Version | Theme |
|---------|-------|
| **v1.1.0** | Publication + Portfolio + Glean Agents + Intelligence Quality — Google Drive output layer (I533), cross-IC portfolio reader (I534), Glean Agent REST API integration (I535), portfolio page (I492), Glean discovery (I494-I495), proactive self-healing via Glean (I531), intelligence surfacing thresholds (I532). 8 issues, all specced. |
| **v1.x** | Theming (I483) — Dark/Cool themes, typography controls, custom theme loader |
| **v2.1.0** | Local-First AI — IntelligenceProvider, Ollama, OpenAI (I432-I434) |
| **v2.2.0** | Document Intelligence (I456) |
