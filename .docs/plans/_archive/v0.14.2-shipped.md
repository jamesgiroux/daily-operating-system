# v0.14.2 Version Brief

**Thesis:** Both systems that were supposed to make DailyOS feel personal are 30–50% realised. Personality affects only empty-state strings — not a single line of AI-generated content changes tone based on this setting. Five role preset fields are defined, typed, and validated in all nine presets but consumed by nothing: `stakeholder_roles`, `internal_team_roles`, `lifecycle_events`, `prioritization`, and the meeting prep prompt that hardcodes "Customer Success Manager" regardless of who's using the app. A Sales person gets prep written for a CSM. A Product Manager's account sort ignores the signals they actually care about. This version finishes what was started: personality shapes the tone of every AI output, role presets drive the UI elements they were always intended to drive, and the user entity page (v0.14.1) gets its preset-specific vocabulary, prominence rules, and playbook sections.

**Status:** Planning.

---

## Scope

### In

| ID | Title | Area |
|----|-------|------|
| I439 | Personality expanded in UI — completion messages, generating states, toasts, error copy; more touchpoints where tone creates delight or calm; never touches AI prompts | P1 | Frontend / UX |
| I440 | Meeting prep preset persona — remove hardcoded "Customer Success Manager"; use active preset's role name and vocabulary in all prep prompts | Backend / Intelligence |
| I441 | Personality coverage + useActivePreset cache — fill missing empty state keys; shared cache with reactivity; all 9 presets verified functional | Frontend |
| I442 | stakeholder_roles wired — relationship type dropdowns on person-to-account linking pull from active preset's stakeholder_roles | Backend / Frontend |
| I443 | internal_team_roles wired — account team role selectors pull from active preset's internal_team_roles | Backend / Frontend |
| I444 | lifecycle_events wired — lifecycle stage pickers pull from active preset; lifecycle stage context injected into AI prompts | Backend / Frontend |
| I445 | prioritization wired — account list sort order and weekly forecast risk ranking use preset's primary_signal and urgency_drivers | Backend / Frontend |
| I446 | User entity page × role preset — section prominence rules, preset-specific vocabulary/placeholders, named playbook sections per preset; all 9 presets fully implemented on the /me page | Frontend / Entity |
| I455 | 1:1 meeting prep focuses on person entity intelligence, not account — relationship history, recent topics, open actions with the person | Backend / Intelligence |
| I457 | Background task throttling — ActivityMonitor, HeavyWorkSemaphore, adaptive polling. 83% reduction in idle wakeups during active use, heavy ops serialized | Backend / Performance |

### Explicitly out

- New preset fields (adding fields to the schema).
- New presets beyond the 9 that exist.
- Logic changes to AI enrichment beyond the tone and persona fixes.
- Frontend redesign beyond what is required to surface the newly-wired preset fields.

---

## Acceptance Criteria

Each criterion must be verified with real data in the running app after a full `pnpm dev` restart. Compilation is not acceptance.

---

### I439 — Personality expanded in UI

Personality shapes how the app *speaks* to you — not the intelligence it produces. Intelligence is framed by role presets. These are orthogonal. Full criteria in `i439.md`.

1. **Completion messages** wired: `getPersonalityCopy` gains `"action-completed"`, `"action-dismissed"`, `"action-archived"`. Professional: "Done." Friendly: "Got it — taken care of." Playful: "Crushed it. ✓"
2. **Generating states** wired: `"generating-briefing"`, `"building-context"`, `"processing-transcript"`. Professional: "Building context…" Playful: "Cooking something up…"
3. **Toast notifications** wired: `"saved"`, `"connected"`, `"setup-complete"`. Tone-appropriate per personality.
4. **Error copy** wired: `"sync-error"`, `"connection-error"`, `"enrichment-failed"`. Same tone principle — personality makes errors less jarring or more matter-of-fact depending on setting.
5. Three previously-defined-but-never-called empty state keys wired: `"accounts-empty"`, `"projects-empty"`, `"actions-waiting-empty"`.
6. **Personality does NOT touch any AI prompt.** `grep -rn "build_tone\|tone_instruction\|personality" src-tauri/src/ --include="*.rs"` — returns only config storage/validation. Never prompt construction.

---

### I440 — Meeting prep preset persona

1. The "Customer Success Manager" hardcoded string in `workflow/deliver.rs` is removed. In its place: `config.active_preset.name` (or equivalent) is read and used as the role description in the meeting prep persona. Verify: `grep -rn "Customer Success Manager" src-tauri/src/` — returns 0 results.
2. With the Sales preset active, open a meeting detail page for a customer meeting. The prep's framing language reflects a sales context — deal stage, close probability, competitive context — not renewal, health score, or QBR language.
3. With the Leadership preset active, meeting prep for a strategic account meeting uses leadership vocabulary — strategic priorities, board-level context, executive alignment — not CSM operational framing.
4. The preset's `briefing_emphasis` field is already injected in some prompts. Confirm it is also injected into the meeting prep prompt after this change. Verify: `grep -n "briefing_emphasis\|briefingEmphasis" src-tauri/src/workflow/deliver.rs` — appears in the meeting prep prompt section.
5. `cargo test` passes. No regressions on existing meeting prep for CS preset users.

---

### I441 — Personality coverage + useActivePreset cache

1. The three defined-but-never-called empty state keys are wired to components: `accounts-empty` → AccountsPage empty state (when no accounts exist), `projects-empty` → ProjectsPage empty state, `actions-waiting-empty` → ActionsPage waiting-on tab empty state. Verify by clearing the relevant data and navigating — personality-differentiated copy renders.
2. `useActivePreset` is refactored to a shared React context (similar to `PersonalityProvider`). The context is initialized once at app root, not three times per entity detail page navigation. Verify: `grep -rn "useActivePreset\|get_active_preset" src/ --include="*.tsx"` — no direct `invoke("get_active_preset")` calls remain in page components; all consumption goes through the context.
3. The active preset context is reactive: switching presets in Settings updates the preset context within 1 second without requiring navigation. Verify: switch from Customer Success to Sales in Settings; the AccountDetailEditorial.tsx vitals panel updates to show Sales-appropriate fields without navigating away and back.
4. All 9 presets render without error when activated. Verify: activate each of the 9 presets in turn in the running app — no console errors, vitals panel renders the correct fields for each.

---

### I442 — stakeholder_roles wired

1. When linking a person to an account (the entity linker component), the relationship type dropdown shows options from the active preset's `stakeholder_roles` array rather than a hardcoded list. Verify: with Customer Success preset, the options include "Champion," "Economic Buyer," "Technical Evaluator" etc. With Sales preset, the options reflect sales relationship vocabulary.
2. Previously linked relationships that used hardcoded role labels are not broken — their existing `relationship_type` string is displayed as-is even if it does not match the current preset's vocabulary.
3. `SELECT DISTINCT relationship_type FROM entity_people` — existing relationship types are preserved in the DB. New links use the preset-sourced labels.

---

### I443 — internal_team_roles wired

1. When adding a team member to an account (the account team management UI), the role dropdown shows options from the active preset's `internal_team_roles`. With Customer Success preset: "CSM," "TAM," "Account Executive" etc. With Partnerships preset: "Partner Manager," "Solutions Engineer" etc.
2. Existing team member role labels are preserved — not retroactively changed when preset switches.

---

### I444 — lifecycle_events wired

1. The lifecycle stage picker on account detail pages shows stages from the active preset's `lifecycle_events` array. With Customer Success preset: "Renewal," "Expansion," "Contraction," "Churn," "Escalation," "Executive Review" etc. With Product preset: "Discovery," "Beta," "Launched," "Deprecated" etc.
2. The current lifecycle stage is stored as a string in the account's metadata. Switching presets does not erase or invalidate existing lifecycle stages — they display as-is even if the new preset does not define that stage.
3. The lifecycle stage context is injected into entity intelligence prompts alongside vocabulary and briefing emphasis. Verify: `grep -n "lifecycle" src-tauri/src/intelligence/prompts.rs` — the lifecycle stage from account metadata appears in the assembled prompt when a stage is set.

---

### I445 — prioritization wired

1. The AccountsPage default sort order is informed by the active preset's `prioritization.primary_signal`. With Customer Success preset (`primary_signal: "renewal_proximity"`): accounts sorted by nearest renewal date first within each health tier. With Sales preset (`primary_signal: "deal_stage"`): accounts sorted by deal stage progression. With Leadership preset: sorted by ARR descending.
2. The sort is a soft suggestion, not a hard override — user-selected sorts (alphabetical, recently viewed) take precedence. The preset-driven sort is the "default" that applies when no explicit sort is selected.
3. The weekly forecast's risk section uses `prioritization.urgency_drivers` to rank attention items. If `urgency_drivers` includes "renewal_at_risk" and "no_recent_meeting," accounts matching these conditions surface higher in the attention section.
4. `cargo test` passes. No regressions on existing sort behavior.

---

### I446 — User entity page × role preset

1. On the `/me` page, the section prominence (expanded vs collapsed by default) matches the preset-specific rules from ADR-0090. With Customer Success: "What I Deliver" and its sub-sections (Value Proposition, Success Definition) are expanded; "Pricing Model" is shown; three named playbook sections appear ("At-Risk Accounts," "Renewal Approach," "EBR/QBR Preparation"). With Sales: Pricing Model is primary; competitive landscape is expanded; named playbooks are "Deal Review," "Territory Planning," "Competitive Response."
2. Placeholder text in each user entity field is role-specific. The Value Proposition placeholder for CS says "What does your platform do for customers? Write it as a one-sentence outcome, not a feature list." For Sales: "What do you sell and what makes it win against the competition?" These are not generic.
3. All 9 presets have complete prominence rules and placeholder text. Activate each preset; navigate to `/me` — each shows a meaningfully different layout emphasis and placeholder copy.
4. Switching presets updates the `/me` page within 1 second (relies on I441's reactive preset context). No page reload required.
5. The "My Playbooks" section shows 3 preset-specific named sections (not the generic "My Methodology" fallback) for all 9 presets. Each section has a role-specific placeholder.

---

### I457 — Background task throttling

1. `get_background_status` returns correct `activityLevel` (Active/Idle/Background) based on window focus and interaction recency.
2. PTY and embedding inference never run simultaneously — `heavy_work_semaphore` serializes them. Manual enrichment (`enrich_account`, `enrich_person`) bypasses the semaphore.
3. Intel/prep/embedding queue processors poll every 30s when user is Active and queues empty (was 5s). Full speed when app is in Background. Instant wake on enqueue via Notify.
4. Calendar poller: 120s Active, 60s Idle, 30s Background (was fixed 30s). Email poller: same adaptive intervals.
5. `cargo test` passes — all 1004 tests, no regressions.

---

## Issue Dependencies

```
I441 (useActivePreset cache + personality coverage) — foundational for I446
  └── needed by I446 (reactive context)

I440 (meeting prep persona) — self-contained backend fix, no blockers

I439 (personality UI expansion) — backend, no blockers
  └── builds on config access pattern established in I440

I442 (stakeholder_roles) — no blockers, small blast radius
I443 (internal_team_roles) — no blockers, can parallel with I442

I444 (lifecycle_events)
  └── UI part unblocked; I441 cache refactor benefits from being done first but not a hard dependency

I445 (prioritization/sort)
  └── no blockers; test carefully against existing sort behavior

I446 (user entity page × preset)
  └── addBlockedBy I441 (reactive context must exist)

I455 (1:1 meeting prep person focus)
  └── benefits from I440 (preset persona) but not blocked by it
```

**Recommended build order:**
1. I441 (useActivePreset cache + personality coverage) — foundation; reactive context needed by I446
2. I440 (meeting prep persona) — high-value, self-contained backend fix
3. I439 (personality UI expansion) — frontend, no backend changes; pure copy expansion
4. I442 + I443 (stakeholder/team roles) — can be parallelized, small blast radius
5. I444 (lifecycle events) — slightly more complex; touches prompt building
6. I445 (prioritization/sort) — touches account list and forecast, test carefully
7. I455 (1:1 meeting prep person focus) — self-contained backend; can parallel with I445/I446
8. I446 (user entity page × preset) — builds on I441's reactive context; do last

---

## Design References

- ADR-0079: Role Presets Replace Kits and Configurable Metadata
- ADR-0083: Product Vocabulary — User-Facing Language Standard
- ADR-0090: User Entity Page Architecture (Decision 5 and Decision 6 — prominence rules)
- `.docs/design/COMPONENT-INVENTORY.md` — existing components to use (dropdowns, pickers, EditableText)
- `.docs/design/PAGE-ARCHITECTURE.md` — editorial section patterns for `/me` preset variations

---

## Ship criteria

`grep -rn "Customer Success Manager" src-tauri/src/` returns 0. Personality = "playful" produces observably different briefing tone than personality = "professional" on the same account. All 9 presets activate cleanly with no console errors. Relationship type dropdowns show preset-specific roles. Account list default sort reflects the active preset's primary signal.

### End-to-end smoke test (mandatory before tagging)

1. Switch to Sales preset. Open a meeting detail page for a customer meeting. Verify: prep framing uses sales vocabulary (no "renewal," "health score," "QBR" unless the user has set them).
2. Set personality to "playful". Trigger entity re-enrichment for a real account. Read the new `executive_assessment` — contractions and casual language visible.
3. On AccountsPage with CS preset active: default sort shows nearest-renewal accounts at top. Switch to Sales preset — sort updates to deal-stage order.
4. Link a person to an account — relationship type dropdown shows CS role options (Champion, Economic Buyer, etc). Switch to Sales preset — dropdown shows Sales-appropriate roles.
5. Navigate to `/me` with CS preset: three named playbook sections visible. Switch to Sales preset — three different named sections appear.
6. All 9 presets activated in sequence — no errors, each shows distinct vitals and playbook sections.
