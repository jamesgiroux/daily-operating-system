# v0.16.1 Version Brief

**Thesis:** Before GA, the app needs to be trustworthy, findable, and honest. Trustworthy: I280 (beta hardening — dependency audit, DB integrity, security review). Findable: I427 (full-text search — Cmd+K finds any entity, meeting, or action). Graceful: I428 (offline mode — the app serves cached intelligence when APIs are unavailable). Honest: I429 (data export — users can always get their data out), I430 (privacy clarity — users understand what's stored), I431 (cost visibility — users see what AI calls cost), I435 (token optimization — the app uses the right model tier for the right task).

**Status:** Planning.

---

## Scope

### In

| ID | Title | Priority | Area |
|----|-------|----------|------|
| I280 | Beta hardening — dependency audit, DB integrity, input validation, security review | P1 | Code Quality |
| I427 | Full-text search — Cmd+K finds entities, meetings, actions, contacts across all data | P1 | Frontend + Backend |
| I428 | Offline/degraded mode — serves cached intelligence gracefully when APIs unavailable | P1 | Backend |
| I429 | Data export — JSON export of entities, signals, intelligence; portability guarantee | P1 | Backend |
| I430 | Privacy clarity — Settings section explaining what's stored, how long, purge option | P1 | Frontend |
| I431 | Cost visibility — Claude call tracking, estimated cost breakdown in Settings | P2 | Backend + Frontend |
| I435 | Token optimization — audit ModelTier usage; Haiku for emails/extraction; quality-gated enrichment | P1 | Backend |

### Explicitly out

- Performance profiling beyond cold launch and hot reload targets — save for post-1.0 instrumentation
- Automated security scanning in CI — manual audit now; CI integration deferred to v0.17.0
- Multi-account Google Workspace support — scoped to 1.1.0
- Granular per-entity data deletion — full purge and intelligence clear cover the use case; per-entity delete is a future affordance
- Real-time cost tracking via Anthropic API usage endpoint — the API does not expose per-call costs; local approximation is the correct approach for 1.0

---

## Acceptance Criteria

Each criterion must be verified with real data in the running app after a full `pnpm dev` restart. Compilation is not acceptance.

---

### I280 — Beta hardening

1. All dependencies audited: `cargo audit` returns 0 critical or high vulnerabilities. `pnpm audit` returns 0 high/critical vulnerabilities.
2. All Tauri IPC commands validate input at boundaries. `grep -n "unwrap()\|expect(" src-tauri/src/commands.rs` — every instance is justified (not hiding an error path). No path traversal vulnerabilities in file operations.
3. DB integrity: all FK constraints enforced. `PRAGMA foreign_key_check` returns 0 violations. All migrations are idempotent.
4. No plaintext secrets in the codebase or DB. Google OAuth tokens stored in keychain only. `grep -rn "token\|secret\|password" src-tauri/src/ --include="*.rs" | grep -v "keychain\|test\|comment"` — reviewed and clean.
5. `cargo clippy --workspace --all-features --lib --bins -- -D warnings` passes clean.
6. App cold launch under 3 seconds. `pnpm dev` hot reload under 500ms for frontend changes.

---

### I427 — Full-text search

1. Cmd+K opens a search palette from any screen. The palette is a modal overlay that does not navigate away from the current page.
2. Typing searches across: account names, project names, person names, meeting titles, action titles, email subjects (from the emails table). Results appear within 300ms of keystroke.
3. Each result shows: entity type icon, name, and a one-line context (account: health indicator; person: title + company; meeting: date + entity link; action: status). Clicking a result navigates to that entity's detail page and closes the palette.
4. Results are ranked by recency and relevance: exact name matches first, then partial matches, then fuzzy matches. Recent meetings (last 30 days) rank above historical.
5. The search backend uses SQLite FTS5 (full-text search) over the key text columns. `SELECT * FROM sqlite_master WHERE type='table' AND name LIKE '%fts%'` — returns at least one FTS virtual table.
6. Cmd+K works offline (searches local DB only — no API calls required).

---

### I428 — Offline/degraded mode

1. When Google Calendar API returns a network error or 5xx, the app shows the last-cached calendar data with a "Last updated X minutes ago" indicator in the folio bar. The daily briefing does not show an error state — it shows cached intelligence with the staleness indicator.
2. When the Claude Code subprocess is unavailable (not installed, network issue, rate limit), the app continues to serve all cached intelligence from the DB. The intel_queue logs failures and retries with backoff. No surfaces show empty states due to PTY unavailability — they show cached content.
3. When Google Gmail API is unavailable, the email page shows the last-fetched emails from the `emails` DB table with a "Mail sync paused" notice. Existing emails remain usable.
4. The system status indicator (Settings → System) shows live connectivity status for: Google Calendar, Gmail, Clay, Gravatar, Linear, and Claude Code. Green/amber/red with last-successful-sync timestamp for each.
5. App startup works correctly with no internet connection. All cached intelligence, meetings, and actions load from DB. No API calls are attempted on startup until the user explicitly triggers them.

---

### I429 — Data export

1. Settings → Data → "Export all data" produces a ZIP file containing:
   - `accounts.json` — all accounts with their entity intel
   - `people.json` — all people with enrichment data
   - `projects.json` — all projects
   - `meetings.json` — all meetings_history with their prep content
   - `actions.json` — all actions
   - `signals.json` — signal_events (last 90 days)
   - `intelligence.json` — entity_intel for all entities
2. The export is completeable offline (reads from local DB only). Export of a typical workspace (50 accounts, 200 people, 500 meetings) completes in under 30 seconds.
3. Exported JSON is human-readable and self-describing — field names match what the user would recognize from the UI (account name, not entity_id).
4. A "What's in your export" description in the UI explains each file before the user exports. No surprises about what's included.
5. The export does NOT include: Google OAuth tokens, keychain credentials, or any credentials. Only user data and intelligence.

---

### I430 — Privacy clarity

1. Settings → Data contains a "What DailyOS stores" section listing: entity data (accounts, people, projects), meeting history (titles, attendees, prep), email metadata (subject, sender — never body), signal events, AI-generated intelligence summaries, embedding vectors. Each item has a one-sentence plain-language description.
2. A "Data retention" section explains: data is stored locally in `~/.dailyos/dailyos.db`. Nothing is sent to DailyOS servers. Google OAuth tokens are stored in the macOS keychain. AI enrichment calls are made to Claude's API (Anthropic) — meeting context is sent but not retained by default per Anthropic's API policies.
3. A "Clear intelligence" button deletes all AI-generated content (`entity_intel` table, `signal_events` table, `emails.contextual_summary` column) while retaining entity structure (accounts, people, projects still exist). This forces fresh enrichment on next use. `SELECT count(*) FROM entity_intel` returns 0 after clearing.
4. A "Delete all data" button clears the entire DB (`~/.dailyos/dailyos.db`) and workspace files. Requires typing "DELETE" to confirm. App returns to first-run state. This is irreversible — a clear warning is shown.

---

### I431 — Cost visibility

1. Settings → System → "AI usage" shows: total Claude API calls this week, estimated cost this week (based on approximate token counts and public Claude API pricing), breakdown by type (entity enrichment, email enrichment, report generation, meeting prep).
2. The tracking is local — the app counts PTY calls via the existing `enrichment_log` table, adds estimated token counts (based on prompt size and output size), and multiplies by the current Anthropic pricing for the active model tier.
3. A weekly usage chart (last 4 weeks) shows trend. If usage is significantly higher than the prior week, a note explains why (e.g., "New connectors configured — Clay enrichment added 34 calls this week").
4. A "Daily AI budget" setting (optional, default: unlimited) can cap the number of AI enrichment calls per day. When the budget is reached, the intel_queue pauses until midnight reset. The budget does not affect user-initiated actions (report generation, manual refresh).

---

### I435 — Token optimization

1. A written audit exists at `.docs/research/i435-token-audit.md` listing every PTY call site (from the I376 enrichment audit), its current model tier assignment, its actual prompt size (measured in production), and a recommendation (keep tier / downgrade to Haiku / batch with other calls).
2. Email enrichment uses Haiku tier, not Sonnet. Every call to `enrich_email` in `prepare/email_enrich.rs` uses `ModelTier::Extraction`. Verify: `grep -n "ModelTier" src-tauri/src/prepare/email_enrich.rs` — returns Extraction, not Synthesis.
3. Entity enrichment only fires when the trigger score (I408) exceeds threshold OR when the entity has never been enriched. The hardcoded "14-day staleness" path (now replaced by I408's trigger function) does not cause enrichments for entities with quality score > 0.75 and no new signals. Verify: an entity with `quality_score = 0.90` and no new signals since last enrichment does NOT appear in the intel_queue after a hygiene scan.
4. Report generation uses appropriate tiers: EBR/QBR and Risk Report use Synthesis. SWOT and Account Health Review use Extraction. Verify: `grep -n "ModelTier" src-tauri/src/` for each report type.
5. Measured token reduction: after implementing the audit recommendations, the average daily PTY call count on a typical workday (5 meetings, 20 emails, 50 entities) is ≤ 40 calls. Before optimization (baseline from enrichment_log), record the count. After: verify reduction.

---

## Issue Dependencies

```
I280 — independent; gate for everything else

I435 (token optimization)
  └── addBlockedBy I280 (run audit on clean, hardened codebase)
  └── requires I408 trigger scoring to be active (v0.13.7 shipped)

I427 (full-text search)
  └── independent of I280/I435; can build in parallel
  └── search palette is a new frontend surface — no backend dependencies other than FTS5 migration

I428 (offline/degraded mode)
  └── independent; reads from existing DB structures
  └── system status indicator in Settings requires no new backend tables

I429 (data export)
  └── independent; reads from existing DB tables
  └── can build in parallel with I428 and I430

I430 (privacy clarity)
  └── "Clear intelligence" depends on knowing which tables constitute AI-generated content (I429 export inventory clarifies this)
  └── "Delete all data" is independent

I431 (cost visibility)
  └── addBlockedBy enrichment_log table existing (confirmed v0.13.2)
  └── no dependency on I428/I429/I430
```

**Recommended build order:**
1. I280 (beta hardening) — gate; run `cargo audit` and `pnpm audit` first
2. I435 (token optimization) — optimize before adding more surface area; audit first, then implement
3. I427 (full-text search) — independent, high user-facing value, parallelizable with I435
4. I428 (offline/degraded mode) — parallelizable with I429/I430/I431
5. I429 (data export) — parallelizable
6. I430 (privacy clarity) — builds on export inventory from I429
7. I431 (cost visibility) — P2; deferred if I280–I430 slip

---

## Ship criteria

`cargo audit` clean. Cmd+K finds entities instantly. App works offline with cached data. Data export produces a complete ZIP. Settings → Data explains what's stored. AI usage dashboard shows weekly call count. Token optimization documented and measured.

### End-to-end smoke test (mandatory before tagging)

1. Disconnect from internet. Open app. Daily briefing loads with cached data and "last updated" indicator. Accounts load. Cmd+K works.

2. Cmd+K: type an account name. Result appears in < 300ms. Click it — navigates to account detail.

3. Settings → Data → "Export all data." ZIP produced with all expected files. Open `accounts.json` — contains real account data with human-readable field names.

4. Settings → Data → "Clear intelligence." `SELECT count(*) FROM entity_intel` = 0. Entity names still exist in accounts table. App shows "No intelligence yet" on account detail pages.

5. Settings → System → "AI usage." Shows call count and estimated cost for current week. Chart renders for last 4 weeks.

6. `cargo audit` — 0 critical/high. `pnpm audit` — 0 high/critical.

7. `cargo clippy --workspace --all-features --lib --bins -- -D warnings` — passes clean.

8. `grep -n "ModelTier" src-tauri/src/prepare/email_enrich.rs` — returns Extraction, not Synthesis.
