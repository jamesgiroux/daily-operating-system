# v1.0.0 GA Review Fixes — Implementation Plan

**Source:** Senior engineering review (2026-03-16)
**Branch model:** Short-lived branches from `dev`, same as Phase 6
**Estimated total:** 2-3 days

---

## Dependency Graph

```
                    ┌─────────────────────────────────┐
                    │  No dependencies between items   │
                    │  — all 5 can run in parallel —   │
                    └─────────────────────────────────┘

  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
  │  I615    │  │  I616    │  │  I617    │  │  I618    │  │  I619    │
  │ Migration│  │ Task     │  │ Toast    │  │ Service  │  │ Prompt   │
  │ Integrity│  │ Restart  │  │ Errors   │  │ Tests    │  │ Evals    │
  │ P0 ~2h   │  │ P0 ~2h   │  │ P1 ~2h   │  │ P1 ~6h   │  │ P1 ~8h   │
  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘
       │              │              │              │              │
       └──────────────┴──────────────┴──────┬───────┴──────────────┘
                                            │
                                    ┌───────▼───────┐
                                    │  cargo test   │
                                    │  cargo clippy │
                                    │  pnpm tsc     │
                                    └───────────────┘
```

## Recommended Execution Order

All 5 are independent — no cross-dependencies. But ordering by risk reduction:

### Wave 1: P0 Blockers (parallel, ~2 hours each)

**I615 — Migration integrity + 068 rebuild**
- Files: `migrations.rs`, `migrations/068_success_plans.sql`
- Steps:
  1. Add `version >= 60` check for `dimensions_json` in `verify_required_schema()`
  2. Add `version >= 68` check for `success_plan_signals_json`
  3. Rewrite 068 SQL: remove `CREATE TABLE IF NOT EXISTS`, use 058 DROP+CREATE pattern
  4. Run `cargo test` — verify the 8 previously-failing tests now pass
- Verify: `cargo test -- intelligence::io` specifically

**I616 — Task restart wrapper**
- Files: new `task_supervisor.rs`, `lib.rs`
- Steps:
  1. Create `src-tauri/src/task_supervisor.rs` (~40 lines)
  2. Add `mod task_supervisor;` to `lib.rs`
  3. Replace 12 `tauri::async_runtime::spawn` calls with `spawn_supervised()`
  4. Each call needs closure that clones state + handle
- Verify: `cargo test` + manual test (panic injection optional)

### Wave 2: P1 Quality (parallel, after Wave 1 green)

**I617 — Toast errors (frontend)**
- Files: 10 TSX files, ~20 insertions
- Steps:
  1. Add `toast.error()` to each of the 20 identified locations
  2. Ensure `import { toast } from "sonner"` exists in each file
  3. Keep `console.error` for debugging, add toast for user visibility
- Verify: `pnpm tsc --noEmit`

**I618 — Service smoke tests (backend)**
- Files: 5 service modules, add `#[cfg(test)] mod tests` blocks
- Steps:
  1. Start with `mutations.rs` (most critical — signal emission)
  2. Then `intelligence.rs` (dismiss vs edit taxonomy is tricky)
  3. Then `accounts.rs`, `success_plans.rs`, `reports.rs`
  4. Each test: `test_db()` → call function → assert DB + signals
- Verify: `cargo test -- services`

**I619 — Prompt eval suite (backend)**
- Files: new `intelligence/fixtures/` dir, 3 test modules
- Steps:
  1. Create golden fixtures (sanitize from real outputs or craft manually)
  2. Write prompt construction tests (8 tests)
  3. Write response parsing tests (8 tests)
  4. Write transcript extraction quality tests (6 tests)
  5. Write dimension merge tests (4 tests)
- Verify: `cargo test -- eval_tests`

## Parallelization Strategy

```
Day 1:
  Morning  → I615 (migration fix, 2h) + I616 (task restart, 2h)
  Afternoon → I617 (toast errors, 2h) + I618 starts (service tests)

Day 2:
  Morning  → I618 continues (service tests, 6h total)
  Afternoon → I619 starts (eval suite, fixtures + prompt tests)

Day 3:
  Morning  → I619 continues (transcript + dimension tests)
  Afternoon → Final verification: cargo test, cargo clippy, pnpm tsc
```

With agent parallelization, Waves 1 + 2 can overlap significantly.

## Verification Checklist (before tagging)

```
[ ] cargo test — all pass (1,189 + ~51 new = ~1,240)
[ ] cargo clippy -- -D warnings — clean
[ ] pnpm tsc --noEmit — clean
[ ] 8 previously-failing intelligence::io tests now pass (I615)
[ ] Simulate task panic → verify restart log entry (I616)
[ ] Toggle a connector with network error → see toast (I617)
[ ] signal_events has correct types after mutation tests (I618)
[ ] Golden transcript → wins have sub-types, risks have urgency (I619)
```
