**START** by reading `.docs/plans/wave-W4/_prompts/_common.md` from the repo cwd. That contains your role, constraints, deliverable, workflow, and template. Read it first.

Then, your specific assignment:

## Agent slot: W4-B — DOS-216

- **Title:** Evaluation Harness Scaffolding
- **Linear:** https://linear.app/a8c/issue/DOS-216 (fetch via linear MCP)
- **Output:** `.docs/plans/wave-W4/DOS-216-plan.md`
- **Wave plan section:** `.docs/plans/v1.4.0-waves.md` §"Agent W4-B — DOS-216 Evaluation Harness Scaffolding"
- **Reviewers (3 stacked):** qa-expert + codex-challenge + codex-consult

## Things to be aware of

- Harness uses the W2-B `IntelligenceProvider` replay path — read `.docs/plans/wave-W2/DOS-259-plan.md` for the `Replay` execution mode contract. The harness must work with `mode = Replay`.
- W3-B (Provenance envelope) shapes fixture format — each fixture captures the full `SourceAttribution` envelope, not raw text. Read `.docs/plans/wave-W3/DOS-211-plan.md`.
- Fixture governance: anonymization (no real customer data), retention policy, purge schedule. Fixtures are checked in under `src-tauri/tests/abilities/{ability}/fixtures/fixture_N/`. The anonymization rule is load-bearing — real customer data must NEVER appear in fixtures.
- CI integration: harness runner must be hermetic (no network, no live DB). `cargo test --test harness` style invocation.
- Prompt-fingerprint regression classifier: detect when a prompt changes unexpectedly and flag it as a regression candidate, not a hard failure (since prompts can improve intentionally).
- Bundle governance: Bundle 1 (golden entity context) and Bundle 5 (correction resurrection) are mandatory for W4 merge gate. Bundles 2-4, 6-8 are tracked but not blocking.

## Files owned

- `src-tauri/tests/abilities/{ability}/fixtures/{fixture_N}/` directory layout
- Harness runner (Rust integration test or binary)
- Fixture governance scripts/docs (anonymization, retention, purge)
- CI integration (how harness slots into `cargo test`)
- Prompt-fingerprint regression classifier

## Key code surfaces to grep

- `src-tauri/src/intelligence/provider.rs` — `IntelligenceProvider` trait (W2-B) — the replay interface the harness drives
- `src-tauri/src/services/context.rs` — `ServiceContext` including `mode` field
- `src-tauri/tests/` — existing test patterns to match
- `.docs/plans/wave-W2/DOS-259-plan.md` — W2-B IntelligenceProvider contract (replay path)
- `.docs/plans/wave-W3/DOS-211-plan.md` — W3-B Provenance envelope fixture shape
- `.docs/plans/v1.4.0-waves.md` §"Adversarial Edge Case Catalogue" — bundle definitions + scoring
- `.docs/plans/v1.4.0-waves.md` §"W4 merge gate" — Suite E evidence requirements

## Specific design decisions to record in §3

- Fixture format: `inputs/` + `expected_output/` + `metadata.json` (provenance, anonymization cert, bundle tag) — confirm or propose
- Scoring: per-category pass/fail vs continuous score — ADR or ticket spec
- Hermetic isolation: how harness avoids network/DB dependencies (mock vs snapshot)
- Prompt fingerprint hash algorithm and where it's stored (git-tracked alongside fixture)

## Coordination notes for §7

- W2-B (DOS-259, already merged) — harness drives IntelligenceProvider::execute in Replay mode. The replay path must already exist; flag if it's missing.
- W3-B (DOS-211) — fixture format includes Provenance envelopes. Coordinate fixture_N metadata schema with W3-B author.
- W4-A (DOS-5) — Bundle 1 + Bundle 5 require Trust Compiler scores. Harness may run without Trust scores (trust = None) in W4 and fill in scores in W5/W6 once Trust Compiler is available.
- W5-A/W5-B (DOS-218, DOS-219) — pilot abilities generate the first real fixtures. Harness scaffolding must exist before W5 lands.

Write the plan now.
