# Implementation Plan: DOS-217

## Revision history
- v2.1 (2026-05-04) — L0 cycle-2 cleanups: defined `McpSessionId` as process-scoped opaque id; specified pre-dispatch resolution order (timing-side-channel guard); clarified `additionalProperties: false` defense-in-depth at registry-build + bridge surfacing; added `request_confirmation` user-attestation/rate-limit/args-mismatch tests.
- v2 (2026-05-04) — applies L0 cycle-1 rulings; supersedes v1
- v1 (2026-05-01) — initial L0 draft.

## 1. Contract restated

DOS-217 adds the bridge layer between the W3 ability registry and every runtime surface: Tauri app, MCP server, scheduled worker, and evaluation harness. Load-bearing Linear lines: "Implement `TauriAbilityBridge`, `McpAbilityBridge`, `WorkerAbilityBridge`, `EvalAbilityBridge`"; "`TauriAbilityBridge`: schema-validates input JSON, constructs `AbilityContext` with `Actor::User` + `Mode::Live`, invokes via registry, renders provenance for `Surface::TauriApp`, returns `AbilityResponseJson`"; "Generic `invoke_ability(ability_name, input_json, dry_run, confirmation)` Tauri command"; "`McpAbilityBridge`: iterates registry at startup; registers one MCP tool per ability with `Agent` in `allowed_actors`"; "`list_tools` MCP endpoint returns actor-filtered list; never exposes maintenance or admin-only"; and "`WorkerAbilityBridge`: scheduled maintenance invocation with `Actor::System`; only path for `global` blast radius scheduled runs."

The 2026-04-18 amendment applies: "**Test: Maintenance-category abilities invoked from `Actor::McpAgent` are rejected with `AbilityError::RequiresUserActor`.**" DOS-304's 2026-04-24 contract also applies: "`ServiceContext` capability handles are the enforcement boundary"; "Proc-macro AST inspection cannot be the hard safety boundary"; and "Do not build two registries and reconcile later." The resolution is locked: abilities are operations; MCP tools are registry-derived.

ADR pins: ADR-0111 fixes the four surfaces, actors, and modes (`.docs/decisions/0111-surface-independent-ability-invocation.md:21-29`), the generic Tauri command (`:33-66`), MCP startup registration and handler steps (`:70-96`), actor-filtered discovery (`:98-102`), worker/eval bindings (`:104-147`), and Phase 2/3 coexistence/cutover (`:150-162`). ADR-0102 says erased invocation is schema-checked at the registry boundary (`.docs/decisions/0102-abilities-as-runtime-contract.md:232-239`) and that registry enumeration must not leak unauthorized names/schemas (`:250-258`). ADR-0108 defines MCP/Tauri provenance rendering and redaction (`.docs/decisions/0108-provenance-rendering-and-privacy.md:31-72`).

Current repo reality: `src-tauri/src/bridges/` and `src-tauri/src/abilities/` do not exist today (`.docs/plans/wave-W3/DOS-211-plan.md:15`). Tauri command exports are centralized in `src-tauri/src/commands.rs:31-51`, with registration in `src-tauri/src/lib.rs:510-987`. Existing command shape uses `State<'_, Arc<AppState>>` and `state.live_service_context()` (`src-tauri/src/commands/core.rs:184-191`). The MCP binary is still a static, read-only, macro-generated sidecar: `#[tool(tool_box)]` starts at `src-tauri/src/mcp/main.rs:164`, `ServerHandler` delegates through that tool box at `:497-517`, and startup opens `ActionDb::open_readonly()` then serves stdio at `:640-666`. W2 frozen seams stay read-only for this plan: `ServiceContext` owns mode/clock/rng/external but no provider (`src-tauri/src/services/context.rs:271-277`), and `IntelligenceProvider::complete()` is the provider API (`src-tauri/src/intelligence/provider.rs:196-202`).

## 2. Approach

Create `src-tauri/src/bridges/` with `mod.rs`, `types.rs`, `tauri.rs`, `mcp.rs`, `worker.rs`, and `eval.rs`. `types.rs` owns `InvocationContext`, `AbilityResponseJson`, `AbilityInvokeError`, `BridgeActor`, `BridgeSurface`, and the session-scoped `InvocationProvenanceCache` for ADR-0108 `get_provenance(invocation_id)`. The MCP cache shape is exactly `HashMap<(McpSessionId, InvocationId), RenderedProvenance>`; there is no process-global lookup by invocation id alone. `McpSessionId` is a process-scoped opaque id minted at MCP server start (one per stdio process today, since rmcp 0.1.5's stdio transport collapses session lifetime to process lifetime), and the cache is cleared on server restart. Defining `McpSessionId` explicitly — rather than re-using a token, transport handle, or invocation id — lets a future multi-session transport (HTTP/SSE) inherit the existing scoping rule without retroactive changes. Add `pub mod bridges;` near `src-tauri/src/lib.rs:66-68`.

Create `src-tauri/src/commands/abilities.rs` for the single `#[tauri::command] invoke_ability`; add `mod abilities; pub use abilities::*;` in `src-tauri/src/commands.rs:31-51`; add `commands::invoke_ability` to the `generate_handler!` list near `src-tauri/src/lib.rs:510`. The command does no ability-specific branching: it rejects locked app state using the existing lock (`src-tauri/src/state.rs:153-158`, `src-tauri/src/commands/workspace.rs:813-817`), builds `InvocationContext { actor: User, mode: Live, surface: TauriApp, dry_run, confirmation }`, and delegates to `TauriAbilityBridge`.

Bridge algorithm for all surfaces:

1. Resolve descriptor from W3-A `AbilityRegistry` by name.
2. Reject if descriptor is experimental, actor-disallowed, mode-disallowed, category-disallowed for surface, or confirmation requirements are unmet.
3. Validate `input_json` against `AbilityDescriptor.input_schema` before dispatch; call W3-A's registry validation helper if exposed, otherwise share one bridge-local validator over the registry schema. Object schemas must declare `additionalProperties: false`; W4-C blocks surfacing/invoking an uploaded or registered schema that omits it. The registry erased wrapper remains the second validation layer.
4. Construct `AbilityContext` using W3-A's constructor, W2 `ServiceContext` (`state.live_service_context()` for Tauri/worker, `ServiceContext::new_evaluate` for eval), W2 `IntelligenceProvider` snapshot (`src-tauri/src/state.rs:246-281`, `:850-856`), and the W4-C tracer handle. Adding provider + tracer to `AbilityContext` is W4-C scope because ADR-0102/0104 expect it and merged W3-A only has services/actor/confirmation.
5. Invoke only `AbilityRegistry::invoke_by_name_json` / typed category entry points from W3-A (`.docs/plans/wave-W3/DOS-210-plan.md:24-30`); bridges contain no ability implementation.
6. Convert W3-B `AbilityOutput<T>` (`.docs/plans/wave-W3/DOS-211-plan.md:19-45`) into `AbilityResponseJson { invocation_id, ability_name, ability_version, schema_version, data, rendered_provenance, diagnostics }`.
7. Render provenance through the single ADR-0108 renderer: `Surface::TauriApp` for Tauri, `Surface::McpTool` for MCP default, `Surface::McpToolDetail` for `get_provenance`. Bridges expose ability invocation ids only; trust recompute is internal to W4-A `FinalizeMode` and never flows through an ability bridge.

Modify `src-tauri/src/mcp/main.rs` from pure static tool-box use to a hybrid Phase 2 server with one explicit migration step: keep the inherent `DailyOsMcp` `#[tool(tool_box)]` implementation for existing mechanical read tools, but remove the `#[tool(tool_box)] impl ServerHandler for DailyOsMcp` macro because it injects `list_tools` and `call_tool` today (`src-tauri/src/mcp/main.rs:497`). Manually implement `ServerHandler::list_tools` and `call_tool`: `list_tools` returns the Phase 2 static allowlist plus `McpAbilityBridge` descriptors from `registry.iter_for(Actor::Agent)`, and `call_tool` routes names either to the inherent static tool box allowlist or to `McpAbilityBridge::invoke_ability`. The local rmcp 0.1.5 trait explicitly supports these dynamic methods (`~/.cargo/registry/src/.../rmcp-0.1.5/src/handler/server.rs:184-197`), and `Tool` is just `{ name, description, input_schema }` (`.../rmcp-0.1.5/src/model/tool.rs:10-20`).

Add a bridge-owned MCP `get_provenance` tool as the ADR-0108 exception, not a capability-level hand-written tool. It is authorized by `Actor::Agent`, looks up only `(mcp_session_id, invocation_id)` pairs previously returned to that same MCP session, and renders the cached `RenderedProvenance` through `Surface::McpToolDetail`. There is no maintenance-audit fallback and no non-current-session detail tool. Default tool responses include rendered summary provenance by value; detail fetch is bounded and audited by session plus invocation id.

Add a CI drift guard script, likely `scripts/check_ability_surface_drift.sh`, and wire it beside existing boundary checks in `.github/workflows/test.yml:62-79`. It blocks new hand-written capability-level Tauri commands under `src-tauri/src/commands/` and new hand-written MCP `#[tool]` handlers outside the Phase 2 allowlist. Mechanical reads and `get_provenance` stay allowlisted until Phase 3.

End-state alignment: this makes the W3 registry the only operation catalog across app, MCP, worker, and eval. It forecloses a second MCP/Tauri operations registry, actor-selected capability handles, and surface code that bypasses schema/provenance policy.

## 3. Key decisions

Input validation: schema comes from W3-A `AbilityDescriptor.input_schema`, generated from ability input types (`.docs/plans/wave-W3/DOS-210-plan.md:24-30`). There is no per-surface or per-ability ad hoc validator. Tauri may inject the current `schema_version` only if W3-A exposes that defaulting API, per ADR-0102's internal-caller allowance (`.docs/decisions/0102-abilities-as-runtime-contract.md:174-179`); MCP rejects missing `schema_version` because it is external. Ability schemas exposed by the bridge must use JSON Schema object closure (`additionalProperties: false`) so agents cannot smuggle untyped fields through permissive descriptors. Enforcement is defense-in-depth at two layers: (1) at registry-build / startup validation, the W3-A registry rejects any compile-time descriptor whose top-level input schema or any object sub-schema omits `additionalProperties: false`, panicking before any surface is wired; (2) at bridge surfacing time (immediately before dispatch in step 3), the bridge re-checks closure on the descriptor it just resolved, which guards against later schema mutation or future descriptor-injection paths. There is no descriptor "upload" or hot-add API today; both checks fail-closed at the descriptor, never at the request, so a malformed descriptor never serves a single request.

Actor identity: caller-supplied JSON never chooses actor. The bridge chooses actor from the surface: Tauri command = `Actor::User`, MCP stdio server = `Actor::Agent`, worker schedule = `Actor::System`, eval harness = `Actor::System` in `Evaluate` mode, matching ADR-0111 (`.docs/decisions/0111-surface-independent-ability-invocation.md:21-27`). MCP uses ADR-0113's canonical server-derived label `agent:<name>:<version>`, specifically `agent:dailyos-mcp:<CARGO_PKG_VERSION>`, not a request header.

Capability-handle leakage: MCP discovery calls the existing public `registry.iter_for(Actor::Agent)` API (`src-tauri/src/abilities/registry.rs:236-245`) and caches only already-filtered descriptors; `call_tool` re-fetches and re-checks actor policy by name before invocation. Unknown ability, unauthorized actor, maintenance-hidden, experimental-hidden, and mode-hidden all map to one bridge-surface error variant, `BridgeSurfaceError::AbilityUnavailable`, serialized byte-for-byte the same externally. Pre-dispatch resolution is a single fixed-order path — name lookup → actor policy → mode policy → maintenance gate → experimental gate — so timing differences cannot disambiguate the reject reason; an out-of-order short-circuit would itself be a side channel. Internal logs may distinguish the reason, but no descriptor, schema, blast radius, confirmation token, raw `ServiceContext`, or provenance handle is exposed to MCP.

Return type: success is typed `AbilityResponseJson`, not raw JSON. MCP serializes that envelope as JSON content inside `CallToolResult::success`, whose rmcp shape is `content + is_error` (`~/.cargo/registry/src/.../rmcp-0.1.5/src/model.rs:775-793`). Errors are typed internally, then redacted at the surface boundary; no prompt text, source snippets, or provider messages leave the bridge.

Invocation ids: bridge `invocation_id`s represent ability invocations only. Trust recompute is an internal W4-A `FinalizeMode` variant, not an ability bridge call, and bridge provenance caches never track or retrieve trust recompute records.

MCP registration: prefer manual `ServerHandler::list_tools/call_tool` over trying to generate rmcp macro methods from runtime inventory. The existing macro is static (`src-tauri/src/mcp/main.rs:164-177`); registry-derived tools require runtime enumeration.

Provider dispatch: bridges do not call `IntelligenceProvider::complete()` themselves except to pass the provider into `AbilityContext`. Ability code owns intelligence calls through W2-B's trait (`src-tauri/src/intelligence/provider.rs:196-202`). This keeps W2-B's seam frozen and avoids a bridge-level orchestration fork.

Confirmation tokens: privileged abilities, including mutations and side effects, require a `ConfirmationToken` separate from the ability invocation. Tokens are issued by the Tauri host, scoped to `(actor, ability, args_hash, ttl)`, and consumed by the bridge before dispatch. MCP does not accept inline confirmation material on arbitrary tool calls; it exposes a separate `request_confirmation` bridge tool that asks the Tauri host for a scoped token and returns only the token result needed for the later ability call. The `request_confirmation` tool is itself a privileged surface and carries three additional safety properties: (a) it blocks until the Tauri host obtains an explicit user-attestation event (a host-driven prompt the user must accept) and times out per a configured TTL, never returning a token without a fresh user signal — this preserves ADR-0103's human-in-the-loop semantics across the agent surface; (b) it rate-limits per `(actor, ability)` with a TTL'd quota so an agent cannot flood the host with prompts to harvest user fatigue clicks; (c) the resulting token's `args_hash` binds the exact arguments the agent committed to at request time, so any later `call_tool` invocation whose arguments hash differently fails through the same byte-equal `BridgeSurfaceError::AbilityUnavailable` path — the agent cannot distinguish "wrong token" from "ability missing." Tests `mcp_request_confirmation_blocks_on_user_attestation`, `mcp_request_confirmation_rate_limited_per_actor`, and `mcp_request_confirmation_args_hash_mismatch_yields_unavailable` (§9) lock these in.

## 4. Security

Primary attack surface is actor-filtered discovery and invocation. Suite S must prove an MCP agent cannot enumerate or invoke Maintenance/admin/human-only abilities, even by guessing names. ADR-0103 default-denies Maintenance to agents (`.docs/decisions/0103-maintenance-ability-safety-constraints.md:197-221`), and global maintenance is never agent-invokable (`:225-245`). The synthetic Maintenance test from the Linear amendment is mandatory.

Agent-accessible entity mutation is blocked at the bridge until DOS-379 fixes typed/existence-checked entity references. The current Suite S-lite chain has no FK on `entity_members` and fails open to `project`, so W4-C must not expose agent mutation abilities that accept loose entity refs. Tauri/User and Worker/System paths can still invoke mutations after schema, actor, mode, and confirmation checks.

Tauri auth/authz: the command rejects while app lock is active (`src-tauri/src/commands/workspace.rs:813-817`), then relies on registry actor policy and confirmation tokens for privileged operations. The existing lock in `src-tauri/src/state.rs:148-158` is a boolean mutex and `get_lock_status` is read-only, so W4-C documents lock as pre-admission only: a long-running ability admitted while unlocked may complete after the app locks. If a later ability needs lock-time cancellation, that ability/service must add a privileged mutation re-check, but W4-C does not introduce cancellation semantics in the generic bridge. This is a single-user local app; there is no separate admin identity, so user-confirmed global maintenance must use the typed `ConfirmationToken` path from ADR-0103 (`.docs/decisions/0103-maintenance-ability-safety-constraints.md:232-243`).

MCP auth/authz: current MCP binds to local stdio per ADR-0027 and opens read-only state today (`src-tauri/src/mcp/main.rs:1-6`, `:645-646`). W4-C must not treat local stdio as a human actor. MCP mode is always Live and never escalates to Simulate/Evaluate (`.docs/decisions/0104-execution-mode-and-mode-aware-services.md:243-254`).

Input JSON is schema-validated before dispatch and never deserialized by hand in the bridge. Confirmation is accepted only as a scoped token issued through the Tauri host; MCP rejects inline `confirmation` fields outside the separate `request_confirmation` tool. Provenance rendering follows ADR-0108: `Surface::McpTool` strips internal IDs, prompt hashes, seeds, and deep child trees (`.docs/decisions/0108-provenance-rendering-and-privacy.md:31-40`, `:54-72`). Bridges consume rendered provenance only and never expose the full internal envelope to MCP.

No secrets/PII in logs. Bridge logs include invocation id, ability name, actor, surface, mode, error kind, and duration only, aligning with ADR-0120 redaction (`.docs/decisions/0120-observability-contract.md:126-135`). `AbilityInvokeError` variants must not embed input JSON, output JSON, prompt/completion text, OAuth tokens, or raw provider errors.

## 5. Performance

Startup: MCP tool derivation is O(N) over filtered registry descriptors and should cache `Vec<Tool>` by actor. Registry validation itself is W3-A's O(N + E) startup work (`.docs/plans/wave-W3/DOS-210-plan.md:64-68`); W4-C must not duplicate graph validation.

Per invocation: expected overhead is one registry lookup, one schema validation, actor/mode/category checks, one erased JSON boundary, one provenance render, and optional recent-provenance cache insert. ADR-0111 budgets generic dispatch + schema validation + rendering at roughly 1-5ms for trivial calls (`.docs/decisions/0111-surface-independent-ability-invocation.md:190-192`), which is below synthesis-bound ability latency.

Cache/locks: use `AppState::context_snapshot()` for coherent provider/context reads (`src-tauri/src/state.rs:841-856`), not separate provider getters. Provenance detail cache is per MCP session, keyed by `(McpSessionId, InvocationId)`, and bounded by count and serialized-byte cap; MCP default provenance render must stay <=10KB per ADR-0108 (`.docs/decisions/0108-provenance-rendering-and-privacy.md:111-119`), while W3-B enforces the envelope cap.

No new SQL migration is planned. `get_provenance` is current-session detail only from the in-memory rendered-provenance cache; it does not read maintenance audit records and does not wait for W3-C/DOS-7 durable provenance rows.

## 6. Coding standards

Services-only mutations: bridges do not mutate DB, files, signals, or external systems directly. They construct context and invoke the registry; ability implementations call services, and service mutators remain behind `ServiceContext::check_mutation_allowed()` (`src-tauri/src/services/context.rs:412-422`). Do not edit `src-tauri/src/services/context.rs` or `src-tauri/src/intelligence/provider.rs`.

Intelligence Loop 5-question check: no new schema, signal type, health-scoring rule, briefing surface, or feedback hook is introduced by the bridge. It is a routing/rendering layer over W3 ability output. Any UI TypeScript wrapper around `invoke_ability` is out of this W4-C Rust-owned plan unless a later pilot needs it.

No direct `Utc::now()` or `thread_rng()` in `src-tauri/src/bridges/` or `src-tauri/src/commands/abilities.rs`; bridge timestamps come from `ctx.services.clock.now()` or existing tracer records. Existing lint coverage is provider-only (`scripts/check_no_direct_clock_rng_in_provider_modules.sh:1-9`); W4-C should extend the W3-A abilities lint or add bridge coverage.

Fixtures and tests use synthetic ability names and generic entities only. Clippy budget remains the existing gate: `cargo clippy --manifest-path src-tauri/Cargo.toml --workspace --all-features --lib --bins -- -D warnings` (`.github/workflows/test.yml:74-75`).

## 7. Integration with parallel wave-mates

W3-A/DOS-210 owns `AbilityRegistry`, descriptors, `Actor`, `AbilityContext`, schema metadata, `invoke_by_name_json`, and `iter_for(Actor::Agent)` (`.docs/plans/wave-W3/DOS-210-plan.md:24-36`). W4-C consumes those. If W4-C implementation starts before W3-A lands, define a narrow `AbilityInvoker` trait in `src-tauri/src/bridges/types.rs` and replace it with W3-A imports during integration.

W3-B/DOS-211 owns `AbilityOutput<T>`, `Provenance`, and `RenderedProvenance` inputs (`.docs/plans/wave-W3/DOS-211-plan.md:19-45`). W4-C consumes the envelope and renderer only; it does not define provenance internals. Bridge tests assert rendered provenance diffs per surface (`TauriApp`, `McpTool`, `McpToolDetail`), not full internal provenance envelope equality.

W4-C explicitly owns `EvalAbilityBridge` and ships it in Phase 2 for W4-B/DOS-216. Public API shape is minimal and Tauri-free: `EvalAbilityBridge::new(registry, EvalAbilityDeps { fixture_services, provider, tracer })` plus `invoke_json(ability_name, input_json, dry_run)` returning `AbilityResponseJson`. `fixture_services` is the Evaluate-mode `ServiceContext`, `provider` is the replay/fixture `IntelligenceProvider`, and `tracer` is the bridge/ability trace sink. It should call the same registry erased invocation path as Tauri/MCP so fixture evidence covers the real bridge.

W2-B/DOS-259 is already merged. W4-C reads `AppState::context_snapshot()` and passes the selected provider into `AbilityContext`; it does not change the provider trait or selection seam (`src-tauri/src/intelligence/provider.rs:14-22`, `:309-326`).

Phase 2 collision points: `src-tauri/src/lib.rs` command registration, `src-tauri/src/commands.rs` module exports, and `src-tauri/src/mcp/main.rs` hybrid handler. Phase 3 cleanup of hand-written capability commands/tools is not this PR unless the migrated abilities already exist.

## 8. Failure modes + rollback

If registry startup validation fails, bridges fail closed: `invoke_ability` returns a typed unavailable error and MCP derived list is empty, while the Phase 2 static mechanical allowlist can still serve existing read tools. If schema validation fails, ability code is never executed. If provenance rendering fails, return a hard bridge error; do not return domain data without provenance.

If MCP tool derivation fails midway, the server must not partially expose unfiltered descriptors. Build the filtered `Vec<Tool>` in a local value, then atomically swap/cache it. If `call_tool` sees a tool name that was listed earlier but is no longer authorized after registry reload, invocation loses and returns the same serialized unavailable error used for unknown/hidden abilities.

Rollback is mechanical and migration-free: remove `invoke_ability` from `generate_handler!`, stop constructing bridges, and let current hand-written commands/static MCP tools continue during Phase 2. W1-B universal write fence is honored because W4-C adds no direct write path; all writes remain inside abilities/services and the existing fence (`scripts/check_write_fence_usage.sh:1-22`).

Global maintenance rollback safety: if `WorkerAbilityBridge` cannot prove it is the scheduled-worker path, it rejects `global` rather than falling back to Tauri or MCP. Eval rollback safety: missing replay fixture returns a typed fixture error and never falls through to Live, matching W2-B replay behavior (`src-tauri/src/intelligence/provider.rs:227-295`).

## 9. Test evidence to be produced

Bridge/unit tests: `invoke_ability_rejects_locked_app`, `invoke_ability_documents_lock_as_pre_admission_only`, `invoke_ability_rejects_unknown_ability_without_enumeration`, property/table test `hidden_ability_errors_are_byte_equal_for_unknown_unauthorized_maintenance_experimental_mode`, `hidden_ability_pre_dispatch_resolution_order_is_fixed_and_timing_independent`, `invoke_ability_schema_invalid_input_fails_before_dispatch`, `invoke_ability_rejects_schema_without_additional_properties_false_at_runtime`, `registry_build_panics_on_descriptor_without_additional_properties_false`, `invoke_ability_rejects_actor_override_in_input_json`, `invoke_ability_user_actor_requires_confirmation_when_policy_requires`, `confirmation_token_scoped_to_actor_ability_args_hash_ttl`, `agent_entity_mutation_blocked_until_dos_379`, `invoke_ability_returns_ability_response_json_with_tauri_provenance`, `bridge_provenance_tests_compare_rendered_surface_output_only`, and `bridge_errors_do_not_include_input_or_prompt_content`.

MCP tests under `--features mcp`: `mcp_list_tools_derives_from_registry_iter_for_agent`, `mcp_list_tools_filters_agent_actor`, `mcp_list_tools_hides_maintenance_admin_experimental_and_mode_hidden`, `mcp_call_tool_rechecks_actor_policy_for_guessed_name`, `mcp_hidden_ability_error_bytes_match_unknown_ability`, `mcp_maintenance_synthetic_internal_requires_user_actor_but_surface_unavailable`, `mcp_rejects_inline_confirmation_field`, `mcp_request_confirmation_issues_scoped_token_via_tauri_host`, `mcp_request_confirmation_blocks_on_user_attestation`, `mcp_request_confirmation_rate_limited_per_actor`, `mcp_request_confirmation_args_hash_mismatch_yields_unavailable`, `mcp_session_id_is_process_scoped_and_cleared_on_restart`, `mcp_tool_descriptor_uses_registry_input_schema`, `mcp_serverhandler_tool_box_macro_removed_and_manual_routes_static_or_ability`, `mcp_response_includes_actor_filtered_rendered_provenance`, `mcp_get_provenance_cache_key_includes_session_id`, `mcp_get_provenance_rejects_cross_session_invocation_id`, `mcp_get_provenance_has_no_maintenance_audit_fallback`, and `mcp_get_provenance_redacts_internal_ids_for_agent`.

Worker/eval tests: `worker_bridge_invokes_maintenance_as_system_live`, `worker_bridge_rejects_global_without_scheduled_worker_marker`, `eval_bridge_constructs_evaluate_context_with_fixture_services_provider_tracer`, `eval_bridge_uses_replay_provider_and_never_live_provider`, `eval_bridge_invokes_same_erased_registry_path_as_runtime_bridges`, and `eval_bridge_runs_without_tauri_runtime`.

CI/lint evidence: `surface_drift_lint_blocks_new_capability_tauri_command`, `surface_drift_lint_blocks_new_handwritten_mcp_tool`, and the workflow line added beside `.github/workflows/test.yml:62-79`. Gate artifact for this PR: `cargo test bridge`, `cargo test --features mcp mcp_ability_bridge`, `cargo test eval_bridge`, `./scripts/check_ability_surface_drift.sh`, and the standard clippy/test gate.

Suite contributions: Suite S owns auth/authz on `invoke_ability`, MCP actor-filter discovery, schema-validation on input JSON, and capability-handle leakage. Suite E gets an end-to-end no-op ability through `EvalAbilityBridge` and MCP/Tauri bridge smoke coverage. Suite P records startup tool-derivation count and p95/p99 bridge overhead for a no-op Read ability.

## 10. Open questions

No unresolved L0 blocker remains; these are now implementation action items and resolved rulings.

1. Action: add provider + tracer to `AbilityContext` in W4-C scope, preserving a bridge-safe constructor from `ServiceContext`, provider, user, tracer, actor, and confirmation without giving W4-C raw `ActionDb`/`AppState` handles.

2. Resolved: `get_provenance(invocation_id)` storage is MCP-session-scoped only: `HashMap<(McpSessionId, InvocationId), RenderedProvenance>`. There is no process-global cache, no maintenance audit fallback, and no non-current-session detail tool.

3. Resolved: MCP actor detail shape is ADR-0113 `agent:<name>:<version>`, using `agent:dailyos-mcp:<CARGO_PKG_VERSION>` for this bridge.

4. Resolved: existing static MCP mechanical tools coexist until Phase 3 per ADR-0111 (`.docs/decisions/0111-surface-independent-ability-invocation.md:152-161`), but only through the explicit Phase 2 allowlist and manual `ServerHandler` routing.
