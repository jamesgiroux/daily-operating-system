# Session prompt — v2.0 Shell Rebuild case document

Draft the v2.0 Shell Rebuild case document for DailyOS. The previous attempt
(`.docs/plans/v2.0-shell-rebuild-boundary.html`, commit 69121e29) was REJECTED
by James as reactive and blinkered — scoped to one session's incidents, no
case. It is an input, not a base. Do not pattern-match to it.

## Read FIRST, fully, before any writing (this order)
1. `.docs/design/product/MISSION.md`, `VISION.md`, `PRODUCT-THESIS.md`,
   `PHILOSOPHY.md`, `PRINCIPLES.md` — the category being served.
2. `.docs/decisions/` — the FULL ADR history (index in README.md). Trace the
   14-week arc: what each major decision learned from the last.
3. `docs/solutions/` — the K-in corpus; the failure→fix record.
4. `.docs/plans/v2.0-shell-audit.md` — measured shell audit (agent-produced;
   verify its key numbers yourself before citing).
5. `.docs/plans/dailyos-rearchitecture-examination-2026-05-29.md` — the prior
   clean-sheet examination.
6. Code: src/pages/ + src/hooks/ data paths; src-tauri db_service/intel_queue
   (starvation class, DOS-866); the composition substrate.

## The case James wants made (his framing, verbatim spec)
- **Personal intelligence as a category** (memory + judgment) — why serving it
  demands a different shell than the one we built.
- **The 14-week journey as evidence**: learned, failed, learned, succeeded —
  the arc led here; this is earned conviction, not reaction.
- **The scaling thesis (core)**: the current shell cannot stay performant for
  even ONE user with ~20 accounts as context grows. Writer starvation
  (DOS-866), 693-claim subjects, composition-commit-on-page-load, enrichment
  grind are SYMPTOMS. v2.0 = rearchitect for performance-as-context-grows
  while simplifying complexity and redundancy.
- Substrate is NOT rewritten (abilities-runtime, claims+services, design
  system, ADR/learnings corpus cross the boundary). Shell, surfaces, dual
  systems die. ~4–5 users; migration cost near zero.

## Process gates (James's standing rules)
- Outline the ARGUMENT and get James's sign-off BEFORE authoring HTML.
- Final doc is Tier 1 HTML per `.docs/plans/AUTHORING.md` (use `_templates/`
  + `_patterns/`; shop existing classes; zero new styles/inline CSS).
- Reuse-before-build is a CLAUDE.md critical rule. Verify every citation by
  opening the file. No rhetoric; measured claims only.

## In flight (check before starting)
- DOS-866 codex implementation in a worktree (L0-approved packet at
  `.docs/plans/dos-866-l0-packet.md`) — validate ACs when it reports; ships
  regardless of v2.0.
- Shell audit agent writing `.docs/plans/v2.0-shell-audit.md`.
- Branch `dos-852-v150-wr-detail-surface-reconciliation` holds the WR spine
  work (L4 learnings feed v2.0). Memory: `project_v20_shell_rebuild_decision`.
- Dev app: `DAILYOS_DB_MODE=replica pnpm tauri dev`; quiet background via
  `DAILYOS_DISABLE_BACKGROUND_WORKERS=1` for L4 sessions.

## Deliverables, in order
1. Argument outline (markdown, ~1 page) → James gate.
2. Tier 1 HTML case doc (supersedes the boundary draft; fold its keep/kill
   matrices in as ONE section of the larger case).
3. v2.0 ADR proposal.
4. Linear sweep proposal (supersede/keep/backlog split) → James gate before
   executing.
