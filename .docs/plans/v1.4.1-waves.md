# v1.4.1 — Parallel Codex Wave Plan

**Date:** 2026-05-07
**Target:** Substrate completion, validation suite, capability migrations to round out the abilities runtime
**Source of truth:** Linear project [v1.4.1 — Abilities Runtime Completion](https://linear.app/a8c/project/v141-abilities-runtime-completion-19eb22af50e7) + v1.4.1 project description (Definition of Done, scope, non-goals)
**Predecessor:** [v1.4.0-waves.md](v1.4.0-waves.md) — substrate spine + 2 pilots + Golden Daily Loop release gate. v1.4.1 builds on, doesn't restate.

## What v1.4.0 leaves behind

v1.4.0 shipped:
- ServiceContext + ExecutionMode + capability handles
- IntelligenceProvider trait + PTY/Glean/Replay implementations
- Ability registry + `#[ability]` macro + 4 bridges (Tauri, MCP, Worker, Eval)
- `intelligence_claims` schema + 9-mechanism atomic consolidation
- Trust Compiler core (factors, bands, recompute, ClaimTrustChanged signal)
- Provenance Envelope + SubjectAttribution + source_asof
- Temporal scope + sensitivity + CLAIM_TYPE_REGISTRY
- 2 ability pilots: `get_entity_context` (Read), `prepare_meeting` (Transform)
- Eval harness + 13 adversarial fixture bundles
- Golden Daily Loop release gate (`pnpm release-gate -- --mode hermetic`)
- Subject-ownership enforcement at the bridge boundary (DOS-288)
- Tauri claim-backed lifecycle (DOS-411)
- ADR-0108 sensitivity rendering audit (DOS-412)

What v1.4.0 does NOT ship:
- 3 capability migrations beyond the 2 pilots (`get_daily_readiness`, `list_open_loops`, `detect_risk_shift`)
- Durable signal infrastructure (registry, invalidation queue, coalescing, load gate)
- Scoring depth (factor library, freshness decay table, shadow-mode rollout)
- Several substrate completions (source taxonomy migration, prompt fingerprinting at the IntelligenceProvider extension, provenance rendering substrate, temporal primitives, key provider seam, contract-first operations, etc.)
- Full validation suite across 5 axes (stale-current, cross-surface consistency, ambiguous identity, source lifecycle, sync/concurrency)
- BrainBench-style harness, edge-case regression suite

v1.4.1 closes those.

## Product thesis alignment

v1.4.1 is the **substrate completion** release — not a surfaces release. The project description is explicit:

> **Non-goals:** Claim receipt UI / Activity Log / Lint Mode → v1.4.4. Recommendation contract / salience scoring → v1.4.5. Proactive trigger policy / notifications → v1.4.6. MCP exposure → v1.5.0.

Every wave in v1.4.1 should preserve the v1.4.0 product invariants and add three more:

6. **Signals propagate durably**, not in-process best-effort. Claim invalidation survives crashes and restarts.
7. **Trust scoring biases real surfaces** through shadow-mode comparison data, not just unit tests.
8. **Capability migrations are validated end-to-end** against bundles that prove parity vs the legacy code path being replaced.

Same Mission Gate as v1.4.1 project: *"Does this make DailyOS better at maintaining and updating the user's working understanding of their professional world?"* — for v1.4.1 the answer should usually be: yes, because it makes the underlying working model more durable, trustworthy, revisable, or measurable.

## Reading order

1. This doc — wave assignments and merge gates.
2. v1.4.1 Linear project description — full scope + Definition of Done.
3. Per-issue Linear ticket — frozen contract + acceptance criteria. **Each agent's prompt is the Linear ticket, not a paraphrase.**
4. ADRs referenced in the ticket.
5. v1.4.0 proof bundles (W2-W6) for the substrate primitives v1.4.1 builds on.

## Reusing v1.4.0's review system

The L0–L6 review ladder, plan-review template, reviewer matrix, test suites S/P/E, proof-bundle template, escalation packet format, and pilot/retro protocol are all defined in [v1.4.0-waves.md](v1.4.0-waves.md). **v1.4.1 reuses them unchanged unless explicitly amended below.**

Amendments for v1.4.1:

- **No pilot wave.** v1.4.0 used W0 as the L0-L6 system pilot (2 bug-fix agents). The system is now proven. v1.4.1 starts at full fan-out from W0.
- **Suite P baseline migrates forward.** The criterion benches established at v1.4.0 W1 stay locked. v1.4.1 measures regression vs v1.4.0 baselines at every gate, not against itself.
- **Suite E coverage expands.** v1.4.0 ran bundles 1+5+13 mandatory. v1.4.1 W6 expands to all bundles 1-13 mandatory; new validation bundles per the 5 validation tickets (DOS-289/290/291/292/293) gate at W6.
- **Frozen-contract verification stricter.** Every L0 plan must include a `git diff fork-point..dev` over surfaces touched, per the [`feedback_l0_reconcile_against_dev`](../../.claude/projects/-Users-jamesgiroux-Documents-dailyos-repo/memory/feedback_l0_reconcile_against_dev.md) memory.

## Scope cleanup before kickoff

Linear hygiene items resolved before plan freeze. Status captured here; history on the tickets.

1. **DOS-312** (Canceled) — confirmed Canceled 2026-05-07; removed from v1.4.1 project.
2. **DOS-313** (Duplicate) — canonical ticket confirmed (folded into the DOS-7 substrate work); removed from v1.4.1 project 2026-05-07.
3. **DOS-314** — Reclassified Duplicate → Done 2026-05-07; substrate landed in v1.4.0 DOS-7 amendment B. Now in v1.4.0 project, not v1.4.1.
4. **DOS-263 → DOS-236 fold** — DOS-263 ("Invalidation job queue — Minions-shape schema for ADR-0115 §5") consolidated into DOS-236 ("Durable Invalidation Job Model"). DOS-263 marked `duplicateOf=DOS-236` 2026-05-07; DOS-236 description carries the schema spec.
5. **Feedback delta tuning + subject-fit confidence floor tuning** — folded into DOS-6 (trust scoring shadow-mode rollout) as two added acceptance criteria, comment on DOS-6 dated 2026-05-07. No new tickets filed; tuning happens within the shadow-mode work where the data lives.
6. **DOS-348 + DOS-349** routed to v1.4.1 project 2026-05-07. DOS-348 (criterion bench harness) lands in W7 close. DOS-349 (ability-runtime crate split) lands as **W0.5** (cycle-1 L0 review moved it from W4.5 → W0.5 to avoid a substrate-trap).
7. **DOS-383 (external-client replay framework)** — confirmed closed in v1.4.0 W4-B-ext per the v1.4.0 proof bundle; W2-I cites it but does not re-implement.

## Wave shape — nine wave units (W0, W0.5, W1–W7) + background lane

Mapping the 47 active v1.4.1 tickets onto codex-agent waves where each wave gates on the prior wave's merge:

| Wave | Issues | Parallel agents | Wall-clock target |
|---|---|---|---|
| **W0** — Research + lint hygiene + scope cleanup | DOS-241 (research), DOS-342, DOS-378, DOS-379, DOS-380, DOS-381, DOS-382 | 7 | 1-2 days |
| **W0.5** — Ability-runtime crate boundary established | DOS-349 | 1 | 2-3 days |
| **W1** — Source taxonomy + signal infrastructure | DOS-212 (taxonomy), DOS-235, DOS-236 (+DOS-263 fold-in), DOS-237, DOS-306 (contract lock) | 5 | 2-3 days |
| **W2** — Substrate completions (fan-out) | DOS-213, DOS-214, DOS-215, DOS-234, DOS-262, DOS-264, DOS-265, DOS-295, DOS-315, DOS-344, DOS-347, DOS-448 | 12 | 3-4 days |
| **W3** — Scoring + trust depth | DOS-10, DOS-238, DOS-6 (with tuning AC folded in) | 3 | 7+ day shadow bake (gates on data sufficiency, not wall clock) |
| **W4** — Claim-shape extensions + canonicalization | DOS-276, DOS-280 | 2 | 2 days |
| **W5** — Capability migrations | DOS-220, DOS-221, DOS-222 | 3 | 3-4 days |
| **W6** — Validation suite + eval depth | DOS-282, DOS-261, DOS-289, DOS-290, DOS-291, DOS-292, DOS-293 | 7 | 4-6 days |
| **W7** — Release gate hardening + close | DOS-285, DOS-439, DOS-440, DOS-441, DOS-260, DOS-348 | 6 | 2-3 days |
| **Background** — telemetry baking | DOS-284 (runs from W0 onward), DOS-260 (W7 close) | 1 | spans full release |

Total = 47 active tickets across 9 wave units (W0, W0.5, W1–W7) + 1 background lane. The W0.5 placement is intentional: DOS-349 establishes the compile-time `abilities-runtime` crate boundary BEFORE any subsequent wave adds new files under `abilities/` — so all W2/W3/W4 substrate is born inside the new crate with the boundary live, instead of being relocated after the fact (cycle-1 BLOCK from architect-reviewer + codex).

## What "parallel" means here

Same shape as v1.4.0:
1. **A frozen contract.** The Linear ticket text is the contract. Agents do not invent shapes.
2. **An exclusive file/table allowlist.** Listed below per agent.
3. **A deny list.** Files no other agent in the wave will touch.
4. **A merge gate.** Concrete artifact (test, lint, command output) proving done before the next wave starts.

## CI-enforced architecture invariants — additions for v1.4.1

v1.4.0's invariants stay active. v1.4.1 adds these:

| Invariant | Mechanism | Activated by |
|---|---|---|
| Ability code cannot reach raw DB / `AppState` / fs at compile time (DOS-304 hard boundary) | crate-graph: `abilities-runtime` does not depend on `db`/`state`/`db_service`/`queries` | W0.5 merge |
| `data_source` is the canonical column name; no new `source` column references | grep CI test | W1-A merge |
| Signal emission goes through `signals/policy_registry.rs` (no direct `db.execute` on signal events; channel inventory enforced — see W1-B done-when) | grep CI test + clippy + W1 Suite S signal-channel traversal property | W1-B merge |
| Invalidation jobs are durable (signal → queue row → claim recompute, all transactional, survives restart and key rotation) | property test on signal/job/claim chain + restart-test + key-rotation simulation | W1-C merge |
| Prompt fingerprints are computed at IntelligenceProvider boundary, not per-call | clippy lint on direct fingerprint calls outside provider boundary | W2-A merge (lint vacuous until then) |
| `EngagementCurve` and `RoleProgression` are domain-neutral primitives | trybuild on trait shape + grep regression on CS-vocabulary terms (account, churn, expansion) inside `abilities/temporal/` | W2-C merge |
| Trust thresholds are config-driven, not literal constants | grep on `f64`/`f32` literals inside `abilities/trust/` excluding `*_test.rs` and `*/config.rs` paths | W3-B merge |
| Capability migrations gate on parity-vs-legacy harness bundle pass | harness assertion in capability migration tests | W5-A/B/C merge |
| **Bundles 1-18 mandatory** in v1.4.1 release gate (no partial-pass cut; per-bundle owner; flake = single-bundle quarantine + ticket; no release with quarantined bundle) | release-gate config + harness test | W6 merge / W7 release gate |
| MCP-bridge re-test for new capabilities (subject-ownership, ADR-0108 sensitivity, signal-payload privacy) | Suite S extension run against MCP responses for the 3 W5 abilities | W5 merge / W7 release gate |

## Proof-bundle + retro template

Same as v1.4.0. Each wave produces a proof bundle and (for designated waves) a retro doc. Save to `.docs/plans/wave-W{N}/proof-bundle.md` and `.docs/plans/wave-W{N}/retro.md`.

Mandatory retros for v1.4.1:

| Wave | Retro |
|---|---|
| W1 | **Mandatory** — first signal-infrastructure work; durability protocol learnings |
| W2 | **Mandatory** — 12-agent fan-out, largest of the release |
| W3 | **Mandatory** — first shadow-mode rollout; first time scoring depth meets real claim-volume data |
| W6 | **Mandatory** — validation matrix expansion |
| Others | Optional unless material observations |

---

# Wave 0 — Research + lint hygiene + scope cleanup

W0 has two intentional jobs running side-by-side:
1. **Research + design lock** for the enrichment refactor that informs everything downstream.
2. **Lint sweep** of v1.4.0 hardening tickets — mostly mechanical, distributes well across parallel codex agents, doesn't block other work.

### Agent W0-A — DOS-241 enrichment refactor design research

- **Spec:** [DOS-241](https://linear.app/a8c/issue/DOS-241).
- **Output:** ADR or design doc + amendments to DOS-235/236/237/295 if the design surfaces conflicts.
- **Files owned:** `.docs/decisions/` for new ADR, `.docs/plans/wave-W1/` for design notes.
- **Done when:** L0 reviewer (architect-reviewer) confirms the design answers the v1.4.0 carry-forward questions about claim-based generation granularity (DOS-295) and signal/job/claim ownership boundaries (DOS-306).

### Agent W0-B — DOS-342 workspace clippy let_underscore_must_use rollout

- **Spec:** [DOS-342](https://linear.app/a8c/issue/DOS-342). Originally split out from v1.4.0 W0 cycle-2 restructuring.
- **Files owned:** `Cargo.toml` clippy lints config, mechanical `_ = x?` -> `?` rollout across 805 patterns.
- **Done when:** clippy clean with the new lint as deny; cargo test still green.

### Agent W0-C — DOS-378 serde_json::json! for signal payloads

- **Spec:** [DOS-378](https://linear.app/a8c/issue/DOS-378).
- **Files owned:** `services/accounts.rs:2566/2610/3811`, `services/people.rs:461`.
- **Done when:** all 4 sites use `serde_json::json!`; lint passes.

### Agent W0-D — DOS-379 fail-closed entity_id check

- **Spec:** [DOS-379](https://linear.app/a8c/issue/DOS-379).
- **Files owned:** `services/people.rs:244` (`stakeholder_entity_type_for_id`), schema FK migration on `entity_members.entity_id`, `derived_state.rs::rebuild_stakeholder_insights_cache_for_entity` pair-validation.
- **Done when:** unit test for unknown entity_id rejection green; schema FK migration green; CI lint enforces.

### Agent W0-E — DOS-380 stakeholder_writer signal lint

- **Spec:** [DOS-380](https://linear.app/a8c/issue/DOS-380).
- **Files owned:** `scripts/check_stakeholder_writer_emits_signal.sh` rewrite, canonical wrapper enforcement.
- **Done when:** lint accepts only allowlisted writers OR canonical wrappers; regression test green.

### Agent W0-F — DOS-381 claim immutability allowlist parser

- **Spec:** [DOS-381](https://linear.app/a8c/issue/DOS-381).
- **Files owned:** `src-tauri/scripts/check_claim_immutability_allowlist.sh` rewrite (preferred Option 3: clippy custom rule).
- **Done when:** rewritten lint catches multi-line forbidden assignments; existing valid writes still pass.

### Agent W0-G — DOS-382 derived_state.rs visibility tightening

- **Spec:** [DOS-382](https://linear.app/a8c/issue/DOS-382). Option 1: `pub(crate)`.
- **Files owned:** `src-tauri/src/services/derived_state.rs:1008/1095/1126/1143/1159/1185`.
- **Done when:** all 6 helpers `pub(crate)`; cargo build green; no external caller depends on the wider visibility.

### W0 merge gate

L0 → L2 cleared per the Review Ladder for the lint-sweep agents (W0-B/C/D/E/F/G); L0 → L2 → L3 for W0-A. Required artifacts:
- L0 plan approvals (architect-reviewer for W0-A; security-auditor for W0-D; code-reviewer for the lint sweep agents)
- L2 diff approvals on all 7 PRs
- L3 wave adversarial on W0-A: codex-challenge against integrated design doc + DOS-241 accepted as input to W1
- CI invariants now active: pre-W1 lint hygiene
- Proof bundle written; retro is optional but recommended given the system has had v1.4.0's pilot

---

# Wave 0.5 — Ability-runtime crate boundary established (hard precondition for W1+)

Single agent. Lifts ability implementations into a workspace crate that cannot reach raw DB / `AppState` / filesystem at compile time. Originally drafted as W4.5 in cycle-1 of this plan; cycle-1 L0 review (architect-reviewer + codex adversarial) flagged the W4.5 placement as a substrate-trap — every W2/W3/W4 agent that adds files inside `abilities/` would land them in the old crate, then W4.5 would relocate the entire tree, forcing rebases or conflicts on every downstream PR. **Resolution: W0.5 lands the boundary first, so all subsequent waves are born inside the new crate with the boundary live.**

### Agent W0.5-A — DOS-349 ability-runtime crate split

- **Spec:** [DOS-349](https://linear.app/a8c/issue/DOS-349). Routed to v1.4.1 W0.5 by Linear comment 2026-05-07; restructured from W4.5 → W0.5 by cycle-1 L0 review.
- **Files owned:** new workspace crate (proposed `abilities-runtime/`), workspace `Cargo.toml` member entry, ability module relocations from `src-tauri/src/abilities/` into the new crate, thin re-export crate exposing only the public `ServiceContext` surface, `scripts/check_no_db_state_imports_in_abilities.sh` and `scripts/check_no_direct_clock_rng_in_abilities.sh` (deleted or downgraded to defense-in-depth).
- **Recommended PR decomposition (per codex cycle-1 review §F2):** stage as five sequential PRs to bound blast radius — (a) workspace skeleton + thin re-export crate, (b) ability registry + provenance + trust modules moved, (c) remaining ability modules moved, (d) lint scripts downgraded or deleted, (e) compile-time-fail-on-bypass integration proof. Each sub-PR L2-reviewed independently; W0.5 gate waits for the integration proof PR.
- **Done when:** `cargo build -p abilities-runtime` fails if any ability body imports `crate::db`, `crate::state`, `crate::db_service`, `crate::queries`, raw `rusqlite`, `std::fs::write`, `File::create`, or `tokio::fs` — verified by removing those crates from the dependency graph. DOS-304 hard-boundary contract upgrades from "lint script enforcement" to "compile-time enforcement via crate visibility." All scripts/lint that were defense-in-depth either deleted or marked redundant.

### W0.5 merge gate

L0 → L2 → L3 → L5 cleared per the Review Ladder. Required artifacts:
- L0 plan approval (architect-reviewer — structural refactor with crate-graph implications)
- L2 diff approvals on the staged PR series
- L3: full Suite S re-run (no regression from the crate move)
- L5 drift check: integrated state matches v1.4.0 DOS-304 hard-boundary contract — boundary now compile-time, not lint-only
- Proof bundle written

---

# Wave 1 — Source taxonomy + signal infrastructure

This wave converts the substrate's data model + reactive layer onto v1.4.1's invariants. Five agents, but **not all parallel** — internal ordering:

- **W1-A (taxonomy migration)** runs in parallel with the signal track.
- **W1-B (Signal Policy Registry)** and **W1-C (Durable Invalidation Job Model)** run in parallel; each shares the W1-E frozen contract.
- **W1-D (Signal Coalescing + Queue Bounds)** depends on W1-B + W1-C merging first; serial within the wave.
- **W1-E (signal/job/claim contract)** runs in parallel with all of the above; the contract doc is consumed by W1-B/C/D L0 plans before they start. **W1-B/C/D L0 plans must cite the W0-A DOS-241 ADR's accepted/rejected amendments to DOS-235/236/237/306 acceptance criteria** — DOS-241 is a hard input, not advisory.

Effective parallelism: 4 lanes (A, B, C, E), then D once B+C merge. The wave-shape table line "Parallel agents: 5" is a count, not a parallelism factor.

### Agent W1-A — DOS-212 source taxonomy migration

- **Spec:** [DOS-212](https://linear.app/a8c/issue/DOS-212).
- **Files owned:** `intelligence_claims` schema migration (rename `source` → `data_source`), all readers/writers across services + abilities + db modules.
- **Don't touch:** signal/invalidation modules (W1-B/C/D own).
- **Done when:** migration backfills atomically; `data_source` is the only reference; legacy `source` references deleted; all tests green.

### Agent W1-B — DOS-235 Signal Policy Registry + consolidated emit_signal

- **Spec:** [DOS-235](https://linear.app/a8c/issue/DOS-235).
- **Files owned:** `src-tauri/src/signals/policy_registry.rs` (new), `signals/bus.rs::emit_signal` consolidation, every direct `db.execute` on signal events refactored.
- **Done when:** registry exhaustiveness check green; `emit_signal` is the only writer; **all signal-emission channels enumerated and routed through `policy_registry.rs`** (per `feedback_enumerate_channels_before_patching.md` — at minimum: services/, abilities/, Tauri/MCP/Worker/Eval bridges, trigger-derived state, replay binary from W2-I, telemetry sinks, future external clients) — channel inventory captured in `.docs/plans/wave-W1/W1-B-channel-inventory.md`; W1 Suite S asserts no signal can reach the invalidation queue without traversing `policy_registry.rs`; CI lint enforces.

### Agent W1-C — DOS-236 Durable Invalidation Job Model + DOS-263 fold-in

- **Spec:** [DOS-236](https://linear.app/a8c/issue/DOS-236) + [DOS-263](https://linear.app/a8c/issue/DOS-263).
- **Files owned:** `invalidation_jobs` table + Minions-shape schema, chain_id/retry/dead-letter, claim recompute consumer wiring.
- **Don't touch:** signal registry (W1-B owns); coordinate via signal -> job emission contract.
- **Done when:** signal → durable job → claim recompute round-trips through transaction; restart-test green (jobs survive process kill); dead-letter visible; chain_id correctly tracks recompute fan-out.

### Agent W1-D — DOS-237 Signal Coalescing + Queue Bounds + Load Test Gate

- **Spec:** [DOS-237](https://linear.app/a8c/issue/DOS-237).
- **Files owned:** coalescing logic in `signals/bus.rs` + `invalidation_jobs` consumer, queue-bound config, load-test harness.
- **Depends on:** W1-B + W1-C must be merged first (this is a single-agent gate but coordinates with both).
- **Done when:** burst-of-N signals coalesces to ≤M jobs (config-driven); queue bound enforced; load test holds at sustained signal rate.

### Agent W1-E — DOS-306 contract: signal bus durability + policy registry + claim-event load gate

- **Spec:** [DOS-306](https://linear.app/a8c/issue/DOS-306).
- **Files owned:** `.docs/plans/wave-W1/DOS-306-contract.md` design doc that locks the boundary between signal registry / job queue / claim recompute. Same shape as v1.4.0 contract docs (DOS-302/303/304/307).
- **Done when:** all three implementing agents (W1-B/C/D) confirm the contract matches their implementation; verification doc cites file:line.

### W1 merge gate

L0 → L2 → L3 → L5 cleared per the Review Ladder. Required artifacts:
- L0 plan approvals (architect-reviewer + performance-engineer for W1-B/C/D; architect-reviewer for W1-A; spec writer for W1-E)
- L2 diff approvals on 5 PRs
- L3 wave adversarial: codex-challenge + architect-reviewer + **Suite P report** (signal coalescing under burst, queue bound under sustained load — within budget) + **Suite S report** (no signal payload PII leakage, no cross-tenant signal routing)
- CI invariants now active: data_source canonical; emit_signal-only path; durable invalidation jobs (transactional)
- L5 drift check: integrated state matches v1.4.1 project DoD §Signal infrastructure
- **Mandatory retro** — first signal-infrastructure wave; system-learning gold
- Proof bundle written

---

# Wave 2 — Substrate completions (fan-out)

Twelve agents in parallel. Strict file ownership. Most are independent; ordering callouts noted.

**Staging — three batches of four.** Memorialized 2026-05-07 to bound dev-box memory/IO load: launch agents in batches of 4 sequentially, not all 12 at once. The L2 review and subsequent fix cycles still run per-agent independently — staging is only on the *initial* codex implementation launches. Batch boundaries:

| Batch | Agents | Rationale |
|---|---|---|
| **Batch 1** | W2-A (DOS-213), W2-B (DOS-214), W2-C (DOS-215), W2-D (DOS-234) | Provider/provenance/temporal/key-provider — all new files, no shared mutation surface |
| **Batch 2** | W2-E (DOS-262), W2-F (DOS-264), W2-G (DOS-265), W2-H (DOS-295) | Fail-improve / operations / claim-edge / claim-granularity — claims-adjacent, but each owns a non-overlapping seam |
| **Batch 3** | W2-I (DOS-315), W2-J (DOS-344), W2-K (DOS-347), W2-L (DOS-448) | Replay / migration / mode-transition / parser-defense — touch existing code more than new files; run last so any merge friction with batches 1+2 surfaces here |

Each batch waits for its own four agents to return *implementation* (not L2 approval) before launching the next. L2 cycles can interleave across batches once they're queued.

**Frozen-contract gates inside W2.** Cycle-1 L0 review found shared-file ownership conflicts on `services/claims.rs` and `abilities/registry.rs`. Resolution: explicit sub-file or function-level seams, plus a frozen-contract gate before batch 3:

- `services/claims.rs` — owned at sub-function granularity. W2-H owns `targeted_repair_*` functions only; W2-I owns the `consolidation_job_*` functions only; W2-L owns the parser site at line 487 only. No cross-write outside owned function bodies. Integration commit on top reconciles imports/use blocks.
- `abilities/registry.rs` (extension) and `abilities/claims.rs::CLAIM_TYPE_REGISTRY` — W2-F owns the registry **operations** sub-module; W2-G owns the registry **claim_types/FRONTMATTER_LINK_MAP** sub-module. If the v1.4.0 layout has them in one file, both agents emit a sub-module split as part of their PR.
- **Provider trait freeze (gate before batch 3 starts):** W2-A's IntelligenceProvider extension shape is frozen and committed before W2-J consumes it. Frozen-contract doc at `.docs/plans/wave-W2/provider-trait-frozen.md`. W2-J cannot start implementation until that doc lands and W2-A merges its trait extension.

### Agent W2-A — DOS-213 Prompt Fingerprinting + IntelligenceProvider extension

- **Spec:** [DOS-213](https://linear.app/a8c/issue/DOS-213).
- **Files owned:** `intelligence/provider.rs` extension for fingerprint hook, `intelligence/prompt_fingerprint.rs` (new), per ADR-0106.
- **Done when:** trait extension committed; `provider-trait-frozen.md` written; clippy lint on direct fingerprint calls outside provider boundary active and passing.

### Agent W2-B — DOS-214 Provenance Rendering + Privacy substrate

- **Spec:** [DOS-214](https://linear.app/a8c/issue/DOS-214).
- **Files owned:** `abilities/provenance/render.rs` (new), per-surface privacy rules consumed by ADR-0108 helper from v1.4.0 DOS-412.
- **Done when:** render output for each ADR-0108 sensitivity tier produces the expected text-elision per surface, fixture-checked; cross-references the v1.4.0 W5 cycle-7 9-channel sensitivity sweep — no new channels introduced without a routing rule.

### Agent W2-C — DOS-215 Temporal Primitives (EngagementCurve + RoleProgression)

- **Spec:** [DOS-215](https://linear.app/a8c/issue/DOS-215).
- **Files owned:** `abilities/temporal/` (new), trait + serializable types.
- **Done when:** trait + types compile inside `abilities-runtime` crate (post-W0.5); trybuild test asserts the trait shape; grep regression confirms no CS-vocabulary terms (account, churn, expansion) in `abilities/temporal/` keeping it domain-neutral.

### Agent W2-D — DOS-234 DbKeyProvider trait seam + LocalKeychain

- **Spec:** [DOS-234](https://linear.app/a8c/issue/DOS-234).
- **Files owned:** `db/key_provider.rs` (new trait), `db/encryption.rs` refactor to consume the trait, LocalKeychain default impl.
- **Done when:** `cargo build` green; LocalKeychain unit test reads test vector; **key-leak fixture in W2 Suite S asserts: no provider call returns key bytes through Provenance Envelope, signal payloads, telemetry sinks (DOS-284 SHA-256 hashing path), or any abilities-runtime surface**; key rotation path defined — durable jobs from W1-C survive a simulated key-rotation event in restart-test (or a v1.4.x follow-up ticket filed and referenced here, no silent deferral per `feedback_no_deferrals_period.md`).

### Agent W2-E — DOS-262 Fail-improve loop wrapper

- **Spec:** [DOS-262](https://linear.app/a8c/issue/DOS-262). Note: classifier wrapper lives in `intelligence/`, not `abilities/` — this is intelligence-adjacent, not claims-adjacent.
- **Files owned:** `intelligence/fail_improve.rs` (new), classifier wrapper.
- **Done when:** classifier handles a known failing fixture and produces an improvement annotation; integrated with intelligence/provider's prompt fingerprint output (W2-A).

### Agent W2-F — DOS-264 Contract-first operations array

- **Spec:** [DOS-264](https://linear.app/a8c/issue/DOS-264).
- **Files owned:** registry **operations** sub-module — `abilities/registry/operations.rs` (new) or equivalent extension scoped to the operations contract only. Does NOT touch the claim-type/FRONTMATTER sub-module owned by W2-G.
- **Done when:** operations contract published as a frozen schema; W5 capability-migration agents cite it as the contract; contract-first lint asserts each ability declares its operations array.

### Agent W2-G — DOS-265 Declarative claim-field → edge-type map

- **Spec:** [DOS-265](https://linear.app/a8c/issue/DOS-265).
- **Files owned:** registry **claim-types/FRONTMATTER_LINK_MAP** sub-module — `abilities/registry/claim_types.rs` (or `abilities/claims.rs` if v1.4.0 layout keeps them separate). Adds the **extension mechanism** (trait/macro). Does NOT add new claim-type variants — those land in W4 with their owning ticket.
- **Done when:** extension mechanism merged; existing CLAIM_TYPE_REGISTRY entries continue to resolve; trybuild test asserts macro shape.

### Agent W2-H — DOS-295 claim-based generation granularity + targeted repair

- **Spec:** [DOS-295](https://linear.app/a8c/issue/DOS-295).
- **Files owned:** `services/claims.rs::targeted_repair_*` functions only (sub-function ownership). Query-shape additions in `services/claims.rs::query_*` if newly introduced; no edits outside the explicit function set.
- **Depends on:** W0-A research design (DOS-241) — L0 plan must cite the DOS-241 ADR's decisions on claim-based generation granularity and accept/reject the DOS-235/236/237 amendment list.
- **Done when:** targeted repair runs against a contradiction-fixture from v1.4.0 bundle 5 and produces the expected delta; signal-driven repair traverses W1-C invalidation queue (matches W1-E frozen contract on signal/job/claim chain).

### Agent W2-I — DOS-315 consolidation job operation + devtools replay

- **Spec:** [DOS-315](https://linear.app/a8c/issue/DOS-315).
- **Files owned:** `bin/replay_consolidation.rs` (new), `services/claims.rs::consolidation_job_*` functions only (sub-function ownership; no overlap with W2-H or W2-L sites).
- **Done when:** replay binary reproduces a consolidation event from a captured fixture; DOS-383 carry-forward (external-client replay framework from v1.4.0) cited — either confirmed closed in v1.4.0 proof bundle, or this agent inherits the surface.

### Agent W2-J — DOS-344 migrate early provider callers

- **Spec:** [DOS-344](https://linear.app/a8c/issue/DOS-344).
- **Files owned:** **explicit allowlist** (not "existing provider callers"). The exact call sites are enumerated in the agent's L0 plan from a fresh grep at v1.4.1 W0.5 close — typical surfaces are `services/intelligence.rs`, `intelligence/glean_provider.rs`, `prepare/*` orchestrators that bypass AbilityContext-routing. Any addition to this list during implementation requires a plan amendment.
- **Depends on:** W2-A trait freeze (provider-trait-frozen.md); W0.5 crate boundary (so the migrated callers cannot reintroduce raw imports).
- **Done when:** every enumerated call site routes through AbilityContext; **no new imports of `crate::db`, `crate::state`, `crate::queries`, raw `rusqlite`, `std::fs`, `tokio::fs` in any abilities body** (verified by re-running the W0.5 import-deny CI check + the `abilities-runtime` crate compile gate).

### Agent W2-K — DOS-347 serialize context-mode transition

- **Spec:** [DOS-347](https://linear.app/a8c/issue/DOS-347).
- **Files owned:** `state.rs::transition_context_mode` (new), refactor 3 command sites in `commands/integrations.rs`.
- **Done when:** regression test races two overlapping calls and asserts post-condition consistency.

### Agent W2-L — DOS-448 reject "global" subject_ref strings at parsers

- **Spec:** [DOS-448](https://linear.app/a8c/issue/DOS-448). Defense-in-depth complement to CLAIM_TYPE_REGISTRY.
- **Files owned:** 5 parser sites: `intel_queue.rs:2951/2974`, `abilities/provenance/ownership.rs:804`, `services/claims.rs:487` (only this single line in services/claims.rs — no overlap with W2-H or W2-I function bodies), `services/trust_extraction.rs:184`.
- **Done when:** all 5 sites reject "global" subject_ref string at parse time; regression test for each site green.

### W2 merge gate

L0 → L2 → L3 → L5 cleared per the Review Ladder. Required artifacts:
- L0 plan approvals per matrix (architect-reviewer for substrate; performance-engineer for hot-path; security-auditor for W2-B/W2-D/W2-J/W2-L)
- Provider-trait freeze doc landed before batch 3 starts
- L2 diff approvals on all 12 PRs
- Single integration commit on top resolves migration numbering inside `migrations/` if any new migrations land
- L3 wave adversarial: codex-challenge + architect-reviewer + **Suite S includes the key-leak fixture (W2-D), the 9-channel sensitivity sweep (W2-B), and the parser-defense regression (W2-L)** + Suite P/E reports
- CI invariants now active per the additions table
- L5 drift check: integrated state matches v1.4.1 project DoD §Substrate completions
- **Mandatory retro** — 12-agent fan-out
- Proof bundle written

---

# Wave 3 — Scoring + trust depth

Closes the v1.4.1 trust story. Builds on v1.4.0 Trust Compiler core.

### Agent W3-A — DOS-10 Domain-aware freshness decay table

- **Spec:** [DOS-10](https://linear.app/a8c/issue/DOS-10).
- **Files owned:** `abilities/trust/freshness_decay.rs` (new), per-DataSource half-life config, integration with W2-A prompt fingerprinting (consumed via the IntelligenceProvider trait extension's fingerprint-on-read hook).
- **Done when:** half-life config covers every `DataSource` variant; freshness factor produces non-flat distribution on bundle 5 (correction-resurrection) fixture; consumed by W3-B factor library.

### Agent W3-B — DOS-238 Scoring Factor Library + Surface Extractors

- **Spec:** [DOS-238](https://linear.app/a8c/issue/DOS-238).
- **Files owned:** `abilities/trust/factors/` (extracted from **v1.4.0** W4-A's mod.rs — disambiguates from this plan's W4-A which is DOS-276 CommitmentClaim), surface extractors per ADR-0114.
- **Depends on:** v1.4.0 W4-A trust core. Refactors that into a library shape.
- **Done when:** factor library compiles; W3-A freshness decay consumed as a factor; thresholds are config-driven (deny-list lint asserts no float literals outside `*_test.rs` and `*/config.rs` in `abilities/trust/`).

### Agent W3-C — DOS-6 Trust scoring shadow-mode rollout (with tuning folded in)

**Note (2026-05-07):** DOS-6 acceptance criteria amended to include the two tuning items that were previously listed as a separate W3-D agent: feedback delta tuning + subject-fit confidence floor tuning. Tuning happens within the shadow-mode rollout where production claim-volume data is already loaded; spinning a separate agent created cross-process tuning conflict. Agent W3-D is removed; W3 now runs 3 agents.

- **Spec:** [DOS-6](https://linear.app/a8c/issue/DOS-6).
- **Files owned:** feature flag `FEATURE_TRUST_COMPILER_SHADOW`, divergence monitor, hourly comparison job, threshold config files for feedback-delta and subject-fit-floor.
- **Wall-clock note:** W3 wall-clock target gates on **data sufficiency**, not elapsed time. Shadow bake runs in the background once instrumentation lands; W4/W5 may proceed in parallel because they don't depend on tuned thresholds. W3 closure waits for the data sufficiency criteria below.
- **Done when (data sufficiency, not "zero alerts"):**
  1. **Volume:** ≥1,000 claim-trust scoring events accumulated in shadow (sample-size check before tuning).
  2. **Surface coverage:** every claim-producing surface in dev (briefing, meeting detail, entity detail, actions, email) emitted at least one shadow score.
  3. **Trust-band distribution:** all three bands (`likely_current`, `use_with_caution`, `needs_verification`) populated with ≥50 events each — a band that never fires is evidence of a broken threshold, not absence of risk.
  4. **Alert-path canary:** at least one synthetic divergence event injected and observed by the alert path; "zero alerts" without canary evidence does not count.
  5. **Threshold deltas captured:** before/after tuning thresholds documented in an ADR with rationale; tuning lands as **data, not code constants**.
  6. **No regression:** Suite E bundles 1-13 still green after threshold update.

### W3 merge gate

L0 → L2 → L3 → L4 cleared per the Review Ladder. Required artifacts:
- L0 plan approvals per matrix
- L2 diff approvals on 3 PRs
- L3 wave adversarial: codex-challenge + Suite P (trust math at v1.4.1 claim volume — no regression vs v1.4.0 baseline) + Suite E (bundles 1-13 still green)
- L4 surface QA: `/qa-only` confirms shadow-mode produces non-trivial distributions on real dev data
- **Mandatory retro** — first shadow-mode rollout
- Proof bundle written

---

# Wave 4 — Claim-shape extensions + canonicalization

Two agents. Adds typed claim shapes that v1.4.0 spine punted on.

### Agent W4-A — DOS-276 CommitmentClaim extraction

- **Spec:** [DOS-276](https://linear.app/a8c/issue/DOS-276).
- **Files owned:** `abilities/extractors/commitment.rs` (new), `CommitmentClaim` struct, structural owner + stable identity + trust integration.
- **Depends on:** W3-A (freshness decay), W3-B (factor library).

### Agent W4-B — DOS-280 Canonicalize semantically duplicate claims

- **Spec:** [DOS-280](https://linear.app/a8c/issue/DOS-280).
- **Files owned:** `services/claims.rs::canonicalize` extension, semantic-similarity collapse rule.
- **Depends on:** W3 trust depth (low-trust duplicates handle differently).

### W4 merge gate

L0 → L2 → L3 cleared per the Review Ladder. Required artifacts:
- L0 plan approvals per matrix
- L2 diff approvals on 2 PRs
- L3 wave adversarial: codex-challenge + Suite E (bundles 1-13 still green; new commitment-extraction fixture; new canonicalization fixture)
- Proof bundle written

---

# Wave 5 — Capability migrations

Three abilities migrate to follow the v1.4.0 W5 pattern (DOS-218 + DOS-219). Run in parallel.

**Pre-W5 boundary precondition:** W0.5 crate boundary live + W2-A trait freeze + W2-F operations contract published. W5 capability migrations land **inside** `abilities-runtime` and consume the contract-first operations array.

### Agent W5-A — DOS-220 migrate get_daily_readiness

- **Spec:** [DOS-220](https://linear.app/a8c/issue/DOS-220).
- **Files owned:** `abilities/get_daily_readiness/` (new, inside `abilities-runtime` crate), parity test against legacy.
- **Done when:** end-to-end run with full Provenance + Trust band; parity vs legacy on bundle-9 fixture green; declares its operations array against W2-F contract.

### Agent W5-B — DOS-221 migrate list_open_loops

- **Spec:** [DOS-221](https://linear.app/a8c/issue/DOS-221).
- **Files owned:** `abilities/list_open_loops/` (new, inside `abilities-runtime` crate), parity test.
- **Done when:** parity vs legacy on bundle-10 fixture green; declares operations array.

### Agent W5-C — DOS-222 migrate detect_risk_shift

- **Spec:** [DOS-222](https://linear.app/a8c/issue/DOS-222).
- **Files owned:** `abilities/detect_risk_shift/` (new, inside `abilities-runtime` crate), parity test.
- **Done when:** parity vs legacy on bundle-11 fixture green; declares operations array.

### W5 merge gate

L0 → L2 → L3 → L5 cleared per the Review Ladder. Required artifacts:
- L0 plan approvals per matrix
- L2 diff approvals on 3 PRs
- L3 wave adversarial: codex-challenge + Suite P (no regression vs legacy) + Suite E (parity green on each migration's named bundle) + **MCP-bridge re-test** (the 3 new abilities respect subject-ownership enforcement DOS-288, ADR-0108 sensitivity rules, signal-payload privacy at the MCP boundary; the bridge is a v1.4.0 surface but new abilities flow through it in v1.4.1)
- L5 drift check: 5 abilities now in registry (2 v1.4.0 pilots + 3 v1.4.1 migrations); contract-first operations array (DOS-264) consistent
- Proof bundle written

---

# Wave 6 — Validation suite + eval depth

Seven agents fan out across the 5 validation axes plus eval-depth tooling.

### Agent W6-A — DOS-282 v1.4.0 edge-case regression suite

- **Spec:** [DOS-282](https://linear.app/a8c/issue/DOS-282).
- **Files owned:** `tests/edge_cases/` extension covering unit + integration + validation layers.
- **Done when:** all 5 validation axes from DOS-289-293 have at least 1 regression each.

### Agent W6-B — DOS-261 BrainBench-style harness

- **Spec:** [DOS-261](https://linear.app/a8c/issue/DOS-261).
- **Files owned:** `bin/brainbench.rs` (new), retrieval + abilities scoring rubric.
- **Depends on:** v1.4.0 eval harness library (`src/harness/`).

### Agent W6-C — DOS-289 stale-current contradiction validation

- **Spec:** [DOS-289](https://linear.app/a8c/issue/DOS-289).
- **Files owned:** validation bundle + assertions for fresh-supersedes-stale; cross-time temporal tests.

### Agent W6-D — DOS-290 cross-surface consistency validation

- **Spec:** [DOS-290](https://linear.app/a8c/issue/DOS-290).
- **Files owned:** validation bundle + assertions for daily/meeting/entity surfaces agreeing on shared claims.

### Agent W6-E — DOS-291 ambiguous identity + primary-context selection

- **Spec:** [DOS-291](https://linear.app/a8c/issue/DOS-291).
- **Files owned:** validation bundle + assertions on subject ambiguity edge cases.

### Agent W6-F — DOS-292 source lifecycle + privacy + actor-specific provenance

- **Spec:** [DOS-292](https://linear.app/a8c/issue/DOS-292).
- **Files owned:** validation bundle 17 + assertions for revoked/restricted source rejection, actor-specific provenance routing.
- **Done when:** validation bundle covers the full **9-channel ADR-0108 sensitivity sweep** baseline established by v1.4.0 W5 cycle-7 + DOS-412 (callouts, prep outputs, MCP responses, Tauri renders, signal payloads, telemetry, eval fixtures, replay, error logs) — parameterized so any new channel introduced in v1.4.1 (BrainBench W6-B, criterion bench W7-F, telemetry W7-E) is automatically covered; revoked/restricted source rejection green for each channel.

### Agent W6-G — DOS-293 sync, refresh, concurrency, partial-failure

- **Spec:** [DOS-293](https://linear.app/a8c/issue/DOS-293).
- **Files owned:** validation bundle + assertions for partial-failure recovery, concurrent enrichment, refresh idempotency.

### W6 merge gate

L0 → L2 → L3 → L4 → L5 cleared per the Review Ladder. Required artifacts:
- L0 plan approvals (qa-expert for all 7 agents; security-auditor for DOS-292)
- L2 diff approvals on 7 PRs
- L3 wave adversarial: codex-challenge + architect-reviewer + **full Suite S re-run** (including the 9-channel ADR-0108 sweep) + **full Suite P re-run** + **Suite E final** (**bundles 1-13 mandatory pass; bundles 14, 15, 16, 17, 18 mandatory pass — all 18 bundles green; no partial-pass cut**)
- L4 surface QA: full `/qa` against v1.4.1 build
- L5 drift check: integrated state matches v1.4.1 project DoD §Validation suite — five new bundles owned, named, and quarantine policy documented (cite per-bundle owner: W6-C/D/E/F/G; flake escalation = single bundle quarantine + ticket; no release with a quarantined bundle)
- **Mandatory retro** — validation matrix expansion
- Proof bundle written

---

# Wave 7 — Release gate hardening + close

Six agents close out v1.4.0 carry-forward hardening + final cleanup.

### Agent W7-A — DOS-285 Linear issues on entity pages

- **Spec:** [DOS-285](https://linear.app/a8c/issue/DOS-285). DOS-75 was the v1.2.x parent; carry-forward path: DOS-75 → DOS-285 in v1.4.1 (verified open, not in v1.4.0 leftover list).
- **Files owned:** entity page component for Linear issue display.
- **Done when:** entity page renders Linear issue chapter; respects ADR-0108 sensitivity (issues from internal-only projects don't leak to MCP responses).

### Agent W7-B — DOS-439 release-gate subprocess timeout + bounded output

- **Spec:** [DOS-439](https://linear.app/a8c/issue/DOS-439). v1.4.0 cycle-4 hardening.
- **Files owned:** `release_gate.rs::run_dos288_selector` timeout machinery.

### Agent W7-C — DOS-440 build.rs SHA worktree-aware watching

- **Spec:** [DOS-440](https://linear.app/a8c/issue/DOS-440). v1.4.0 cycle-4 hardening.
- **Files owned:** `src-tauri/build.rs` worktree resolution.

### Agent W7-D — DOS-441 build.rs source-only fail-fast

- **Spec:** [DOS-441](https://linear.app/a8c/issue/DOS-441). v1.4.0 cycle-4 hardening.
- **Files owned:** `src-tauri/build.rs` source-only branch + DAILYOS_BUILD_SHA opt-in.

### Agent W7-E — DOS-260 opt-in anonymous aggregate telemetry

- **Spec:** [DOS-260](https://linear.app/a8c/issue/DOS-260).
- **Files owned:** telemetry collector + client emission + user toggle.
- **Closes:** v1.4.1 background lane started by DOS-284.

### Agent W7-F — DOS-348 criterion bench harness for v1.4.x hot paths

- **Spec:** [DOS-348](https://linear.app/a8c/issue/DOS-348). Skipped by W1 + W2 retros in v1.4.0; routed to v1.4.1 W7 close 2026-05-07.
- **Files owned:** `src-tauri/Cargo.toml` `[[bench]]` sections + `criterion` dev-dep, `src-tauri/benches/{mutation_gate,write_fence,provider_hotswap}.rs` (new), `scripts/run-suite-p.sh`, baseline JSON at `.docs/perf/baseline-v1.4.1.json`, `.docs/perf/README.md`.
- **Done when:** `bash scripts/run-suite-p.sh` runs all three benches and writes a results JSON; baseline captured at v1.4.1-W7-complete; README documents the per-wave compare workflow.
- **Out of scope (defer to a later v1.4.x):** wiring Suite P into CI as a merge gate.

### W7 merge gate = release gate

Same shape as v1.4.0 W6 release gate, expanded for v1.4.1's wider validation matrix. Required artifacts:
- L0 → L2 → L3 → L4 → L5 cleared per the Review Ladder (L5 explicit on the release: integrated state matches v1.4.1 project DoD verbatim)
- All capability migrations parallel-run validated (W5 gate already verified this)
- Full adversarial fixture matrix green (W6 gate already verified this)
- Signal infrastructure load-test gate passes (W1 gate already verified this)
- Trust scoring shadow mode emits non-trivial distributions across all three trust bands (≥50 events per band); feedback deltas tuned with documented threshold deltas (W3 gate already verified this)
- `cargo clippy -D warnings && cargo test && pnpm tsc --noEmit` green
- **`pnpm release-gate -- --mode hermetic` exits zero against bundles 1-18** (all 18 mandatory; `release-gate` config promoted to enumerate 14-18 alongside 1-13 with quarantine policy documented per W6 gate)
- **MCP-bridge re-test green** for the 3 new W5 capabilities (subject-ownership DOS-288, ADR-0108 sensitivity, signal-payload privacy)
- **Suite P baseline captured** at v1.4.1-W7-complete (W7-F deliverable); regression band against v1.4.0 baseline-w2 documented
- Manual dogfood evidence captured against ≥20 real-dev meetings
- Proof bundle written
- Tag `v1.4.1` on `trunk` after `dev` merge (gated on user release-checklist + UI validation per `feedback_no_auto_tag_without_user_validation.md`)

---

# Background lane — DOS-284 telemetry

Single agent, runs from W0 onward. Independent of the spine.

- **Spec:** [DOS-284](https://linear.app/a8c/issue/DOS-284). Per-dimension output-hash logging in `run_parallel_enrichment()`.
- **Files owned:** SHA-256 logging in `intelligence/parallel_enrichment.rs`, telemetry sink.
- **Done when:** logging deployed at start of v1.4.1 window so two weeks of telemetry accumulate before any v1.4.x DOS-204 chapter-TTL revisit.

---

# L0 questions — resolved 2026-05-07 (founder) + cycle-1 L0 review revisions

The eight L0 ambiguities from the first draft were resolved by the founder before plan freeze. The cycle-1 L0 review (2026-05-07; codex adversarial + architect-reviewer + code-reviewer + security-auditor) returned 1 BLOCK + 3 REVISE; the consolidated revision pass folded those findings into this plan version. Headline structural changes vs first draft:

- **W4.5 → W0.5.** Crate-split moved BEFORE substrate waves so all subsequent W2/W3/W4 work is born inside `abilities-runtime` with the boundary live (architect F1, codex F2 — both BLOCK).
- **W2 sub-file ownership.** `services/claims.rs` split at function granularity across W2-H/W2-I/W2-L; `abilities/registry.rs` split into operations sub-module (W2-F) and claim-types sub-module (W2-G). Provider-trait freeze gates batch 3 (architect F2, codex F1).
- **W2-J explicit allowlist + import deny-list.** Replaces "existing provider callers" with an enumerated list at L0 plan time + boundary-import-deny CI re-run on done (security F1).
- **W3 sufficiency gate.** Replaces "7 days, zero alerts" with 6 explicit data-sufficiency criteria (volume, surface coverage, band distribution, alert canary, threshold deltas, no regression). Wall-clock decoupled from W4/W5 (codex F3, code-reviewer §4).
- **W7 release gate enumerates bundles 1-18.** No partial-pass cut; per-bundle owner; quarantine policy explicit (codex F4).
- **DOS-241 → W1 hard input.** W1-B/C/D L0 plans must cite the DOS-241 ADR's accepted/rejected amendments; not advisory (codex F5).
- **Security additions.** Key-leak fixture in W2 Suite S (W2-D); 9-channel ADR-0108 sweep parameterized in W6-F; key-rotation in W2-D done-when; MCP-bridge re-test in W5/W7 gates.

Founder resolution items 1-8 below remain canonical:

1. **DOS-263 vs DOS-236.** Resolved: fold. DOS-263 marked `duplicateOf=DOS-236`; DOS-236 carries the schema spec. W1 lists W1-C as "DOS-236 + DOS-263 fold-in."

2. **Feedback delta tuning + subject-fit confidence floor tuning.** Resolved: fold into DOS-6 shadow-mode rollout. Two acceptance criteria appended to DOS-6 (comment dated 2026-05-07). No standalone tuning agent in W3; W3 runs 3 agents not 4.

3. **DOS-241 research scope.** Resolved: design must specify how enrichment is reshaped with abilities **for the surfaces that consume it** — briefing, meeting detail, entity detail (account/project/person), actions, and email. Section A audit must include per-surface consumer mapping so propose/commit + trust integration decisions are evaluated per consumer surface, not pipeline-by-pipeline. Comment on DOS-241 dated 2026-05-07.

4. **DOS-314 status.** Resolved: reclassified Duplicate → Done 2026-05-07. Substrate landed in v1.4.0 DOS-7 amendment B. Moved from v1.4.1 project to v1.4.0 project so it counts as completed scope, not canceled scope, in v1.4.0 retrospectives.

5. **Validation bundle naming.** Resolved: Option A — five new bundles 14, 15, 16, 17, 18, one per DOS-289-293. Mandatory in v1.4.1 release gate alongside existing bundles 1-13.

6. **W6 mandatory bundles for v1.4.1 release gate.** Resolved: Option A — bundles 1-13 + 14-18 all mandatory. No partial-pass cut.

7. **v1.4.0 carry-forwards not yet in v1.4.1.** Resolved: Linear sweep 2026-05-07 found two unprojected v1.4.x follow-ups — DOS-348 (criterion bench harness) and DOS-349 (ability-runtime crate split). Both routed to v1.4.1: DOS-348 → W7-F (release-gate close), DOS-349 → W4.5-A (hard precondition for W5 capability migrations). Plan updated.

8. **Codex prompt sizing for W2's 12 agents.** Resolved: stage in 3 batches of 4. Batch boundaries memorialized in the W2 section above. Initial implementation launches batched; L2 cycles can interleave once queued.

---

# Summary

| | |
|---|---|
| Total tickets | 47 active (DOS-348 + DOS-349 routed in 2026-05-07; DOS-314 reclassified Done in v1.4.0 project; DOS-263 folded into DOS-236; DOS-312/313 confirmed canceled and removed from project) |
| Wave units | 9 (W0, W0.5, W1, W2, W3, W4, W5, W6, W7) + 1 background lane |
| Total agents | 47 — W0=7 + W0.5=1 + W1=5 + W2=12 + W3=3 + W4=2 + W5=3 + W6=7 + W7=6 + background=1 |
| Mandatory retros | W1, W2, W3, W6 |
| Estimated wall-clock | ~3 weeks across the wave ladder; W3 gates on data sufficiency, not elapsed time, so W4/W5 may run in parallel with the shadow bake |
| Substrate completion target | All capability migrations parallel-validated; bundles 1-18 mandatory green; signal infra durable + load-tested with full channel enumeration; scoring shadow-mode tuned with documented threshold deltas; ability-runtime crate boundary compile-time-enforced |
| Definition of Done | matches v1.4.1 project DoD verbatim |
