# VERDICT: BLOCKED

1. **CRITICAL — W2 violates the outer/inner contract.** Evidence: W2 says account inner blocks are `Composition.sections[0..3]` views at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W2-entity-surfaces.md:197`, `:207`, `:218`, `:224`, and AC-W2.6 requires `AbilityOutput<Composition>`/`Composition.sections[].blocks[]` at `:585`. The landed ability returns `AbilityResult<EntityIntelligenceEnvelope>`, not `Composition`, at `src-tauri/abilities-runtime/src/abilities/get_entity_intelligence/mod.rs:44`. The W1 envelope sections are only `Facts, Health, MetadataProposals, OpenLoops, Touchpoints, Threads, Record` at `src-tauri/abilities-runtime/src/abilities/get_entity_intelligence/contracts.rs:68`. W2’s health/context/work/record view split cannot trace each named inner block to those variants; e.g. `outlook`, `on_track`, `stakeholders`, `commercial_shape`, `linear_issues`, `appendix`, and `recommended_actions` are not envelope fields. Recommended fix: reopen W1 or amend W2 so the producer emits real ADR-0130 `Composition` blocks from `BlockType` (`src-tauri/abilities-runtime/src/abilities/composition.rs:330`) or explicitly map every inner block to the 7 `EnvelopeSection` variants with missing slices removed. Path-α candidate: **no**.

2. **LOW — The cited skeleton proves invocation shape only.** Evidence: W2 cites skeletons at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W2-entity-surfaces.md:562`; `git show 00f38b3b:wp/dailyos/blocks/account-detail/render-functions.php` does use `$scope_set` as the third `invoke_ability` arg at lines `60` and `64-70`, and inner receipt/feedback calls also pass `$scope_set` at lines `133-142` and `148-152`. But the same skeleton only echoes `$content` at lines `90-98`; it does not prove typed inner-block `Composition` consumption. Recommended fix: cite it only for the 3-arg runtime gate, not for the W2 Composition contract. Path-α candidate: **no**.

3. **HIGH — DOS-725 tint decision is not locked in code or ADR.** Evidence: W2 claims an ADR-0077 amendment and `$stub_tints['dailyos_project'] = 'olive'` at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W2-entity-surfaces.md:273`, `:298`, `:587`. `chrome_config()` has no `$stub_tints`; it returns direct branch arrays, and the project branch lacks `tint` at `wp/dailyos/theme/functions.php:161` and `:222`. ADR-0077 only lists olive as a color token at `.docs/decisions/0077-magazine-layout-editorial-redesign.md:75`; no amendment exists. Recommended fix: land/amend ADR-0077 before L0 close and update the packet to the real `chrome_config()` structure. Path-α candidate: **no**.

4. **HIGH — AgentMcp placeholder leaks a timing/existence oracle.** Evidence: W2 Option A exposes “timestamp + trust band + lifecycle” at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W2-entity-surfaces.md:726` and explicitly wants the agent to know the touchpoint exists at `:729`. W1 AC-341.12 forbids AgentMcp source-asof timestamps as a timing oracle and forbids graph IDs at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W1-substrate-gaps.md:939` and `:973`; the builder nulls `source_asof`/`updated_at` for AgentMcp at `src-tauri/src/services/claim_receipt/privacy.rs:321` and `:328`. Recommended fix: choose Option B, or define a coarse aggregate coverage signal with no per-item timestamp. Path-α candidate: **no**.

5. **CRITICAL — Meeting Detail is a W1 substrate gap, not L1 verification.** Evidence: W2 invokes `entity_type=meeting` and names agenda/attendees/context bundle/post-meeting slices at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W2-entity-surfaces.md:363` and `:369-376`. `EntityKind` only has Account/Project/Person at `src-tauri/abilities-runtime/src/abilities/get_entity_intelligence/contracts.rs:28`, and `EnvelopeSection` has no meeting-specific variants at `:68`. Wave AC-W5 says mid-W2 substrate gaps reopen W1, not defer, at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-wave-plan.md:310-314`. Recommended fix: C4 reopen W1 for Meeting entity/slices or remove Meeting Detail from W2. Path-α candidate: **no**.

6. **HIGH — List shells invent a contract outside W1.** Evidence: W2 defines `ListEnvelope<T>` without `cursor_state` at `.docs/plans/v1.4.4-wp-surface-migration/L0-packet-W2-entity-surfaces.md:405` and uses `sections: [Facts, Vitals]` at `:421`; W1’s actual `Paginated<T>` includes `cursor_state` at `src-tauri/abilities-runtime/src/abilities/get_entity_intelligence/contracts.rs:139`, and `Vitals` is not an `EnvelopeSection` at `:68`. Recommended fix: consume the W1 `Paginated<T>`/`CursorState` contract or add a W1 list ability and section enum amendment. Path-α candidate: **no**.

VERDICT: BLOCKED
