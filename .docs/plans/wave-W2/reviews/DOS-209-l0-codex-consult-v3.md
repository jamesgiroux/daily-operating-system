# L0 review cycle 3 — DOS-209 plan v3 — codex consult mode

**Reviewer:** /codex consult (cycle 3; cycle-2 verdict was APPROVE)
**Plan revision under review:** v3 (2026-04-29)
**Verdict:** APPROVE

## Cycle-3 delta verification

### NF1 (catalogue + audit script) — landed cleanly: yes
The L6 packet authorized only a script-based re-audit, committed catalogue output, and a no-drift CI test for DOS-209 (escalation lines 69-72, 104-106). Plan v3 adds that binding in §2/§3/§9: the PR must include `src-tauri/tests/dos209_mutation_catalog.rs` generated from the audit script (line 92), the catalogue is generated by `scripts/dos209-mutation-audit.sh` and checked against `src-tauri/tests/dos209_mutation_catalog.txt` (lines 108-109), and `mutation_catalog_no_drift` re-runs the script and asserts exact snapshot match (line 399). I also verified the script prints the deterministic catalogue (script lines 288-294), the script output matches the snapshot, and the five cycle-2 misses are now present in the plan/snapshot: `accounts::snooze_triage_item` (plan line 133), `emails::{unarchive_email,unsuppress_email,pin_email}` (lines 180-183), and `entity_linking::rules::p2_thread_inheritance::evaluate` (line 209).

### NF2 (landing order amendment) — landed cleanly: yes
L6 chose option 1A, amending DOS-209 so W2-B lands first; the Linear 2026-04-29 decision comment says "Landing order: 1A" and the issue description now contains the amendment block. Plan v3 restates that amendment verbatim in §1, preserving the original dependency line while explicitly superseding it (lines 11-17, 57-59), and §7 makes W2-B-first the frozen DOS-209 contract rather than coordination guidance (lines 377-381). This resolves the contract conflict identified in the L6 packet (lines 25-30) without weakening the W2 file-ownership constraints in the wave protocol, where W2-A owns service mutation boundaries and W2-B owns provider modules (wave protocol lines 432-444).

### NF3 (full-suite CI command) — landed cleanly: yes
The L6 packet requires restoring the full `cargo clippy && cargo test && pnpm tsc --noEmit` command, with targeted DOS-209 tests only as supplemental evidence (lines 70-72, 104-105). Plan v3 §9 restores the full regression command, including Rust clippy, Rust tests, and TypeScript checking, then separately names `cargo test ... dos209` as additional evidence, not a replacement (line 404). That is aligned with the W2 merge gate's required full suite (wave protocol line 453).

## Scope discipline

### Cycle 3 changes confined to the three authorized items: yes
The L6-authorized DOS-209 scope was exactly catalogue/audit/no-drift, landing-order reconciliation, and full-suite CI restoration (escalation lines 67-72, 98-106; Linear 2026-04-29 decision comment decisions 1, 3, 4, 5). Plan v3's own revision history lists only those three closures (line 5), and the new material I found maps to those items: amendment text in §1/§7, catalogue/audit text and generated rows in §2/§3/§9, and the restored CI command in §9. The cycle-2 APPROVE conditions remain intact: DOS-304 capability-boundary handling is still present (plan line 78), caller construction surfaces remain explicit (lines 84-90), Intelligence Loop answers remain concrete (lines 364-369), the W1 hard pre-code gate remains binding (line 375), and the implementation spine I approved in cycle 2 remains present across `ServiceContext` carriers, visibility, errors, transactions, tests, and CI (cycle-2 review lines 28-32; plan lines 82-104, 391-404).

## Fresh findings (if any)

None.

## Verdict rationale

v3 preserves the architectural soundness I approved in cycle 2 while closing the three L6-authorized blockers. NF1 is now mechanically enforceable through an audit script, committed snapshot, and no-drift test; NF2 is backed by the actual Linear ticket amendment instead of a plan-only sequencing choice; NF3 restores the full W2 regression suite required by the wave protocol. I found no new v3 text that contradicts the approved `ServiceContext`/capability-boundary shape, the W1 pre-code gate, or the W2-A/W2-B ownership split.

## If APPROVE

v3 preserves cycle-2 APPROVE conditions + closes the three NF blockers cleanly + scope-disciplined.
